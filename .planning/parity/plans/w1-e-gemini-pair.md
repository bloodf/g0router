# w1-e — openai→gemini request translator + gemini→openai response translator + filterToOpenAIFormat

Rows: PAR-TRANS-023, PAR-TRANS-024, PAR-TRANS-025, PAR-TRANS-026, PAR-TRANS-027, PAR-TRANS-028, PAR-TRANS-029, PAR-TRANS-030 (request direction, plain Gemini path); PAR-TRANS-043 (response direction); PAR-TRANS-010 (`filterToOpenAIFormat` helper).
Touched-not-implemented: PAR-TRANS-004/005 (pipeline normalization — already HAVE since w1-b via `Registry.TranslateRequest`/`TranslateResponse` at `internal/translation/registry.go:95-166`; task 5 only exercises the new registrations through that existing pipeline — no row state change claimed by this plan).
NOT in scope: PAR-TRANS-039/040 (Antigravity REQUEST envelope wrap/unwrap — w1-f), gemini-cli Cloud Code envelope and thinking signatures (`openai-to-gemini.js:229-269` — w1-f), Vertex (w1-f), Gemini-format *clients* (`request/gemini-to-openai.js` — w1-j).

Row-043 boundary clarification (read before judging task 3 scope): PAR-TRANS-043's frozen-ref citation is `open-sse/translator/response/gemini-to-openai.js:5-245` — the WHOLE function, whose first statement is the wrapper-tolerance line (`:9`, verbatim `const response = chunk.response || chunk;`). That single line is in-row 043 behavior of this one shared function (the ref registers the SAME function for gemini/gemini-cli/antigravity/vertex at `:241-245`). Rows 039/040 cover the Antigravity REQUEST-side envelope construction and request unwrapping in different files (`request/openai-to-gemini.js:271-447`, `request/antigravity-to-openai.js`) — none of that is ported here, and only the `gemini:openai` direction is registered.

Wave 1 plan 5 of ~10. Worker: Kimi. Reviewer: gpt-5.5 diff gate.
Precondition check — worker runs BEFORE any edit and STOPs with IMPL-BLOCKED if any fails (each guards a dependency this plan builds on):
1. `grep -n "func NewRegistry" internal/translation/registry.go` prints one match — guards PAR-TRANS-001/002 registry (merged by w1-b; current main has it at `internal/translation/registry.go:55`).
2. `grep -n "FormatGemini" internal/translation/formats.go` prints at least one match — guards PAR-TRANS-003 format constants (merged by w1-b in `internal/translation/formats.go`).

## Production-wiring scope ruling (read before judging dead code)
Same ruling as w1-d: these translators register the `openai:gemini` (request) and `gemini:openai` (response) directions in `NewRegistry`. Their production consumer is the provider dispatch layer, sequenced by the user-approved wave map at `.planning/parity/plans/WAVE-MAP.md:14`, verbatim: `| 2 | Providers: all 43+ adapters, executors, model catalogs, full Bifrost-size interface (decision 9) with typed not-implemented stubs | PAR-PROV 66 + ~25 PR ports | 10–14 | 1 | Adapters consume translator; interface expansion lands here once |`. The Wave 1 deliverable is the registered, unit-tested engine exercised end-to-end through the registry pipeline (task 5). The existing `internal/providers/gemini/converter.go` is NOT modified by this plan — it is replaced by registry consumption in Wave 2.

## Matrix corrections honored by this plan
- **PAR-TRANS-030** (corrected 2026-06-09, see row footnote + GATE-RESOLUTION.md): the plain OpenAI→Gemini path DROPS `tool_choice` entirely; no `toolConfig` is emitted. The translator here must NOT invent a mode mapping.

## File ownership (exclusive while in flight)
- `internal/translation/gemini_helpers.go`, `gemini_helpers_test.go` — NEW (name sanitizer, content→parts, schema cleaner, safety settings)
- `internal/translation/openai_gemini_request.go`, `openai_gemini_request_test.go` — NEW
- `internal/translation/gemini_openai_response.go`, `gemini_openai_response_test.go` — NEW
- `internal/translation/openai_filter.go`, `openai_filter_test.go` — NEW (PAR-TRANS-010)
- `internal/translation/registry.go`, `registry_test.go` — two registration lines in `NewRegistry`, additive-only `StreamState` fields needed by task 3 (`StreamState` is defined in this file at `internal/translation/registry.go:15-31`; w1-b/w1-d precedent), + wiring/pipeline tests

## Tasks (TDD order — each test fails before its fix)

1. **Rows 027/028/029 + helper substrate consumed by rows 023-026: Gemini helpers** (`gemini_helpers.go`). TDD: write every test listed at the end of this task FIRST (all fail — the package does not compile until the helpers exist), then port from `_refs/9router/open-sse/translator/helpers/geminiHelper.js` and `request/openai-to-gemini.js:26-36` (read both files whole). The non-027/028/029 helpers below are not extra scope — each is invoked inside the cited line ranges of this plan's request-direction rows: `DEFAULT_SAFETY_SETTINGS` is referenced in the base shape (`openai-to-gemini.js:44`, row 023's function), `convertOpenAIContentToParts` at `:98` (row 024's cited 92-97 sits in the same message loop; user-branch consumer), `extractTextContent` at `:95,118` (row 024 systemInstruction text + assistant text), `tryParseJSON` at `:129,164` (row 026 tool-call args + tool-response parsing). Porting a row's cited code without its callees is impossible; they land here once, tested directly:
   - **027** `sanitizeGeminiFunctionName` (`openai-to-gemini.js:26-36`): empty → `"_unknown"`; replace every char not in `[a-zA-Z0-9_.:\-]` with `_`; if first char is not `[a-zA-Z_]`, prefix `_`; truncate to 64.
   - **029** `unsupportedSchemaConstraints` slice — the exact keyword list from `geminiHelper.js:4-23` (basic constraints, `default`/`examples`, meta keywords, object validation keywords, `anyOf/oneOf/allOf/not`, dependency keywords, `title/if/then/else/contentMediaType/contentEncoding`, the 10 UI styling keys; count corrected 2026-06-10 — geminiHelper.js:21-22 lists exactly ten: cornerRadius, fillColor, fontFamily, fontSize, fontWeight, gap, padding, strokeColor, strokeThickness, textColor).
   - **028** `cleanJSONSchemaForGemini` (`geminiHelper.js:298-371`, ref name `cleanJSONSchemaForAntigravity` — keep a comment noting the ref name) — five phases in ref order: (1) `convertConstToEnum` (`:148-162`: `const`→single-element `enum` only when no `enum` exists) + `convertEnumValuesToStrings` (`:164-181`: enum values stringified via `String()`; adds `type:"string"` when enum present and `type` missing); (2) `mergeAllOf` (`:183-215`: properties of all items merged into parent `properties`, `required` union-merged with dedup, `allOf` deleted) + `flattenAnyOfOneOf` (`:244-273`: filter out `type:"null"` variants, pick the best remaining via `selectBest` (`:217-242`: score object/properties=3 > array/items=2 > other non-null typed=1, first max wins), delete the keyword, `Object.assign` the selected schema onto the parent) + `flattenTypeArrays` (`:275-289`: `type:[...]` collapsed to first non-null entry else `"string"`); (3) `ensureObjectType` (`:291-296`: `properties` present without `type` → `type:"object"`, recursive); (4) `removeUnsupportedKeywords` recursively at all levels including inside arrays (`:124-146` — also strips keys starting `x-`); (5) `cleanupRequired` (filter `required` to existing properties; delete when empty) then `addPlaceholders` (`type:object` with empty/missing `properties` gets the verbatim placeholder `{reason:{type:"string",description:"Brief explanation of why you are calling this tool"}}` with `required:["reason"]`, recursive) — both inner functions of `:298-371`.
   - `DefaultSafetySettings` — the 5 verbatim categories all `threshold:"OFF"` (`geminiHelper.js:26-32`).
   - `convertOpenAIContentToParts` (`geminiHelper.js:35-81`): string → single text part; array items: `text`→text part; `image_url` data-URI → `inlineData {mime_type, data}` split at first comma with mime from `data:`-prefix before `;`; `image_url` http(s) → `fileData {fileUri, mimeType:"image/*"}`; `input_audio` → `inlineData` with mime `audio/mpeg` for mp3 else `audio/<format>` (default format `wav`); `audio_url` data-URI → `inlineData` like images. Unknown types dropped.
   - `extractTextContent` (`geminiHelper.js:85-91`: string passthrough; arrays → join of `type:"text"` block texts; else `""`) + `tryParseJSON` (`:94-101`: non-string returned as-is; parse failure → nil).
   Tests (`gemini_helpers_test.go`): `TestSanitizeGeminiFunctionName` (027: empty, invalid chars, bad first char, >64 truncation), `TestCleanSchemaRemovesUnsupportedKeywords` (029: each keyword family incl. `x-*` and one UI styling key), `TestCleanSchemaConstToEnum`, `TestCleanSchemaEnumToStrings` (numeric enum → strings + injected `type:"string"`), `TestCleanSchemaFlattenAnyOf` (null variant dropped, object variant preferred over string variant per selectBest), `TestCleanSchemaMergeAllOf`, `TestCleanSchemaTypeArrays`, `TestCleanSchemaEnsureObjectType`, `TestCleanSchemaRequiredCleanup`, `TestCleanSchemaEmptyObjectPlaceholder` (028: verbatim `reason` placeholder), `TestConvertContentParts` (text, data-URI image, http image, input_audio mp3/wav, audio_url, unknown dropped).
2. **Rows 023/024/025/026/030: openai→gemini request translator** (`openai_gemini_request.go`), registered request direction `openai:gemini`. TDD wiring first: `TestNewRegistryWiresOpenAIGeminiRequest` asserting `NewRegistry().RequestTranslatorFor(FormatOpenAI, FormatGemini) != nil` fails, then port `openaiToGeminiBase` + `openaiToGeminiRequest` (`openai-to-gemini.js:39-226`):
   - base shape: `{model, contents:[], generationConfig:{}, safetySettings: DefaultSafetySettings}` (41-46).
   - **023**: `temperature`→`generationConfig.temperature`, `top_p`→`topP`, `top_k`→`topK`, `max_tokens`→`maxOutputTokens` — each only when present (48-59).
   - pre-pass maps (61-83): `tcID2Name` from assistant `tool_calls` (function type with id+name); `toolResponses` from `role:tool` messages keyed by `tool_call_id`.
   - **024**: `role:system` → `systemInstruction {role:"user", parts:[{text}]}` ONLY when `len(messages) > 1`; a lone system message becomes a `role:user` content (91-97).
   - user messages → `convertOpenAIContentToParts`, pushed only when parts nonempty (97-100).
   - **025**: assistant `reasoning_content` → part `{thought:true, text}` followed by separator part `{thoughtSignature: signature, text:""}` (104-115) where `signature` is the default thinking signature constant — port the byte-exact value of `DEFAULT_THINKING_AG_SIGNATURE` from `_refs/9router/open-sse/config/defaultThinkingSignature.js` as a package constant.
   - **026**: assistant tool_calls → parts `{thoughtSignature: signature, functionCall:{id, name: sanitized, args: tryParseJSON(arguments or "{}")}}` pushed with any text parts as one `role:"model"` content; then, when any called id has a recorded tool response, a `role:"user"` content of `functionResponse` parts follows immediately — name from `tcID2Name` with the ref's id-split fallback (split on `-`, drop last two segments when >2 parts, else whole id); response body `tryParseJSON(resp)`, non-JSON → `{result: resp}`, non-object → `{result: parsed}`, wrapped as `response: {result: parsedResp}` (124-182). Assistant without tool_calls → `role:"model"` content when parts nonempty (183-185).
   - tools (190-219): Claude-format entries (`name` + `input_schema`) and OpenAI-format entries (`type:"function"`) both → `functionDeclarations {name: sanitized, description: "" default, parameters: cleanJSONSchemaForGemini(deep-copied schema or {type:object,properties:{}})}`; emitted as `tools: [{functionDeclarations}]` only when nonempty.
   - **030** (corrected row): `tool_choice` is read NOWHERE; assert no `toolConfig` key in output.
   Tests (`openai_gemini_request_test.go`): `TestNewRegistryWiresOpenAIGeminiRequest`, `TestOpenAIGeminiGenerationConfig` (023: all four params + absent-param omission), `TestOpenAIGeminiSystemInstruction` (024: multi-message vs lone-system), `TestOpenAIGeminiReasoningContentThoughtParts` (025: thought part + signature separator order), `TestOpenAIGeminiToolCallPairing` (026: functionCall parts then functionResponse user content; id-split name fallback; non-JSON tool response wrapping), `TestOpenAIGeminiToolsCleaned` (sanitized name + cleaned schema + claude-format tool accepted), `TestOpenAIGeminiNoToolConfig` (030: body with `tool_choice:"auto"` and object forms produces output without `toolConfig`).
3. **Row 043: gemini→openai response translator** (`gemini_openai_response.go`), registered response direction `gemini:openai`. TDD wiring first: `TestNewRegistryWiresGeminiOpenAIResponse` fails, then port `response/gemini-to-openai.js:5-245`:
   - nil/no-candidate chunks → nil; wrapper tolerance via `chunk.response || chunk` (`:9` — in-row 043 behavior per the Row-043 boundary clarification above; this is one line of the row's cited function, not Antigravity scope).
   - first chunk: state init `messageId` from `responseId` or `msg_<now>`, `model` from `modelVersion` or `"gemini"`, role chunk emitted (17-32). All emitted chunk ids are `chatcmpl-<messageId>`; `created` is current unix seconds.
   - parts loop (35-165): parts with `thoughtSignature`/`thought_signature` — text content emitted as `reasoning_content` delta when `thought:true` else `content` delta; functionCall handled inline then `continue` (37-93). Plain text parts → content delta (96-108). functionCall parts → tool_calls delta with `id: <name>-<unixms>-<index>`, sequential `state.functionIndex`, `arguments: JSON.stringify(args or {})`, name decloaked via `state.ToolNameMap` when present (111-141); record in `state.ToolCalls`. `inlineData`/`inline_data` with data → `delta.images` `[{type:image_url, image_url:{url:"data:<mime>;base64,<data>"}}]`, mime default `image/png` (144-163).
   - usage (168-208): from `usageMetadata` — `prompt_tokens = promptTokenCount`; `candidatesTokens` fallback `total - prompt - thoughts` clamped ≥0 when zero with total>0; `completion_tokens = candidates + thoughts`; `total_tokens = totalTokenCount`; `prompt_tokens_details.cached_tokens` when cached>0; `completion_tokens_details.reasoning_tokens` when thoughts>0; stored on state.
   - finishReason (211-238): lowercase; `stop` with recorded tool calls → `tool_calls`; final chunk `delta:{}` with finish_reason and `usage` attached when state has it; `state.FinishReason` set.
   - StreamState gains the fields this requires (`FunctionIndex int`, `ToolNameMap map[string]string` if not already present) — additive only.
   Tests (`gemini_openai_response_test.go`): `TestNewRegistryWiresGeminiOpenAIResponse`, `TestGeminiOpenAIFirstChunkRole`, `TestGeminiOpenAIThoughtParts` (thought:true → reasoning_content; signature + plain text → content), `TestGeminiOpenAIFunctionCallDelta` (sequential indices, JSON args, name map), `TestGeminiOpenAIInlineImage`, `TestGeminiOpenAIUsageMapping` (cached + thoughts details, candidates fallback), `TestGeminiOpenAIFinishReasonToolCalls` (stop→tool_calls promotion + usage on final chunk), `TestGeminiOpenAIResponseKeyUnwrap` (row 043 `:9`: chunk nested under `response` key parses identically to a bare chunk).
4. **Row 010: `filterToOpenAIFormat`** (`openai_filter.go`). TDD first: `TestFilterToOpenAIFormat*` fail, then port `_refs/9router/open-sse/translator/helpers/openaiHelper.js:9-129`:
   - `developer` role → `system` (14).
   - `role:tool` and assistant-with-tool_calls messages pass untouched (17-20).
   - string content passes (23); array content filtered: drop `thinking`/`redacted_thinking`; keep `VALID_OPENAI_CONTENT_TYPES` (`text,image_url,image,input_audio,audio_url`) with `signature` and `cache_control` keys stripped; `tool_use` dropped; `tool_result` kept cleaned; all-filtered → `[{type:text,text:""}]` (26-53).
   - second pass drops messages whose content is empty/whitespace-only text — but NEVER `role:tool` or assistant-with-tool_calls (60-74).
   - empty `tools` array deleted (77-79); tools normalized to OpenAI format from Claude (`{name,input_schema}`) and Gemini (`functionDeclarations`) shapes with flatten (81-112); object `tool_choice` normalized: `{type:auto}`→`"auto"`, `{type:any}`→`"required"`, `{type:tool,name}`→`{type:function,function:{name}}` (115-126).
   This is a standalone exported helper (`FilterToOpenAIFormat(body map[string]any) map[string]any`); its production call sites are the source-format request translators that 9router invokes it from — wired when those translators land (claude→openai already merged in w1-b predates this helper; the w1-b translator's own filtering stays as-is, and consolidation onto this helper is a Wave-2 refactor concern, NOT this plan).
   Tests: `TestFilterDeveloperRole`, `TestFilterThinkingBlocks` (thinking dropped, signature/cache_control stripped, empty→placeholder text), `TestFilterEmptyMessagesKeepsToolMessages`, `TestFilterToolsNormalization` (claude + gemini shapes, empty array deleted), `TestFilterToolChoiceNormalization` (all three object forms).
5. **Pipeline verification over the new registrations** (`registry_test.go`) — exercises the already-merged PAR-TRANS-004/005 pipeline (see Touched-not-implemented header line; `internal/translation/registry.go:95-166` on current main) against the directions registered by tasks 2-3, exactly as the frozen ref only ever invokes these translators via `translateRequest`/`translateResponse` (`open-sse/translator/index.js:92-110,160-186`). `TestRegistryGeminiRoundTripViaPipeline`: drive `TranslateRequest(FormatOpenAI, FormatGemini, ...)` on a fixture with system+tools+reasoning_content+tool history and assert the gemini body shape (systemInstruction, model contents, functionDeclarations, no toolConfig); drive `TranslateResponse(FormatGemini, FormatOpenAI, ...)` over a recorded gemini chunk sequence (role init → thought part → text → functionCall → finishReason STOP with usageMetadata) and assert the OpenAI chunk sequence ends with `finish_reason:"tool_calls"` carrying usage.

## Acceptance (binary)
- `go test ./...` green; `go vet ./...` clean.
- All tests named in tasks 1-5 exist and pass.
- `grep -n "toolConfig" internal/translation/openai_gemini_request.go | wc -l` prints `0` (PAR-TRANS-030 corrected behavior).
- `grep -c "HARM_CATEGORY" internal/translation/gemini_helpers.go` prints `5` (all five safety categories present).
- `git diff <merge-base> -- internal/api/ internal/providers/` is empty (translators register only; provider/handler wiring is Wave 2).

## Out of scope
Antigravity envelope/unwrap + `requestType:"agent"` + double system injection (PAR-TRANS-039/040 — w1-f). gemini-cli Cloud Code envelope, `openaiToGeminiCLIRequest` thinkingConfig, credentials-aware registration signature (w1-f — registry `RequestTranslator` signature extension decided there). Vertex (w1-f). Gemini-format client requests (`request/gemini-to-openai.js` — w1-j). Provider adapter consumption (Wave 2). Consolidating w1-b's claude→openai filtering onto `FilterToOpenAIFormat` (Wave 2 refactor).
