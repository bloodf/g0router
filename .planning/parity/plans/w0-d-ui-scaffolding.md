# w0-d — UI scaffolding (rev 3)

Rows: AUD-075, AUD-076, AUD-077. Binary contract: PARITY.md §2 Bundle D acceptance column.
Parallelism: only Wave 0 plan touching `ui/` — verifiable: the ownership sections of `w0-a`, `w0-b`, `w0-c`, `w0-e` in `.planning/parity/plans/` list exclusively `internal/**` and `cmd/**` paths.
Worker: M3. Reviewer: Kimi diff gate.

## File ownership (exclusive while in flight)
- `ui/src/lib/types.ts` (new)
- `ui/src/routes/**` (new files; `__root.tsx` may be edited)
- `ui/src/routeTree.gen.ts` (TanStack Router generated artifact — regenerated by the route plugin during build)
- `ui/src/App.tsx`, `ui/src/main.tsx`
- `.planning/parity/plans/w0-d-recon.md` (new artifact)

## Required reconnaissance (before writing code; committed as `w0-d-recon.md` — this repo pushes to main, no PRs; the recon file is the Kimi diff gate's comparison artifact)
1. Symbol list the e2e mocks import: `grep -rh "from ['\"].*lib/types" ui/e2e ui/src | sort -u` + import clauses (AUD-075 says 36 — verify true count).
2. Route paths the e2e specs visit: `grep -rhn "goto(" ui/e2e | sort -u`.
3. Read `ui/package.json` and `internal/admin/connections.go` + `providers.go` + `handlers.go` JSON tags (types must mirror the snake_case `{data, error}` envelope). `ui/AGENTS.md` does not exist (AUD-074) — do not look for it.

## Tasks (TDD order — each step starts from a observed failing command)

1. **AUD-075 types**: run `cd ui && npx tsc --noEmit`; record the missing-module failures for `lib/types` imports. Create `ui/src/lib/types.ts` exporting exactly the recon-1 symbols — no more, no fewer. Re-run: green.
2. **AUD-077 router mount**: edit `App.tsx` to import and mount the TanStack Router root (file-based routing pattern; `main.tsx` only if the pattern requires). Run `npm run build`: fails (no route tree yet) — this is the failing state for task 3. Bundle D acceptance for this row is exactly "`App.tsx` imports and mounts TanStack Router root component; `npm run build` green" — no API client is built in Wave 0 (the api wrapper lands in Wave 6 with its first consumer and tests; AUD-077's "no API client" observation is satisfied row-wise by Bundle D's binary check, which does not require one).
3. **AUD-076 route shells**: create one route file per recon-2 path. Each is a shell: default export rendering the page heading only. Bundle D's "at least one route file" is the floor; the bounded scope here is the recon-2 list, justified by AUD-076's own finding text ("zero page routes for 30+ e2e specs") — no route beyond that list. Run `npm run build`: green (closes the task-2 failing state).

## Acceptance (binary)
- `cd ui && npx tsc --noEmit` green; `cd ui && npm run build` green.
- `types.ts` export list == recon-1 list in committed `w0-d-recon.md` (Kimi compares; any extra or missing symbol = REJECT).
- Route file exists for every recon-2 path; no route files outside that list (Kimi compares).
- `App.tsx` contains the router mount import; the static placeholder markup is gone.

## Out of scope
API client wrapper (Wave 6, lands with first consumer + tests). Login/auth wiring. Page content, data fetching, i18n, styling. New e2e specs or unit-test infra. Go code. Making e2e behavior assertions pass (Wave 6, PAR-UI rows).
