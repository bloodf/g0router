# w1-b — format registry + /v1/messages endpoint (Claude format end-to-end)

Rows: PAR-TRANS-001 (registry), PAR-TRANS-002 (formats), PAR-TRANS-003 (endpoint detection), PAR-TRANS-004/005 (openai-intermediate pipelines), PAR-TRANS-044 (openai→claude response translator), PAR-TRANS-048 **(PARTIAL by design: Claude `event:` framing branch only — `formatSSE` lines 117-119; the row's other two behaviors, `hasValuableContent` and `fixInvalidId`, belong to the central stream processor and ship in w1-c with PAR-TRANS-046/047; the row stays open until w1-c)**, PAR-TRANS-056 (`/v1/messages` route), PAR-TRANS-057 (claude→openai request translator). Rows 056-057 were added to the matrix by orchestrator amendment dated 2026-06-09 (see matrix footer and GATE-RESOLUTION.md addendum). Traceability rule: every task names the row(s) it implements in its heading; rows 004/005 are a single pipeline pair implemented by one task; PAR-TRANS-048's Claude framing branch is implemented as a `internal/translation` package helper (task 7); the route tasks (8-9) implement row 056 and *consume* rows 004/005/044/048/057 via the package API only.
Wave 1 plan 2 of ~10. Coordination: dispatched only after w1-a merges (`WAVE-MAP.md:35` — "Plans are written wave-by-wave"; same solo-window rule as w0-a/w1-a). Precondition check — worker runs BEFORE any edit and STOPs with IMPL-BLOCKED if either fails:
1. `grep -n "modelResolver" internal/api/chat.go` prints the interface declaration (expected at lines 13-15).
2. `grep -n "func PreprocessChatRequest" internal/translation/preprocess.go` prints one match.

These greps verify the w1-a deliverables exist in the working tree — no reliance on prose claims about merge state.
Worker: Kimi. Reviewer: gpt-5.5 diff gate.

## Why these rows together
PAR-TRANS-001/004/005 define the registry and the openai-intermediate pipeline; a registry with no registered translator is dead code, so the first translator pair ships in the same plan: PAR-TRANS-057 (request) + PAR-TRANS-044 (response) — exactly the pair 9router registers in `request/claude-to-openai.js:231` and `response/openai-to-claude.js:266`. PAR-TRANS-056 is the route that exercises all of it end-to-end. Every later Wave 1 translator plan only adds `register()` calls to this skeleton.

## File ownership (exclusive while in flight)
- `internal/translation/` additions: `formats.go`, `formats_test.go`, `registry.go`, `registry_test.go`, `claude_request.go`, `claude_request_test.go`, `claude_response.go`, `claude_response_test.go`, `sse.go`, `sse_test.go` (package created by w1-a; w1-a is merged before dispatch)
- `internal/api/messages.go`, `internal/api/messages_test.go` — NEW
- `internal/server/routes_openai.go` (one registration line in `RegisterOpenAIRoutes`, `internal/server/routes_openai.go:10-19`) and `internal/server/server_test.go` (one route-registration test added next to `TestAdminRoutesRegistered`, `server_test.go:59`)

## Tasks (TDD order — each test fails before its fix)

1. **PAR-TRANS-002 format constants** (`formats.go`). `type Format string`; constants for the 13 identifiers, verbatim from `_refs/9router/open-sse/translator/formats.js:2-16`: `openai`, `openai-responses`, `openai-response`, `claude`, `gemini`, `gemini-cli`, `vertex`, `codex`, `antigravity`, `kiro`, `cursor`, `ollama`, `commandcode`. Test `TestFormatConstants`: all 13 values exact.
2. **PAR-TRANS-003 `DetectFormatByEndpoint(path string, hasInputArray bool) Format`** (`formats.go`). Contract verbatim from `formats.js:22-35` (source quoted so the contract is not disputable):

   ```js
   if (pathname.includes("/v1/responses")) return FORMATS.OPENAI_RESPONSES;
   if (pathname.includes("/v1/messages")) return FORMATS.CLAUDE;
   // /v1/chat/completions + input[] → treat as openai (Cursor CLI sends Responses body via chat endpoint)
   if (pathname.includes("/v1/chat/completions") && Array.isArray(body?.input)) {
     return FORMATS.OPENAI;
   }
   return null;
   ```

   The chat+`input[]` branch returns `FORMATS.OPENAI` in the source — Cursor CLI sends a Responses-shaped body but 9router deliberately treats it as `openai` (their comment, line 30). Port exactly; do not "correct" it to `openai-responses`. Go signature returns empty `Format` for the null fallback. Tests `TestDetectFormatByEndpointResponses`, `TestDetectFormatByEndpointMessages`, `TestDetectFormatByEndpointChatWithInput` (asserts `openai`, not `openai-responses`), `TestDetectFormatByEndpointUnknown`.
3. **PAR-TRANS-001 registry** (`registry.go`). `type RequestTranslator func(model string, body map[string]any, stream bool) (map[string]any, error)`; `type ResponseTranslator func(chunk map[string]any, state *StreamState) ([]map[string]any, error)`; `Registry` struct with separate request/response maps keyed `from:to` (`index.js:10-24`), `Register`, `RequestTranslatorFor`, `ResponseTranslatorFor`, `NeedsTranslation(from, to) bool` (`index.js:198-200`). No package-level mutable registry: a single `NewRegistry()` constructor in `registry.go` wires all registrations explicitly (repo convention, AGENTS.md `## Conventions`: "No global state"; "No init() functions"). Constructor ownership: `NewRegistry` is created in this task with an empty wiring body; tasks 5 and 6 each add their one registration line to it — all inside this plan's exclusive ownership of `registry.go`, no split ownership. Tests `TestRegistryRegisterLookup`, `TestNeedsTranslation`.
4. **PAR-TRANS-004/005 pipeline** (`registry.go`). `TranslateRequest(from, to Format, model string, body map[string]any, stream bool)`: same-format skip; `from→openai` then `openai→to`, each step only when registered (`index.js:92-110`). `TranslateResponse(to, from Format, chunk map[string]any, state *StreamState)`: same-format returns chunk as-is; `to→openai` then `openai→from`, fan-out preserved — a step may return multiple chunks (`index.js:160-186`). `StreamState` struct holds the translator state fields used by task 6 (messageID, model, block indices/flags, toolCalls map, toolArgBuffers, usage, finishReason — matrix "TranslatorState", `index.js:204-246`). Tests `TestTranslateRequestPipeline` (fake translators recording call order), `TestTranslateRequestSameFormatSkips`, `TestTranslateResponseFanOut`.
5. **PAR-TRANS-057 Claude→OpenAI request translator** (`claude_request.go`), registered `claude:openai`. TDD for the wiring itself: FIRST write `TestNewRegistryWiresClaudeRequest` (registry_test.go) asserting `NewRegistry().RequestTranslatorFor(FormatClaude, FormatOpenAI) != nil` — fails; then add the translator and its registration line in `NewRegistry`. Port `_refs/9router/open-sse/translator/request/claude-to-openai.js:6-232`: system string-or-array → single `role:system` message (array items joined by `\n` on `.text`); message conversion — string content passthrough; block arrays: `text`→text part, `image` base64 → `image_url` data URI, `tool_use`→assistant `tool_calls` entry (arguments JSON-stringified), `tool_result`→`role:tool` message with text extraction rules (string | text-blocks joined `\n` | JSON fallback, lines 154-172); tool_results precede the same message's text content (lines 177-186); local fix-missing-tool-responses inserting `[No response received]` placeholders immediately after the assistant message's responded block (lines 75-110 — note: different insertion text and position rules than w1-a's preprocessing helper; port this one as written); tools → `{type:"function",function:{name,description:String,parameters:input_schema||{type:object,properties:{}}}}` (lines 56-66); tool_choice mapping `auto→auto`, `any→required`, `tool→{type:function,function:{name}}`, default `auto` (lines 218-229); `max_tokens` passes through `AdjustMaxTokens` only when set (line 14-16; AdjustMaxTokens arrives in w1-c with PAR-TRANS-011 — here apply the raw value and leave a single `// PAR-TRANS-011 (w1-c): adjustMaxTokens applies here` comment; the plan that adds the helper owns that call site change). Tests: `TestClaudeRequestSystemString`, `TestClaudeRequestSystemArray`, `TestClaudeRequestImageBlock` (base64 image block → `image_url` part with `data:<media_type>;base64,<data>` URI), `TestClaudeRequestToolUse`, `TestClaudeRequestToolResult`, `TestClaudeRequestMissingToolResponses`, `TestClaudeRequestTools`, `TestClaudeRequestToolChoice`.
6. **PAR-TRANS-044 OpenAI→Claude response translator** (`claude_response.go`), registered `openai:claude` (response direction). TDD for the wiring itself: FIRST write `TestNewRegistryWiresClaudeResponse` (registry_test.go) asserting `NewRegistry().ResponseTranslatorFor(FormatOpenAI, FormatClaude) != nil` — fails; then add the translator and its registration line. Port `_refs/9router/open-sse/translator/response/openai-to-claude.js:64-253` as a `ResponseTranslator` over `StreamState`: first chunk emits `message_start` (id from `chatcmpl-` strip or `msg_<now>`, lines 107-130); `delta.reasoning_content` → thinking block start/delta with text-block close (lines 133-152); `delta.content` → text block start/delta with thinking-block close (lines 155-174); `delta.tool_calls` → `content_block_start` per new id (OAuth `proxy_` prefix stripped, lines 177-208) with argument deltas BUFFERED (lines 210-216); on `finish_reason`: flush buffered args as one sanitized `input_json_delta` (Read-tool arg sanitization, lines 8-41), `content_block_stop` per open block, `message_delta` with `stop_reason` mapping `stop→end_turn`, `length→max_tokens`, `tool_calls→tool_use`, default `end_turn` (lines 255-263) + usage (prompt/completion → input/output with cache token subtraction, lines 73-104), then `message_stop`. Tests: `TestClaudeResponseMessageStart`, `TestClaudeResponseTextDelta`, `TestClaudeResponseThinkingDelta`, `TestClaudeResponseToolCallBuffering` (asserts arg deltas are NOT emitted before finish), `TestClaudeResponseStripsProxyPrefix` (tool name `proxy_Read` → block `name:"Read"`), `TestClaudeResponseToolArgSanitization` (Read-tool args with bad params → sanitized `input_json_delta` at finish, per lines 8-41), `TestClaudeResponseFinish` (stop_reason mapping all four cases), `TestClaudeResponseUsageCacheTokens`.
7. **PAR-TRANS-048 (Claude framing branch): `FormatSSE` helper** (`internal/translation/sse.go`). `FormatSSE(format Format, event map[string]any) []byte`: for `FormatClaude` with a `type` key, returns `event: <type>\ndata: <json>\n\n`; otherwise `data: <json>\n\n`. Verbatim source `_refs/9router/open-sse/utils/streamHelpers.js:117-121`:

   ```js
   if (sourceFormat === FORMATS.CLAUDE && data && data.type) {
     return `event: ${data.type}\ndata: ${JSON.stringify(data)}\n\n`;
   }
   return `data: ${JSON.stringify(data)}\n\n`;
   ```

   The row's other behaviors (`hasValuableContent`, `fixInvalidId`, `[DONE]`/null handling at `streamHelpers.js:104-114`) stay in w1-c with the stream processor; this helper lives in `internal/translation` so w1-c extends the same file rather than extracting from a handler. Tests: `TestFormatSSEClaudeEventFraming`, `TestFormatSSEDefaultDataFraming`.
8. **PAR-TRANS-056 (part 1): `/v1/messages` handler, non-streaming** (`internal/api/messages.go` + route registration). Contract per row 056 evidence (`src/app/api/v1/messages/route.js:28-34` — the route delegates to the shared chat pipeline; `chatCore.js:34` — source format from endpoint detection). Dependency seams (existence verified by the precondition check above):
   - `modelResolver` interface: `internal/api/chat.go:13-15` (`// modelResolver is the subset of inference.Router used by ChatHandler.`); injected as struct field `internal/api/chat.go:58`.
   - Preprocessing: `translation.PreprocessChatRequest(&req)` called at `internal/api/chat.go:74`; function defined `internal/translation/preprocess.go:37`.

   `MessagesHandler` is a NEW struct in `messages.go` with its own `router modelResolver` field, mirroring `ChatHandler` line 58 — **`internal/api/chat.go` is not modified** (acceptance enforces this). Flow: decode body as `map[string]any`; `TranslateRequest(FormatClaude, FormatOpenAI, model, body, stream)` (consumes task-4 pipeline + task-5 translator); re-marshal into `schemas.ChatRequest`; `translation.PreprocessChatRequest`; resolve provider; call `ChatCompletion`.
   **Non-streaming: return the OpenAI-shaped `ChatResponse` JSON untranslated.** This is reference behavior, not an omission — `nonStreamingHandler.js:15-16` verbatim: `if (targetFormat === sourceFormat || targetFormat === FORMATS.OPENAI) return responseBody;` — `translateNonStreamingResponse` only converts provider→OpenAI and never synthesizes Claude JSON; Claude-format clients receive OpenAI JSON on non-streaming requests (Claude Code always streams; path is cold in practice). Do NOT invent a Claude-JSON builder.
   Tests: `TestMessagesHandlerNonStreamingPassthrough` (fake resolver returning fixed ChatResponse → asserts OpenAI-shaped JSON returned unchanged, per row 056), `TestMessagesHandlerBadJSON` (400), `TestMessagesHandlerTranslatesClaudeBody` (fake resolver captures the `schemas.ChatRequest` → asserts claude `system` + `tool_use` blocks arrived as OpenAI system message + tool_calls, proving the task-5 translator is wired), and **route-level** `TestMessagesRouteRegistered` in `internal/server/server_test.go` (pattern: `TestAdminRoutesRegistered`, `server_test.go:59`) — POST `/v1/messages` through the constructed server router returns non-404, written FIRST and failing until the `r.POST("/v1/messages", ...)` line is added to `RegisterOpenAIRoutes` (`routes_openai.go:15-18`).
9. **PAR-TRANS-056 (part 2): `/v1/messages` streaming** (same files as task 8, builds on it). Provider stream → per chunk `TranslateResponse(FormatOpenAI, FormatClaude, ...)` (consumes task-4 pipeline + task-6 translator) → each result framed via `translation.FormatSSE(FormatClaude, ...)` (consumes task 7; no framing logic in the handler). In-band `chunk.Error` abort is not new scope — it is the repo's existing terminal-error contract for ALL stream consumers, established by w0-e Amendment 1 (AUD-046/047, merged): producer field `internal/schemas/chat.go:159` (`Error *ProviderError \`json:"-"\``), consumer check `internal/api/chat.go:32` (`if chunk.Error != nil {` → abort). A new stream consumer that ignored it would silently drop provider errors — a regression against the merged w0-e behavior, which the diff gate would correctly flag. Tests: `TestMessagesHandlerStreamingFraming` (fake resolver with chunk channel → asserts `event: message_start` first line, `event:`-prefixed framing for every event, terminal `message_stop`), `TestMessagesHandlerStreamingAbortsOnErrorChunk` (mirrors `TestWriteSSEStreamAbortsOnErrorChunk` in `internal/api/chat_test.go`).

## Acceptance (binary)
- `go test ./...` green; `go vet ./...` clean.
- All tests named in tasks 1-9 exist and pass.
- `TestNewRegistryWiresClaudeRequest` and `TestNewRegistryWiresClaudeResponse` (registry_test.go) pass — `NewRegistry` wires both directions; mechanical, binary.
- `TestMessagesRouteRegistered` passes — `/v1/messages` reachable through the real server router.
- `TestMessagesHandlerNonStreamingPassthrough` asserts the response body is byte-identical to the marshaled `ChatResponse` the fake resolver returned (OpenAI shape, per row 056).
- `git diff <merge-base> -- internal/api/chat.go internal/api/chat_test.go` is empty (this plan never touches those files) — mechanical, binary.

## Out of scope
openai→claude REQUEST direction enrichment (PAR-TRANS-012..022 — w1-d, provider-side). Gemini/Responses/Antigravity translators (w1-e..w1-h). Central stream processor PAR-TRANS-046/047/049/050 (w1-c). PAR-TRANS-048 remainder — `hasValuableContent` chunk filtering and `fixInvalidId` (w1-c, alongside the stream processor that calls them; this plan implements only the `formatSSE` Claude framing branch and the row stays open). `AdjustMaxTokens` helper (w1-c). Cloaking (PAR-TRANS-022, w1-h). Usage estimation on finish (Wave 5).
