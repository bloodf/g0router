# w1-d — openai→claude request translator + claude→openai response translator

Rows: PAR-TRANS-014, PAR-TRANS-015, PAR-TRANS-016, PAR-TRANS-017, PAR-TRANS-018, PAR-TRANS-019, PAR-TRANS-020, PAR-TRANS-021 (request direction); PAR-TRANS-041, PAR-TRANS-042 (response direction). PR ports: PAR-PR-1084 (PARITY.md §3, verbatim: `| #1084 | PAR-PR-1084 | Drop literal <think>/</think> markers from Claude→OpenAI stream |`), PAR-PR-1264 (verbatim: `| #1264 | PAR-PR-1264 | Strip temperature for Claude models with extended thinking |`).
Touched-not-implemented: PAR-TRANS-012/013 (already HAVE via the anthropic converter; the registry translator reproduces them as part of the same port — no row state change claimed by this plan).
NOT in scope: PAR-TRANS-022 (cloaking — w1-h with `claudeCloaking.js`).

Wave 1 plan 4 of ~10. Worker: Kimi. Reviewer: gpt-5.5 diff gate.
Precondition check — worker runs BEFORE any edit and STOPs with IMPL-BLOCKED if any fails:
1. `grep -n "func NewRegistry" internal/translation/registry.go` prints one match (w1-b merged).
2. `grep -n "func AdjustMaxTokens" internal/translation/maxtokens.go` prints one match (w1-c merged).

## Production-wiring scope ruling (read before judging dead code)
These two translators register the `openai:claude` (request) and `claude:openai` (response) directions in `NewRegistry` — the inverse pair of w1-b's. Their production consumer is the provider dispatch layer, sequenced by the user-approved wave map at `.planning/parity/plans/WAVE-MAP.md:14`, verbatim: `| 2 | Providers: all 43+ adapters, executors, model catalogs, full Bifrost-size interface (decision 9) with typed not-implemented stubs | PAR-PROV 66 + ~25 PR ports | 10–14 | 1 | Adapters consume translator; interface expansion lands here once |` — the "Adapters consume translator" clause with dependency on Wave 1. The Wave 1 deliverable is the registered, unit-tested engine; Wave 2 swaps `internal/providers/anthropic/converter.go` for registry consumption. Within w1-d the translators ARE exercised end-to-end through the registry pipeline (task 3).

## File ownership (exclusive while in flight)
- `internal/translation/openai_claude_request.go`, `openai_claude_request_test.go` — NEW
- `internal/translation/openai_claude_response.go`, `openai_claude_response_test.go` — NEW (claude→openai response direction; file name reflects registry key direction `claude:openai`)
- `internal/translation/registry.go`, `registry_test.go` — two registration lines in `NewRegistry` + wiring tests only

## Tasks (TDD order — each test fails before its fix)

1. **Rows 014-021: openai→claude request translator** (`openai_claude_request.go`), registered request direction `openai:claude`. TDD wiring first: `TestNewRegistryWiresOpenAIClaudeRequest` asserting `NewRegistry().RequestTranslatorFor(FormatOpenAI, FormatClaude) != nil` fails, then port `_refs/9router/open-sse/translator/request/openai-to-claude.js:11-207` (read the whole file):
   - base: `model`, `max_tokens: AdjustMaxTokens(body)` (line 16; helper from w1-c), `stream`, `temperature` passthrough (21-23).
   - **014**: system messages extracted into `systemParts` via text extraction (29-35, 380-387); top-level `system` array is `[claudeCodePrompt]` or `[claudeCodePrompt, {type:text, text:joined, cache_control:{type:ephemeral,ttl:"1h"}}]` (124-134) with `claudeCodePrompt = {type:"text", text:"You are Claude Code, Anthropic's official CLI for Claude."}` (`appConstants.js:152`).
   - **015**: message merge loop (40-88): role mapping `user|tool→user`, else `assistant`; tool_result blocks split into their own user message BEFORE other blocks; same-role runs merged; flush after tool_use.
   - **016** (row corrected 2026-06-09 — see row footnote and GATE-RESOLUTION.md): `cache_control {type:ephemeral}` WITHOUT ttl on the last cacheable block (`text|tool_use|tool_result|image`, NOT thinking) of the last assistant message (91-105, verbatim line 100 `block.cache_control = { type: "ephemeral" };`); `cache_control {type:ephemeral,ttl:"1h"}` on last tool (164-166) and last system block (128-131).
   - **017**: `response_format` → appended system part: `json_schema` variant embeds the pretty-printed schema in the exact prompt text (108-117); `json_object` variant uses the exact one-liner (118-120).
   - **018**: tools conversion (137-166): non-`function` types pass through untouched; function tools → `{name, description: "" default, input_schema: parameters || input_schema || {type:object,properties:{},required:[]}}`; OAuth prefix is `""` (line 8 — comment: "Empty prefix matches real Claude Code behavior"); toolNameMap recorded when non-empty (size>0 → attach, 196-199 — with empty prefix the map keys equal values; port the mechanism, it becomes meaningful for response decloaking).
   - **019**: `convertOpenAIToolChoice` (293-326): string `"required"`→`{type:any}`, other strings→`{type:auto}`; object with `function.name`→`{type:tool,name}`; claude-native object passed through ONLY when `type ∈ {auto,any,tool,none}`; everything else → `{type:auto}`. The row's "rejects unknown object types" means exactly this never-pass-through fallback, verbatim source comment (296-298): "anything else (e.g. OpenAI's 'function') triggers a 400, so we never pass an unrecognized type through" — the rejection is replacing the malformed value with `{type:auto}`, not erroring (source 313-325: unknown types fall through to `return { type: "auto" };`).
   - **020/021**: explicit `thinking` block passthrough with `budget_tokens`/`max_tokens` (173-180); `reasoning_effort` → `thinking.budget_tokens` lookup `{none:0, low:4096, medium:8192, high:16384, xhigh:32768}` only when no explicit thinking (183-200). The `none` entry maps to budget `0`, and the source then emits NO thinking block for budget 0 — verbatim (195-199): `if (budget === 0) { // none → no thinking } else if (budget) { result.thinking = { type: "enabled", budget_tokens: budget }; }`. The row's lookup table and this behavior are the same contract: `none` participates in the lookup, and its `0` result means thinking is omitted.
   - **PAR-PR-1264**: when the outgoing body has `thinking` enabled, drop `temperature` (Anthropic 400s on temperature+extended thinking; PARITY-accepted port). Implement as a final guard: `if result has thinking and temperature → delete temperature`.
   - message block conversion — this is NOT extra scope: row 015's merge loop converts every message through `getContentBlocksFromMessage` (`openai-to-claude.js:54` inside the row's cited 42-88 range; function body 210-291), so the row's contract includes the block conversion it calls. Port: role tool → tool_result with `tool_call_id` (215-219); user string/parts — text, tool_result with `is_error` (226-233), `image_url` data-URI → base64 source / http(s) URL → url source (234-247), raw image passthrough (248-250); assistant content arrays — text, tool_use kept as-is, thinking block with `cache_control` stripped (254-269); assistant string content (270-275); `tool_calls` → tool_use with `tryParseJSON(arguments)` (277-289, helper 390-397: non-JSON arguments stay as raw string).
   Tests (all in `openai_claude_request_test.go`): `TestNewRegistryWiresOpenAIClaudeRequest`, `TestOpenAIClaudeSystemExtraction` (014: prompt prefix + ttl cache_control), `TestOpenAIClaudeMessageMergeAndToolResultSplit` (015), `TestOpenAIClaudeCacheControlPlacement` (016: last assistant block gets `{type:ephemeral}` and asserts NO `ttl` key on it; system block and last tool assert `ttl:"1h"`; thinking block skipped), `TestOpenAIClaudeResponseFormatJSONSchema` + `TestOpenAIClaudeResponseFormatJSONObject` (017), `TestOpenAIClaudeToolsConversion` (018: function rewrite, builtin passthrough, defaults), `TestOpenAIClaudeToolChoice` (019: all five mappings PLUS malformed-object case — `{type:"function"}` without `function.name` and `{type:"garbage"}` both → `{type:auto}`, asserting the unknown type never passes through), `TestOpenAIClaudeReasoningEffort` (020: each lookup value incl. `none`), `TestOpenAIClaudeThinkingPassthrough` (021), `TestOpenAIClaudeThinkingDropsTemperature` (PAR-PR-1264), `TestOpenAIClaudeImageBlocks` (data URI + http URL), `TestOpenAIClaudeToolCallArgumentsParse` (valid JSON → object; invalid → raw string).
2. **Rows 041/042 + PAR-PR-1084: claude→openai response translator** (`openai_claude_response.go`), registered response direction `claude:openai`. TDD wiring first: `TestNewRegistryWiresClaudeOpenAIResponse` fails, then port `_refs/9router/open-sse/translator/response/claude-to-openai.js:20-191`:
   - `message_start` → role chunk; capture messageId (strip nothing; `chatcmpl-` prefix added on emission) and model (27-32).
   - `content_block_start`: `server_tool_use` → record index and skip (38-41); `text` → mark started; `thinking` → mark in-thinking — **PAR-PR-1084: do NOT emit the literal `<think>` content chunk** (the frozen ref emits `{content:"<think>"}` at 45-46 and `{content:"</think>"}` at 97-98; the accepted PR port drops both markers — thinking text still flows as `reasoning_content` via thinking_delta, so no information is lost; document with a comment citing PAR-PR-1084); `tool_use` → tool_calls chunk with sequential `toolCallIndex`, name decloaked via `state.ToolNameMap` (48-62).
   - `content_block_delta`: skip server-tool indices (69); `text_delta`→content; `thinking_delta`→`reasoning_content`; `input_json_delta`→append to buffered arguments AND emit incremental tool_calls chunk (71-87).
   - `content_block_stop`: server-tool index reset; thinking block close (no `</think>` emission per PAR-PR-1084); clear text/thinking flags (91-104).
   - `message_delta` (**042**): usage normalization `prompt_tokens = input + cache_read + cache_creation` (106-128); `stop_reason` mapping via `convertStopReason` (`end_turn→stop`, `max_tokens→length`, `tool_use→tool_calls`, `stop_sequence→stop`, default `stop`, 193-201); final chunk carries usage with `prompt_tokens_details.cached_tokens`/`cache_creation_tokens` when nonzero (131-156); mark finishReasonSent.
   - `message_stop`: only when finish not yet sent — fallback finish_reason (`tool_calls` if any tool calls else `stop`) + usage from state (163-188).
   Tests: `TestNewRegistryWiresClaudeOpenAIResponse`, `TestClaudeOpenAIMessageStart`, `TestClaudeOpenAITextDelta`, `TestClaudeOpenAIThinkingNoMarkers` (thinking block start/stop emit NO `<think>`/`</think>` content chunks; thinking_delta arrives as `reasoning_content` — PAR-PR-1084), `TestClaudeOpenAIToolUseStartAndArgs` (sequential indices, incremental argument chunks), `TestClaudeOpenAIServerToolSkipped`, `TestClaudeOpenAIStopReasonMapping` (042: all five cases), `TestClaudeOpenAIUsageCacheTokens` (042: prompt_tokens sum + details fields), `TestClaudeOpenAIMessageStopFallback`.
3. **Pipeline verification for rows 004/005 over the new registrations** (`registry_test.go`) — consumes (not implements) PAR-TRANS-004 ("Request translation normalizes through OpenAI as intermediate", `index.js:92-110`) and PAR-TRANS-005 (response counterpart, `index.js:160-186`): the rows' pipeline must route through the directions registered by tasks 1-2, exactly as 9router exercises these translators only via `translateRequest`/`translateResponse`. `TestRegistryClaudeRoundTripViaPipeline`: drive `TranslateRequest(FormatOpenAI, FormatClaude, ...)` on a fixture with system+tools+tool_choice and assert the claude body shape; drive `TranslateResponse(FormatClaude, FormatOpenAI, ...)` over a recorded claude event sequence (message_start → text deltas → tool_use → message_delta → message_stop) and assert the OpenAI chunk sequence ends with a finish_reason chunk.

## Acceptance (binary)
- `go test ./...` green; `go vet ./...` clean.
- All tests named in tasks 1-3 exist and pass.
- `grep '"<think>"' internal/translation/openai_claude_response.go | wc -l` prints `0` — no code path emits the literal marker string (PAR-PR-1084); comments may reference the marker without quotes.
- `git diff <merge-base> -- internal/api/ internal/providers/` is empty (this plan registers translators only; provider/handler wiring is Wave 2).

## Out of scope
PAR-TRANS-022 cloaking (w1-h). PAR-PR-952 thinking signatures (w1-h, alongside `claudeHelper.js` PAR-TRANS-055). Gemini/Responses translators (w1-e..g). Provider adapter consumption of the registry (Wave 2 per WAVE-MAP). `openaiToClaudeRequestForAntigravity` variant (`openai-to-claude.js:330-376` — w1-h with the Antigravity translator).
