
## w6-g (Usage + logs + quota + pricing cluster) - 2026-06-14
- [x] Quota runtime Go source — no `GET /api/quota` exists on the Go side (real per-connection source is `GET /api/usage/{connectionId}`). w6-g ships PAR-UI-012 as variant-HAVE against the `/api/quota` MOCK (consumed unchanged) since closing the runtime gap needs NEW Go (forbidden in w6-g). Raise a serial Go follow-up to add an aggregating `GET /api/quota` or wire the page to per-connection reads. (ESCALATION-1c) — **RESOLVED by w7-route-a §1.8: real aggregating `GET /api/quota` over `ListConnections` + an injectable per-connection usage fetcher seam (`internal/admin/quota.go`); PAR-UI-012 → HAVE.**
- [ ] SSE under `vite preview` — usage page opens `EventSource("/api/usage/stream")`; the e2e `MockEventSource` (`fixture.ts`) handles only traffic+console streams and idles for the usage url. Plan asserts SSE via a UNIT test (stubbed EventSource) and keeps e2e REST-deterministic, NOT editing `fixture.ts`. If a real EventSource throws/blocks under the harness, escalate for a sanctioned one-line `fixture.ts` idle branch. (ESCALATION-2) — RESOLVED at P8: MockEventSource (`fixture.ts:49-98`) fires `open` then idles for the usage url (no throw, no block); `fixture.ts` NOT edited; ESCALATION-2 NOT needed.
- [ ] Mock path corrections vs shared specs — w6-g corrects `usage.ts`/`logs.ts`/`pricing.ts` handler BODIES to the real Go paths (`/api/usage/stats`, `/api/usage/request-logs`, `/api/pricing` GET/PATCH/DELETE). If a corrected shared-handler body breaks a non-w6-g spec, escalate for orchestrator serialization. (ESCALATION-3) — Shared mock handlers are cross-plan; a body change can ripple.
- [ ] PAR-UI-081 confirmed ALREADY HAVE (w6-a; apiFetch = TanStack Query queryFn adapter, matrix line 92 + WORKFLOW 6629/6645). w6-g consumes apiFetch and does NOT mount QueryClientProvider. The MAP-decision-2 TanStack Query provider wiring (if ever wanted) remains an orchestrator serial follow-up. — Resolves the w6-e open-questions note that flagged Query installed-but-unwired; no frozen-file edit in any page plan.

## w6-h (Combos + routing cluster) - 2026-06-14
- [ ] Combos DTO/key divergence — real Go `GET/POST /api/combos` + `PUT/DELETE /api/combos/{name}` serve `comboResponse {name,models:[]string}` keyed by NAME (`internal/admin/combos.go:15-21,65-67,89-91`), which diverges from the mock/UI `Combo` type `{id,name,strategy,steps:[{provider,model}],is_active}`. w6-h ships PAR-UI-091..094 variant-HAVE against the `/api/combos` MOCK (consumed as-is); NO Go edit (forbidden), NO new Go (MAP assigns none to w6-h). Serial Go follow-up: reconcile the combos DTO/key with the UI `Combo` type (or adapt page to `{name,models[]}`). (ESCALATION-1) — **RESOLVED by w7-route-a §1.7 (ESC-COMBOS Option A): a NEW separate `combos_admin` table + handlers OWN `/api/combos[/{id}]` serving the id-keyed UI shape; the engine `{name,models[]}` HTTP routes were re-registered to the admin surface (verified no non-page consumer; `/v1/models` lister reads the store directly), the engine store + lister stay intact via a best-effort mirror-write. PAR-UI-091..094 → HAVE.**
- [ ] Aliases admin endpoint absent — `store.ListAliases()` exists (`internal/store/aliases.go:64-65`, consumed by `routes_openai.go:205-206`) but there is NO `/api/aliases` ADMIN route and no `internal/admin/aliases.go`. w6-h ships PAR-UI-116 variant-HAVE against the `/api/aliases` MOCK (consumed unchanged). Serial Go follow-up: add admin `GET/POST/PUT/DELETE /api/aliases` over the existing alias store. (ESCALATION-2) — **RESOLVED by w7-route-a §1.4 (ESC-ALIAS-SHAPE): a NEW typed `aliases` admin table + id-keyed CRUD (`internal/store/aliasesadmin.go` + `internal/admin/aliases.go`) mirroring the UI `{id,alias,provider,model}` shape; the gateway `model_aliases` resolver stays frozen and is best-effort mirror-written from admin edits. PAR-UI-116 → HAVE.**
- [ ] Routing-rules backend absent — `grep -rE 'routing-rules|RoutingRule' internal/` → ZERO (no store/admin/route). w6-h ships PAR-UI-130 `/routing-rules` subset variant-HAVE against the `/api/routing-rules` MOCK. Serial Go follow-up: add routing-rules store + admin CRUD. (ESCALATION-3a) — **RESOLVED by w7-route-a §1.5: real routing-rules store + admin CRUD (`internal/store/routingrules.go` + `internal/admin/routingrules.go`); TEXT-PK, created_at rendered RFC3339. NOTE: admin CRUD only — live rule application to inference traffic is a NEW tracked follow-up (below).**
- [ ] Model-limits backend absent — `grep -rE 'model-limits|ModelLimit' internal/` → ZERO. w6-h ships PAR-UI-130 `/model-limits` subset variant-HAVE against the `/api/model-limits` MOCK. Serial Go follow-up: add model-limits store + admin CRUD. (ESCALATION-3b) — **RESOLVED by w7-route-a §1.6 (ESC-IDTYPE): real model-limits store + admin CRUD (`internal/store/modellimits.go` + `internal/admin/modellimits.go`); INTEGER-PK numeric ids (strconv.ParseInt), `allowed_key_ids` as a JSON blob. PAR-UI-130 `/model-limits` subset → HAVE.**
- [ ] Combos DnD reorder e2e — drag-reorder is untestable via renderToString and finicky under Playwright. w6-h's authoritative proof is a PURE `moveStep` helper (`ui/src/lib/combo-order.ts`) unit-tested deterministically; e2e asserts member rows render in order and proves a reorder via keyboard-DnD OR a persisted-order PUT-body intercept (whichever is green). If BOTH live-DnD e2e paths are infeasible under the harness, drop the live-DnD e2e and rely on the pure-helper unit + render-in-order e2e; record in WORKFLOW.md. (ESCALATION-4) — The plan does NOT block on live DnD; the pure helper unit is the floor.

## w6-i (Chat + console + translator cluster) - 2026-06-14
- [x] Console-logs stream backend absent — w6-i shipped PAR-UI-017 variant-HAVE driven by the e2e `MockEventSource`. (ESCALATION-1) — **RESOLVED by w7-misc**: real `GET /api/console-logs/stream` SSE shipped over a NEW in-process log-capture seam (`internal/logging/console.go` bounded ring buffer + subscriber fan-out; `internal/admin/console.go` SSE mirroring usagestream — `data:{level,message,timestamp}\n\n` + keepalive + `Recent()` replay + `ctx.Done()` unsubscribe; nil-safe 501 when unwired). Fed by `log.SetOutput(io.MultiWriter(os.Stderr, consoleLogWriter))` in `NewAdminHandlers` (ESC-CONSOLE-SRC MultiWriter capture; server.go untouched, and main.go has no admin-handler handle, so the additive wiring lives in the serial-slot file `routes_admin.go`). Hermetic frame-emit/ring-bound/slow-drop/unsubscribe tests; frozen `fixture.ts` unchanged; console.spec green. Flipped variant-HAVE → true-HAVE (PAR-UI-017).
- [ ] Chat-sessions admin backend absent — NO `/api/chat-sessions` Go route. The chat send/receive turn uses the REAL gateway route `/v1/chat/completions` (`routes_openai.go:84`); the 9router ref keeps sessions in localStorage, so server-side persistence is out of scope. (ESCALATION-1a) — **DEFERRED by w7-misc (§0 / ESC-CHATSESS, deciding evidence recorded)**: building `/api/chat-sessions` CRUD (table + handlers + routes) would add a surface with NO consuming requirement = exactly the dead-code the brief warns against. Stays OPEN; the `chat-sessions.ts` mock stays as-is. A future feature genuinely needing server-side session history triggers a dedicated follow-up plan (table + CRUD + routes).
- [x] Translator: no editor dep AND no Go backend — w6-i shipped PAR-UI-018/086 variant-HAVE: a plain `<textarea>` editor + a self-contained `handlers/translator.ts` mock. (ESCALATION-2) — **RESOLVED by w7-misc**: real `GET /api/translator/load` + `POST /api/translator/translate` shipped (`internal/admin/translator.go` over the in-tree `translation.Registry.TranslateRequest`; load returns `{file,payload}` containing `gpt-4o`; translate runs the openai→claude pipeline and returns `{payload,translated:true,from,to}`; 400 on bad payload/format; hermetic, no network). `save`/`send` DEFERRED (ESC-TRANS-SCOPE — no page/spec/mock consumer = dead code; `send` additionally overlaps the real gateway request path and needs its own scope decision; see the new DEFER follow-up below). Textarea→Monaco editor upgrade still optional/deferred (no dep added). Mock verified to mirror the Go DTO; translator.spec green. Flipped variant-HAVE → true-HAVE (PAR-UI-018/086).
- [ ] Translator save/send backends DEFERRED (w7-misc follow-up) — `POST /api/translator/save` (persist a named sample) + `POST /api/translator/send` (execute the translated request against a live provider) have NO page/spec/mock consumer today, so w7-misc deliberately did NOT build them (no dead code). Follow-up if later wanted: (a) `save` = a small named-sample store + handler; (b) `send` = a scope decision since it overlaps the real `/v1/chat/completions` gateway path (auth/credentials/streaming). (w7-misc §1.5 / ESC-TRANS-SCOPE) — Mock translator never had save/send; building them needs a real consumer first.
- [ ] Chat-stream transport choice — RESOLVED at T3: chat page streams `/v1/chat/completions` via a **plain-fetch ReadableStream reader** (`streamChatCompletion` in `ui/src/components/chat/chat-window.tsx`), NOT `@ai-sdk/react`. `@ai-sdk/react@3`'s `DefaultChatTransport` expects the AI SDK UI-message stream protocol, not raw OpenAI `chat.completion.chunk` SSE; pointing it at the raw gateway route would need a new adapter dependency (forbidden). The plain-fetch reader maps cleanly to the `inference.ts` mock chunk shape and adds NO dependency (the sanctioned §1.3 point-2 in-plan fallback, NOT ESCALATION-3). Unit-tested with a stubbed fetch returning a ReadableStream of OpenAI chunks; e2e proven by the chat.spec "mock assistant" send/receive + assistant-turn-appended tests. (ESCALATION-3 NOT fired.) Also at T3: corrected the `inference.ts` mock BODY (within-mock fix, §1.4) — build chunks via `JSON.stringify` so the assistant content's embedded `"` quotes are escaped (the prior hand-interpolated template produced invalid JSON that broke chat streaming); the mock is consumed only by the w6-i-owned chat.spec, no non-w6-i spec affected (ESCALATION-5 NOT fired).
- [ ] NEW route file + routeTree.gen.ts — w6-i is THE only page-wave-1 plan adding a route file (`ui/src/routes/translator.tsx`); the TanStack Router Vite plugin regenerates `ui/src/routeTree.gen.ts` to register `/translator`. That generated change IS in w6-i's allowed diff (unlike sibling page-wave-1 plans where routeTree.gen.ts must be UNCHANGED), same as w6-c's callback.tsx. NEVER hand-edited; a regen failure is an escalation. (ESCALATION-6) — The SOLE diff-gate difference from w6-c/e/g/h. RESOLVED at T4: `npm run build` regenerated `routeTree.gen.ts` cleanly to register `/translator` (grep confirms `TranslatorRoute`/`/translator` present); committed as generated output in the route-adding commit; ESCALATION-6 NOT fired.

## w6-e (Providers + Connections + Models cluster) - 2026-06-14
- [ ] Catalog path naming — `/api/providers/catalog` + `/{id}/catalog` chosen to avoid colliding with the existing `GET /api/providers` CRUD; confirm fasthttp/router static-vs-`{id}` precedence holds, else rename to `/api/provider-catalog` (ESCALATION-1/-3). — Determines page+mock+Go path agreement; a wrong choice silently 404s the providers page.
- [ ] Connections page data source — consume existing `GET /api/connections` CRUD DTO (`provider_id`/`kind`/`secret_set`) with client-side mapping, OR require the new UI-shaped read; decide at T5 (ESCALATION-2). — Existing CRUD DTO field names diverge from the UI `Connection` type; the wrong path forces a forbidden existing-Go edit.
- [ ] `card-elev` class — providers.spec asserts `[class*='card-elev']` but the class exists nowhere in ui/src; ProviderCard must introduce it. — Hard contract marker; without it the pre-existing spec stays red.
- [ ] TanStack Query is installed but unwired (no QueryClientProvider in frozen __root.tsx/main.tsx); w6-e uses plain apiFetch. — MAP decision 2 (TanStack Query) deferred to an orchestrator follow-up; record so PAR-UI-081 (w6-g) is not assumed satisfied by w6-e.

## w6-f (Endpoint + keys + virtual-keys cluster) - 2026-06-14
- [ ] w6-pre catalog API NOT landed — `internal/admin/catalog.go`/`internal/inference/catalog.go` ABSENT, no `/api/catalog` route, no KeyIDs threading in `selection.go`. The MAP says the KeyIDs editor consumes "w6-pre's catalog API"; w6-f instead sources available models from w6-e's SHIPPED `GET /api/providers/{id}/models` + `/api/models` and pinnable KeyIDs from `GET /api/providers/{id}/connections`, writing the real w5-g VK `provider_configs[].key_ids`. (ESC-1) — If w6-pre's `/api/catalog` later lands and is preferred as the source, a serial follow-up swaps the page's source path; w6-f does not block on it. Also: w6-pre itself remains unmerged (PAR-ROUTE-057/058 + PAR-ROUTE-030 KeyIDs Go half) — flag for the orchestrator.
- [ ] keys/VK mock-vs-Go DTO divergence — e2e `keys.ts`/`virtual-keys.ts` + seeds use shapes that diverge from the real Go DTOs (apiKey mock `full_key`/`scopes`/`rpm_limit` vs Go `key`/`machine_id`; VK mock flat `budget_usd`/`budget_period` vs Go nested `budget{limit,period,used}` + `provider_configs[]`). w6-f corrects the mock BODIES + seeds to mirror Go (MAP decision 4); UI `ApiKey`/`VirtualKey` types' extra fields are display-optional. (ESC-2) — A corrected shared body could ripple; keys/VK handlers are consumed only by w6-f specs, so low risk, but record.
- [x] model-test / availability / custom-models mock-only — w6-f shipped PAR-UI-117/119/120 variant-HAVE. (ESC-3) — **RESOLVED by w7-misc**: real Go shipped for all three — `POST /api/models/test` → `{ok,latency_ms}` (injectable `ModelProber`, hermetic; default = network-free catalog resolve), `GET /api/models/availability` → `{available:[{id,available}]}` (catalog + enabled provider connections, full-catalog fallback), `GET/POST /api/models/custom` + `DELETE /api/models/custom/{id}` over a NEW additive `custom_models` table (`internal/store/custommodels.go`) + `internal/admin/models.go` (`customModelDTO` `is_custom:true`; `recordAudit` on create/delete). Mocks (`nodes.ts` test/availability, `models.ts` custom) verified to already mirror the Go DTOs — no body change. models.spec green. Flipped variant-HAVE → true-HAVE (PAR-UI-117/119/120).
- [x] provider-nodes Go store shape — RESOLVED at T2: `internal/admin/nodes.go` composes the existing `providers` table filtered to `type=="openai-compatible"` with NO schema change. `prefix`/`api_type` (camelCase or snake_case) are accepted at decode but NOT persisted (the node DTO `{id,name,base_url,type,enabled}` does not surface them and the table lacks the columns). `internal/store/**` untouched; no `ensureColumn` needed (ESC-6 did not fire).
- [x] routes_admin.go serial slot (wave 2) — RESOLVED at close: at P7 no concurrent plan held an unmerged `routes_admin.go` edit (w6-e merged @ b78ffca; no w6-j commits). w6-f TOOK the slot, landed ONE additive 3-route commit at T3 (`54ca615`), and RELEASES it to w6-j on close. Only one unmerged holder at a time (MAP decision 5) — honored.

## w6-j (Settings/profile + version cluster) - 2026-06-14
- [ ] version/shutdown Go wiring path — the binary `version`/`buildDate` (`cmd/g0router/main.go:20-21`) and the real graceful shutdown (`srv.Close()`, `main.go:144` / `server.go` `Shutdown()`) are NOT reachable from `internal/admin`. w6-j adds additive `SetVersionInfo(version,buildDate)` + `SetShutdownFunc(fn)` setters on `Handlers` (mirroring `SetUsageServices`, `handlers.go:43-47`; `New(...)` signature UNCHANGED) and a minimal additive forward in `server.go`/`main.go`. The `Shutdown` handler is pure-testable: nil-safe (501 if unwired), response-first, invokes the injected hook ASYNC, NEVER `os.Exit`/`srv.Close` inside the handler body (§1.5b; `! grep os.Exit version.go` is a §5 proof). (ESC-1) — If the minimal wiring forces a non-additive change to server.go/main.go, escalate for the orchestrator to choose the wiring shape; never break the frozen `New(...)` or edit existing settings/OIDC Go.
- [ ] password-change endpoint absent — NO Go `/api/auth/password` (auth.go has login/me/logout only). w6-j ships PAR-UI-100 variant-HAVE against a mock route added to the ALREADY-REGISTERED `settings.ts` handler body. Serial Go follow-up: real `POST /api/auth/password` (verify current pw, hash, persist). (ESC-2) — Live password change is mock-only until the follow-up.
- [ ] DB-info endpoint absent — NO Go `/api/settings/database` (only a guard.go:38 future comment). w6-j ships PAR-UI-101 variant-HAVE against a mock route added to the registered `settings.ts` body. Serial Go follow-up: real `GET /api/settings/database` (path/size/table counts from the store). (ESC-3) — Live DB-info panel is mock-only until the follow-up.
- [x] OIDC config persistence + secret-at-rest — w6-j stored `oidc_client_secret` plaintext in the flat settings map and flagged the secret-at-rest question. (ESC-4) — **RESOLVED by w7-misc**: the OIDC client secret is now encrypted at rest. NEW singleton `oidc_secret` table (`oidc_secret_enc` column, the `*_enc` precedent) + `internal/store/oidcsecret.go` (`GetOIDCSecret`/`SetOIDCSecret` over `s.cipher.Encrypt/Decrypt`; idempotent first-read migration that encrypt-moves the legacy plaintext `settings["oidc_client_secret"]` then BLANKS it). Three MINIMAL sanctioned read/write redirects (ESC-OIDC-READSITE): `auth.go` `oidcConfigured` and `oidc.go` callback `ClientSecret` consult `GetOIDCSecret()`; `settings.go` `PutSettings` routes an incoming `oidc_client_secret` to `SetOIDCSecret` + strips it from the plaintext map. The OIDC TEST handler (`oidc.go:247-300`, body-provided secret) is UNTOUCHED. No-echo guarantee: responses never carry the decrypted secret or `oidc_secret_enc`; `GetSettings` no longer returns plaintext after migration. Hermetic round-trip + not-plaintext + legacy-migration + admin no-leak tests; existing `oidc_test.go` callback/login-gating tests stay green (the one map-seeded `TestOIDCConfigured` was additively corrected to seed via `SetOIDCSecret`).
- [ ] update-checker data source — the FROZEN sidebar (`sidebar.tsx:72-73,101-110`) renders the `data-testid="update-badge"` from `useSettingsStore` `updateAvailable`/`latestVersion`. w6-j does NOT edit the sidebar or the store definition; it adds a w6-j-owned hook `ui/src/hooks/use-version-check.ts` that fetches `/api/version` and CALLS the FROZEN public action `setUpdateInfo(true, latest_version)` (`settings.ts:9,19-20`). The hook is invoked from the w6-j settings page. (ESC-6) — If a later decision wants the check on every page load (mounting in the frozen root/sidebar), that is an orchestrator serial follow-up, not a w6-j blocker.
- [ ] DonateModal/ChangelogModal mounting — neither header.tsx nor sidebar.tsx has a donate/changelog trigger (grep EMPTY) and the header exception is SPENT; w6-a left donate unwired ("donate is w6-j"). w6-j CREATES both modals and mounts them from the w6-j-owned settings page about-block (no frozen-file edit, §1.7b). Changelog/donate content sources are mock-route-intercepted (`/api/version/changelog`, `/api/version/donate` in the registered `version.ts` body) — no outbound network in tests. — Resolves the w6-b "donate is w6-j" deferral with zero frozen edits.
- [ ] changelog markdown dep — 9router's ChangelogModal uses `marked`; w6-j adds NO dependency and renders the changelog as preformatted/sanitized text unless a markdown renderer is ALREADY installed (verify at T4). (ESC-7) — Markdown rendering is a serial follow-up if a sanctioned dep is later added.
- [ ] routes_admin.go serial slot — FINAL holder — w6-j TAKES the slot (free after w6-f's release; routes_admin.go:60-62 shows the merged w6-f provider-nodes block) for the two version routes and RELEASES it to NOBODY: the wave-6 serial chain (w6-pre→w6-d→w6-e→w6-j) CLOSES on w6-j (§1.8). Orchestrator confirms the slot is free at P7 before T3 (only one unmerged holder, MAP decision 5).

## w6-k (Governance pages cluster — teams/audit/feature-flags/guardrails/prompts/alerts) - 2026-06-14
- [ ] MAP "phases 13-19 complete" assumption is FALSE — the WAVE-6-MAP w6-k row claims "Backend COMPLETE (phases 13-19) — pure UI", but VERIFICATION (full `internal/server/routes_admin.go` 58-route enumeration + `grep -rniE 'api/teams|api/audit|api/feature-flags|api/guardrails|prompt-templates|alert-channels|Team/Audit/FeatureFlag/Guardrail/PromptTemplate/AlertChannel Handler' internal/ cmd/` → ZERO non-test matches) shows NONE of the six governance domains has a Go store/handler/route. w6-k ships all six pages variant-HAVE against the registered MOCKS and adds ZERO Go. Orchestrator: update the MAP row and schedule the serial Go follow-ups below. (ESCALATION-1) — Every governance page is mock-only until its backend lands.
- [x] teams backend absent — no Go `/api/teams`. w6-k ships `/teams` variant-HAVE vs the `/api/teams` MOCK (consumed unchanged). Serial Go follow-up: teams store + admin CRUD. (ESC-1a) — **RESOLVED by w7-gov-1: real Go `/api/teams` CRUD shipped (`internal/store/teams.go` + `internal/admin/teams.go`, table `teams`); mock corrected to mirror the 6-field teamDTO. Flipped mock→true-HAVE (PAR-UI-130/131).**
- [x] audit backend absent — no Go `/api/audit`. w6-k ships `/audit` variant-HAVE vs the `/api/audit` MOCK (paginated `{items,total}` shape). Serial Go follow-up: audit-log store + admin read endpoint. (ESC-1b) — **RESOLVED by w7-gov-1: real Go `GET /api/audit?limit=N` → `{items,total}` shipped (`internal/store/auditlog.go` + `internal/governance/audit.go` `WriteAudit` + `internal/admin/audit.go`, table `audit_log`); audit writes wired into this plan's own mutations (teams/users/password/setup) best-effort. Flipped mock→true-HAVE (PAR-UI-130/131).**
- [x] feature-flags backend absent — no Go `/api/feature-flags`. w6-k ships `/feature-flags` variant-HAVE vs the MOCK (GET + PUT-by-id toggle only; mock has no POST/DELETE). Serial Go follow-up: feature-flags store + admin GET/PUT. (ESC-1c) — **RESOLVED by w7-gov-2: real Go `GET /api/feature-flags` (list) + `GET/PUT /api/feature-flags/{id}` (get + toggle, NO POST/DELETE) shipped (`internal/store/featureflags.go` + `internal/admin/featureflags.go`, table `feature_flags` INTEGER-PK numeric ids per ESC-IDTYPE); toggle writes a best-effort audit entry via the w7-gov-1 `h.recordAudit` seam; mock confirmed already-mirroring the 5-field flagDTO. Flipped mock→true-HAVE (PAR-UI-130/131).**
- [x] guardrails backend absent — no Go `/api/guardrails` or `/api/guardrails/test`. w6-k ships `/guardrails` (config form + prompt tester) variant-HAVE vs the MOCK. The tester spec (`guardrails.spec.ts:15-21`) needs `/api/guardrails/test` to return `blocked:true` for "my secret password", but `seedGuardrails()` defaults to `guardrails_enabled:false` + blocklist `["badword1","badword2"]`; w6-k reconciles via page-driven enable (path A) OR a w6-k-owned `seed/guardrails.ts` correction (path B, §1.3). Serial Go follow-up: real guardrails settings + blocklist/PII test endpoint over the request pipeline. (ESC-1d) — **RESOLVED by w7-gov-3: real Go `GET/PUT /api/guardrails` (SINGLETON config) + `POST /api/guardrails/test` shipped (`internal/store/guardrails.go` single-row `guardrails` table per ESC-GR-STORE + `internal/governance/guardrails.go` `GuardrailEngine.Evaluate` + `internal/admin/guardrails.go`). `/test` is a STANDALONE blocklist/PII evaluator (NOT a live inference-pipeline hook — ESC-GR-PIPELINE; that wiring is a tracked follow-up below) computing `blocked = enabled && case-insensitive-substring-of-blocklist`, returning `{blocked, redacted_prompt, matches}`; "my secret password" → blocked:true under the seed config (deterministic unit + admin tests). PII redaction (email/phone/ssn → [REDACTED]) applies only when `pii_redaction_enabled`, else verbatim. update writes best-effort audit via `h.recordAudit`. Mock + path-B seed verified to already mirror the Go DTO — no body change. Flipped variant-HAVE→true-HAVE (PAR-UI-130/131).**
- [x] prompts backend absent — no Go `/api/prompt-templates`. w6-k ships `/prompts` variant-HAVE vs the MOCK. Serial Go follow-up: prompt-template store + admin CRUD. (ESC-1e) — **RESOLVED by w7-gov-2: real Go `GET/POST /api/prompt-templates` + `GET/PUT/DELETE /api/prompt-templates/{id}` + `POST /api/prompt-templates/test` shipped (`internal/store/prompttemplates.go` + `internal/admin/prompttemplates.go`, table `prompt_templates` INTEGER-PK; models stored as JSON blob; `is_active` defaults true). `/test` is a deterministic dependency-free render (NO LLM) returning `{rendered}` per ESC-PROMPT-TEST, registered static-before-`{id}` (ESC-ROUTE verified). create/update/delete write best-effort audit via `h.recordAudit`. DTO omits `updated_at` (stored for hygiene only, ESC-PROMPT-UPDATEDAT); mock POST/PUT corrected to drop `updated_at`. Flipped mock→true-HAVE (PAR-UI-130/131).**
- [x] alerts backend absent — no Go `/api/alert-channels` (the `/alerts` page IS the alert-channels UI; no `/api/alerts` exists by design). w6-k ships `/alerts` variant-HAVE vs the `/api/alert-channels` MOCK. Serial Go follow-up: alert-channel store + admin CRUD + real test-notification endpoint. (ESC-1f) — **RESOLVED by w7-gov-3: real Go `GET/POST /api/alert-channels` + `GET/PUT/DELETE /api/alert-channels/{id}` + `POST /api/alert-channels/{id}/test` shipped (`internal/store/alertchannels.go` table `alert_channels` INTEGER-PK numeric ids per ESC-IDTYPE + `internal/governance/alertchannels.go` `AlertDispatcher` with an injectable `Sender` seam + `internal/admin/alerts.go`). The `config` blob (webhook URLs/tokens) is ENCRYPTED at rest via `config_enc` (s.cipher, the connections.go precedent — ESC-ALERT-SECRET); the read DTO echoes `config` (the edit form needs it) but the `/test` notification NEVER echoes secrets (runtime no-leak test). The dispatcher does a best-effort HTTP POST to the channel target (real `httpSender`, 5s timeout); tests inject a fake Sender (no network) — deterministic per ESC-ALERT-TEST. create/update/delete/test write best-effort audit via `h.recordAudit`. `/{id}/test` registered deeper than `/{id}` (ESC-ROUTE verified — no conflict panic). Mock verified to already mirror the 7-field alertChannelDTO — no body change. Flipped variant-HAVE→true-HAVE (PAR-UI-130/131). **With ESC-1d+1f resolved the ENTIRE w6-k governance cluster (ESC-1a..1f + ESC-2) is now real-Go-backed.****
- [x] user-management (PAR-UI-132) backend absent — `internal/admin/auth.go` exposes only Login/Logout/Me/Status; NO `/api/auth/setup`, `/api/auth/password`, `/api/auth/users[/{id}]` Go route. w6-k surfaces the in-app subset as a Users panel on the `/teams` page (`ui/src/components/governance/users-panel.tsx`) consuming the EXISTING w6-c-owned `auth.ts` MOCK routes (consumed unchanged — w6-k edits NEITHER `auth.ts` NOR `seed/auth.ts`). PAR-UI-132 ships variant-HAVE. Serial Go follow-up: real `POST /api/auth/setup`, `PUT /api/auth/password`, `GET/POST /api/auth/users`, `DELETE /api/auth/users/{id}` over the user store (password hashing + `*_enc` secret-at-rest precedent). (ESCALATION-2) — **RESOLVED by w7-gov-1: real Go shipped in `internal/admin/usermgmt.go` over `internal/store/users.go` (+additive `display_name`/`role` columns + `CreateUserFull`), reusing `auth.HashPassword`/`VerifyPassword`. `setup` is public first-user onboarding (self-guards CountUsers()==0, auto-authenticates); `password` verifies current; users list/create/delete behind RequireSession; last-user delete guarded; password/hash never echoed (runtime no-leak tests). `auth.ts` users-route mocks corrected to mirror Go (login/logout/status untouched). PAR-UI-132 flipped → HAVE (real Go).**

- [ ] guardrails request-pipeline integration (NEW follow-up, opened by w7-gov-3) — w7-gov-3 ships the guardrails SINGLETON config store + a STANDALONE `/test` evaluator (ESC-GR-PIPELINE), but does NOT apply the stored config to LIVE inference traffic. A full integration — applying the configured blocklist/PII rules to real `internal/inference` requests (blocking/redacting in-flight prompts), with the appropriate failure-mode + telemetry — is a larger, separate concern. The config storage + evaluator shipped by w7-gov-3 are the prerequisite for that wiring. Serial Go follow-up: wire `GuardrailEngine.Evaluate` into the request pipeline. — Standalone-evaluator-only until the follow-up.

## w6-l (MCP + skills cluster — mcp page, mcp/tools page, McpMarketplaceModal, skills NEW route) - 2026-06-14
- [ ] MAP "MCP gateway backend in-tree" assumption is FALSE — the WAVE-6-MAP w6-l row claims the MCP gateway backend exists in-tree, but VERIFICATION (`internal/mcp/` contents + `grep -nE 'mcp|Mcp|/api/mcp|skills|Skill' internal/server/routes_admin.go` + `ls internal/admin/ | grep -iE 'mcp|skill'` + `grep -rniE '"/api/mcp|"/api/skills|McpHandler|SkillHandler' internal/ cmd/`) shows: `internal/mcp/` is a Phase-1 PLACEHOLDER (only `doc.go` + a no-op `TestPackageCompiles`); ZERO `/api/mcp/*` or `/api/skills` admin routes; NO `internal/admin/{mcp,skills}.go`; the only in-tree MCP reference is `guard.go:46` (a forward-looking `LOCAL_ONLY_PATHS` entry with no live route behind it). w6-l ships all four surfaces variant-HAVE against the registered MOCKS and adds ZERO Go. Orchestrator: update the MAP row and schedule the serial Go follow-ups below. (ESCALATION-1) — Every MCP/skills surface is mock-only until its backend lands.
- [ ] MCP clients/instances + marketplace backend absent — no Go `/api/mcp/clients|instances`. w6-l ships `/mcp` (clients/instances list, instance delete + OAuth `…/auth/start`) and the McpMarketplaceModal (PAR-UI-054) variant-HAVE vs the in-tree mcp MOCK (browse `GET /api/mcp/clients`, install `POST /api/mcp/instances` via pure `toInstancePayload`). The 9router ref's `/api/cli-tools/cowork-mcp-registry` + `…cowork-mcp-tools` registry endpoints exist in NEITHER g0router Go NOR the mock; the marketplace was REMAPPED to the mcp mock (no new cli-tools mock added). Serial Go follow-up: `internal/mcp/` gateway implementation + admin `GET /api/mcp/clients`, `GET/POST/DELETE /api/mcp/instances`, instance OAuth `…/auth/start` (and, if a true marketplace registry is later wanted, a real `/api/cli-tools/cowork-mcp-registry`-equivalent endpoint + mock). (ESC-1a) — **RESOLVED by w7-mcp-3**: real `GET /api/mcp/clients`+`/{id}`, `GET/POST /api/mcp/instances`+`GET/DELETE/{id}` (url-vs-command branch, allowlist-gated pre-spawn — PAR-MCP-022/049), `GET /api/mcp/instances/{id}/accounts` (OAuth tokens STRIPPED — masked accountDTO), `POST …/{id}/auth/start` (`{url}` via the shipped w7-mcp-2 `Engine.Start`, state/verifier never echoed) shipped (`internal/admin/mcp.go` over the shipped store + launcher + OAuth engine). clientDTO/instanceDTO carry EXPLICIT PascalCase json tags (ESC-CASING — the frozen page consumes PascalCase) so the mock needed ZERO body change. mcp.spec green. PAR-UI-054 + PAR-MCP-022/049 → HAVE.
- [ ] MCP tools/tool-groups backend absent — no Go `/api/mcp/tools|tool-groups`. w6-l ships `/mcp/tools` (tools list + per-tool execute, tool-groups CRUD + is_active toggle) variant-HAVE vs the mcp MOCK. Serial Go follow-up: `GET /api/mcp/tools`, `POST …/{name}/execute`, tool-groups CRUD over the MCP gateway. (ESC-1b) — **RESOLVED by w7-mcp-3**: real `GET /api/mcp/tools` (OpenAI-tool shape, prefix-stripped via PURE `stripServerPrefix` PAR-MCP-046 + the antigravity ride-along PAR-MCP-060), `POST …/{name}/execute` (bridge-driven via the shared `mcp.NewBridgeToolExecutor` → `Bridge.Send` + capturing `SessionSink` + `smartFilterText`, returning `{result}`), and `GET/POST/GET/PUT/DELETE /api/mcp/tool-groups` over the NEW additive `mcp_tool_groups` INTEGER-PK store table (snake_case toolGroupDTO) shipped (`internal/admin/mcp.go`, `internal/store/mcptoolgroups.go`). Tool-execute + the bounded multi-turn agent loop (`internal/mcp/agent.go`, PAR-MCP-040) share the one real `ToolExecutor`; the LLM round-trip is integration-only behind the injected `ModelStep` (ESC-AGENT-DEPTH). ZERO mock change (snake_case casing matched). mcp.spec green. PAR-MCP-040/045/046/047 → HAVE; 018/019/050/060 → HAVE-SUBSET.
- [ ] skills backend absent — no Go `/api/skills` + no `internal/admin/skills.go`. w6-l ships the NEW `/skills` route (PAR-UI-020) variant-HAVE vs the registered `/api/skills` MOCK (groups by category, copy-to-clipboard). Serial Go follow-up: a real `GET /api/skills` endpoint over a skills catalog/store. (ESC-1c) — **RESOLVED by w7-mcp-3**: real `GET /api/skills` → `{data:[skillDTO{name,category,description,url}]}` from a static `skillsCatalog()` (ESC-SKILLS-SRC: read-only catalog, NO store table — the frozen page has no create/delete control) shipped (`internal/admin/skills.go`); NOT under `/api/mcp/` so a normal `RequireSession` route (not local-only, §1.1 guard note). ZERO mock change (flat lowercase casing matched). skills.spec green. PAR-UI-020 → HAVE. **With ESC-1a/1b/1c resolved the ENTIRE w6-l MCP+skills cluster is real-Go-backed.**
- [ ] NO routes_admin.go serial slot — w6-l adds ZERO Go and NEVER touches `internal/server/routes_admin.go`/`internal/mcp/*`/`guard.go`. The wave-6 serial chain (w6-pre→w6-d→w6-e→w6-j) had already CLOSED on w6-j; w6-l holds NO slot and releases nothing.
- [ ] /mcp parent-route Outlet finding (non-blocking, resolved) — at base the `/mcp` stub rendered no `<Outlet>`, so navigating to the nested `/mcp/tools` route swallowed the child and showed only the parent's "MCP" (the base `/mcp/tools` "Tools" smoke was RED at base for this reason). The rewrite makes `mcp.tsx` render `<Outlet/>` when the `/mcp/tools` route is active (§1.8). No tree change (both routes were already registered).

## w6-m (Platform pages cluster — mitm/proxy-pools/tunnels — THE designated PARTIAL plan) - 2026-06-14
- [ ] PARTIAL disposition is the binding call (NOT variant-HAVE) — w6-m is THE designated PARTIAL plan of wave 6. The WAVE-6-MAP w6-m row explicitly pre-declares that the Go backends for mitm/proxy-pools/tunnels remain W7, so w6-m flips PAR-UI-013/019/104/105/112/113/114 from MISSING to **PARTIAL** (NOT HAVE): the UI half ships against the registered mocks (specs green), the Go half is a tracked W7 follow-up. Unlike w6-k/w6-l (variant-HAVE), w6-m records PARTIAL so the matrix honestly reflects "UI done, backend pending W7." Orchestrator: schedule the three W7 Go follow-ups below (ESC-1a/1b/1c) which flip each row PARTIAL → HAVE when its backend lands.
- [ ] mitm backend absent (PAR-UI-013) — no Go `/api/mitm/*` (route-table + `grep -rniE '"/api/mitm|MitmHandler' internal/ cmd/` ZERO; no `internal/admin/mitm.go`). w6-m ships `/mitm` PARTIAL vs the `/api/mitm/{status,toggle,ca-cert,tools/{id}}` MOCK (status panel + per-tool toggle + CA-cert download; the ca-cert route returns RAW PEM not `{data}`, so the download uses plain `fetch`/anchor not `apiFetch`). W7 Go follow-up: real MITM proxy config + CA-cert serving + per-tool enable/DNS-override over the MITM subsystem. (ESC-1a) — **RESOLVED by w7-plat-3**: real `/api/mitm/status` ({enabled,tools}), `POST /api/mitm/toggle`, `GET /api/mitm/ca-cert` (raw PEM `application/x-pem-file`, NOT `{data}`), `POST /api/mitm/tools/{id}` shipped (`internal/admin/mitm.go`, `internal/platform/mitm/{ca,proxy,service}.go`, `internal/store/mitm.go`); root CA gen + leaf-cert minting + CA signing + chain verify (`x509.Verify`) + leaf cache + the `GetCertificate` SNI closure + the pure `nextBackoff` policy + the full status/toggle/tool/ca-cert admin API are FULLY unit-tested hermetically (no port bind, no real TLS handshake, no network, no OS-privileged op); the CA private key is at rest as a 0o600 file under the data dir (ESC-CA-STORE A, mirrors `secret.go`) and is NEVER served/logged/DTO'd; the live TLS reverse-proxy listener (`proxy.go`) is integration-only (§1.9); OS-privileged system-trust-store auto-install + hosts-file/`dns_override` enforcement are DEFERRED/escalated (ESC-OS-PRIV — `dns_override` is stored+surfaced as config, not OS-enforced); mock verified to already mirror the Go `{data}` 5-field DTO + the raw-PEM ca-cert contract (no correction needed); PAR-UI-013 + PAR-PLAT-024/025/028 → HAVE.
- [ ] proxy-pools backend absent (PAR-UI-019 + PAR-UI-104 + PAR-UI-105) — no Go `/api/proxy-pools*` (grep ZERO; no `internal/admin/proxy_pools.go`). w6-m ships `/proxy-pools` PARTIAL vs the MOCK (`GET/POST /api/proxy-pools`, `POST /api/proxy-pools/batch`, `GET/PUT/DELETE /api/proxy-pools/{id}`, `POST /api/proxy-pools/{id}/test`); the ref's `?includeUsage=true` query + `vercel/deno/cloudflare-deploy` sub-routes are NOT served by the mock and NOT ported. W7 Go follow-up: proxy-pool store + admin CRUD + batch import + a real connectivity-test endpoint over the outbound-proxy subsystem. (ESC-1b) — **RESOLVED by w7-plat-1**: real `/api/proxy-pools*` CRUD + `/batch` + SSRF-guarded `/test` shipped (`internal/admin/proxypools.go`, `internal/platform/proxypools.go`, `internal/store/proxypools.go`); mock corrected to mirror Go; PAR-UI-019/104/105 + PAR-PLAT-001..005/009 + PAR-AUTH-020 → HAVE.
- [ ] tunnels backend absent (PAR-UI-112 + PAR-UI-113 + PAR-UI-114 + `/tunnels` page) — no Go `/api/tunnels*`; the ONLY in-tree tunnel reference is `internal/server/guard.go:135-141` (a settings-driven `tunnelDashboardAccess`/`tunnelUrl`/`tailscaleHost` host-access guard — forward-looking, NOT a CRUD route; never edited by w6-m). The ref's `/api/tunnel/{enable,disable,tailscale-*}` paths DIVERGE from the in-tree mock `/api/tunnels/{type}` (REMAPPED, §1.5). w6-m ships `/tunnels` PARTIAL vs the MOCK (`GET /api/tunnels`, `GET /api/tunnels/health`, `POST/DELETE /api/tunnels/{type}` for cloudflare/tailscale). W7 Go follow-up: real tunnel status/enable/disable over the Cloudflare + Tailscale subsystems. (ESC-1c) — **RESOLVED by w7-plat-2**: real `/api/tunnels` + `/api/tunnels/health` + `POST/DELETE /api/tunnels/{type}` shipped (`internal/admin/tunnels.go`, `internal/platform/tunnel/*`, `internal/store/tunnels.go`); enable/disable/status/health state machine + injectable `Runner` FULLY unit-tested via a fake (hermetic — no spawn/download/network); real cloudflared/tailscale binary download + process spawn + OS-privileged tailscale install/TUN are integration-only behind `Runner` (§1.9 / ESC-OS-PRIV); mock verified to already mirror the Go `{data}` 4-field DTO (no correction needed); PAR-UI-112/113/114 + PAR-PLAT-015..023 → HAVE.
- [ ] tunnels live-status harness = REST-poll, NOT SSE (RESOLVED, §1.5) — both the 9router ref (`setInterval` poll on `/api/tunnel/status`, ZERO `EventSource`) and the in-tree mock (`GET /api/tunnels` + `/api/tunnels/health`, no `/stream`) are REST. The e2e `MockEventSource` (`fixture.ts:35-111`) has branches ONLY for `/api/traffic/stream` + `/api/console-logs/stream` — NO tunnels branch, and none is needed. Decision: the tunnels page reads status via `apiFetch` REST (mount + optional interval/interaction refresh); NO `EventSource`, NO `fixture.ts` edit. If a real streaming status endpoint is later wanted, that is a W7 follow-up requiring a sanctioned `fixture.ts` branch — NOT w6-m.
- [ ] NO routes_admin.go serial slot — w6-m adds ZERO Go and NEVER touches `internal/server/routes_admin.go`/`internal/admin/*`/`guard.go`. The wave-6 serial chain (w6-pre→w6-d→w6-e→w6-j) had already CLOSED on w6-j; w6-m holds NO slot and releases nothing. routeTree.gen.ts is UNCHANGED (all three routes already registered; w6-m is NOT a new-route plan — the SOLE diff-gate difference from w6-l).

## WAVE-7-MAP (backend parity to 100%) — 2026-06-14
- [ ] Deploy relays (PAR-PLAT-006/007/008 + PAR-UI-106/107/108) build or defer? — Heavy external-API surface (Vercel/Cloudflare-Workers/Deno multipart upload + deployment polling + SSO toggle), niche value; recommend defer to post-W7 escalation. Affects whether W7 "100%" includes 6 deploy rows.
- [ ] Cookie-auth reverse-engineered providers (PAR-PROV-030 perplexity-web, PAR-PROV-031 grok-web) build or defer? — Fragile RE'd web endpoints; matrix Go-port note says defer until after GA. Recommend exclude from w7-prov-special committed set.
- [ ] Translation Stage-2 rows (PAR-TRANS-006 strip content-types, PAR-TRANS-050b responses-passthrough stream) — both blocked on multi-part Message.Content schema (Stage-1 is Content string); needs non-additive schema change. Schedule a schema-evolution plan or defer to responses-passthrough wave? Not in current W7 backend set.
- [ ] PAR-AUTH-003 (HS256 JWT sessions) — g0router uses opaque revocable DB tokens by design (more secure). Variant decision, not a gap. Label as variant-HAVE or leave MISSING-by-design with rationale?
- [ ] MCP Cowork / Claude-Desktop 3p config (PAR-MCP-018..023, 045..050) + Cursor MCP protobuf (PAR-MCP-026..029) — Cowork is globally disabled in the 9router ref (PAR-MCP-042 EXTRA); protobuf only matters if cursor adapter is built. Scope into w7-mcp-3 only if cheap, else defer (low parity value)?
- [ ] Media-provider adapters (w7-prov-media, 12 rows: PAR-PROV-053..066) — image/video/stt/tts/embedding specialists, non-chat interface methods, NO UI page (media-providers UI W6-deferred). Matrix Stage-1 ranking defers all. Scope catalog entries only and defer real adapters to a media escalation, or build in W7?
- [ ] Platform OS-privileged ops — MITM trust-store auto-install (PAR-PLAT-025), hosts-file patching (PAR-PLAT-026/027), tray/auto-start/auto-update (PAR-PLAT-030..036), CLI-tools auto-config (PAR-PLAT-037/038), IDE token auto-import (PAR-PLAT-039..043). Desktop-app concerns ill-suited to a server binary. Recommend defer to a "desktop/agent" escalation (matrix: "drop for server binary; add g0router service install"); W7 scopes only the dashboard-controllable halves. Confirm?
- [ ] Combos DTO reconciliation direction (w7-route) — real Go {name,models[]} vs UI Combo {id,strategy,steps[],is_active}. Adapt the W6 page to the real DTO (frozen-file serial follow-up) or carry a compatibility shape in the Go DTO? Default = real Go canonical, page adaptation tracked separately.

## w7-prov-openai - 2026-06-14
- [x] ESC-1 (format-claude escalation — RESOLVED disposition): PAR-PROV-013 (minimax/minimax-cn), PAR-PROV-034 (glm), PAR-PROV-036 (kimi) use format:"claude" (Anthropic Messages wire format, baseUrl …/anthropic/v1/messages or …/v1/messages). The generic adapter is openai-only (factory.go:108). These three rows stay MISSING in the matrix (annotated). Question for orchestrator: which W7 plan owns the format:"claude" API-key providers (glm/kimi/minimax/minimax-cn/agentrouter)? They need a claude-capable generic path, not a specialized adapter.
- [x] ESC-2 (opencode-go specialization — recorded, non-blocking): catalog entry satisfies PAR-PROV-048 HAVE. OpenCodeGoExecutor's subscription token-exchange auth is a w7-prov-oauth concern. Catalog base_url+format entry is correct and sufficient for generic adapter routing.
- [x] ESC-3 (codebuddy device-code auth — recorded, non-blocking): catalog entry satisfies PAR-PROV-051 HAVE. Device-code OAuth acquisition is a w7-prov-oauth concern. Catalog base_url+format entry is correct.
- [x] ESC-4 (agentrouter is claude — EXCLUDED from PAR-PROV-067 openai set): agentrouter uses format:"claude" (providers.js:406, CLAUDE_CLI_SPOOF_HEADERS). PAR-PROV-067 flipped HAVE for 28/29 openai free-tier providers; agentrouter carved out with a matrix footnote. Orchestrator decision: PAR-PROV-067 accepted HAVE for 28 providers with agentrouter tracked under ESC-1.
- [x] ESC-5 (targetFormat field absent — deferred): ModelEntry has no TargetFormat field and this plan does NOT add one (no struct change). Ref entries with targetFormat:"claude" (opencode-go minimax-m2.7/m2.5, etc.) are ported with ID/Name/UpstreamModelID only; per-model claude routing is a read-site/Stage-2 concern.
- [x] ESC-6 (no-catalog providers — informational): gitlab, codebuddy, vercel-ai-gateway, chutes have no static model block in the ref. Their rows flip HAVE on the catalog entry alone; models are discovered at runtime. Tests assert ModelsFor is empty for these.
- [ ] api/models owned_by non-determinism (preflight fix applied): internal/api/models.go now iterates catalog.Providers in sorted key order; api test spot-checks updated to use provider-unique model IDs. When more providers are added (future waves), any newly shared model ID may need its test spot-check updated to a unique ID — document as a pattern for future catalog expansions.

## w7-gov-1 (Governance backends A — teams + user-management auth + audit-log) - 2026-06-14
- [x] ESC-USERMGMT — `/api/auth/setup` semantics + User shape — **APPLIED (recommended default):** `setup` = first-user onboarding (succeeds iff CountUsers()==0; creates admin user hashed + auto-authenticates with a session cookie via `Sessions.Login`; returns 409 if users exist; registered PUBLIC, self-guarding). Added additive `ensureColumn` users.display_name+users.role + `CreateUserFull`; `CreateUser(username,hash)` now delegates to `CreateUserFull(..., username, "admin")` so SeedAdmin/existing callers are unchanged. Implemented in `internal/admin/usermgmt.go` + `internal/store/users.go`.
- [x] ESC-AUDIT-WRITE — audit write-integration scope — **APPLIED (recommended default):** w7-gov-1 shipped audit store + `GET /api/audit` read + `governance.AuditService.WriteAudit(actor,action,target,details)`, and wired best-effort/post-success writes into ITS OWN mutations only (team create/update/delete; user create/delete; password change; setup) via `Handlers.recordAudit`. A WriteAudit failure is logged and never fails the parent. NOT retrofitted into pre-existing handlers — see the follow-up below. The mock seed's key-lifecycle actions stay illustrative.
- [x] ESC-ARCH — teams transport→store edge — **RESOLVED at impl:** no phase-12B arch test exists in-tree enforcing transport→domain→repository (grep found none; virtualkeys/apikeys call `h.store` directly with no enforcing test). Followed the virtualkeys precedent: teams handlers call `h.store` directly; NO `internal/governance/teams.go` created. Audit keeps a domain service (`governance/audit.go`) only because of the `WriteAudit` seam.
- [ ] Audit-write follow-up (deferred to a later plan): retrofit `WriteAudit` calls into the pre-existing mutation handlers (apikeys/virtualkeys/providers/connections/combos/disabledmodels) so their actions land real audit entries. `Handlers.recordAudit` + `governance.AuditService` are already in place for the follow-up to reuse. — Until then those actions are not recorded in the live audit trail.
- [x] teams mock POST extras + audit POST removal (mock-mirror corrections, decision 1) — **APPLIED:** `teams.ts` POST drops `keys_count`/`members`; `audit.ts` removed the unused POST; `auth.ts` setup/users routes mirror the Go {data}+userDTO (login/logout/status untouched). ESC-MOCK not triggered — the full playwright suite stayed green (no non-w7-gov-1 spec red).

## w7-gov-2 (Governance backends B — feature-flags + prompt-templates) - 2026-06-14
- [ ] ESC-IDTYPE — INT vs TEXT primary key (recommended default, awaiting confirmation) — Both `feature_flags` and `prompt_templates` use `INTEGER PRIMARY KEY AUTOINCREMENT` (int64 ids), DIVERGING from the w7-gov-1 teams TEXT-PK `newID()` precedent. Driven by the binding contract: mocks + UI types declare `id:number` and the feature-flags toggle spec hard-asserts a numeric URL (`/\/api\/feature-flags\/\d+$/`, `feature-flags.spec.ts:29`). Handlers parse `{id}` via `strconv.ParseInt`, not the string-only `pathID`. — A wrong id type silently 404s the toggle/CRUD and breaks the spec; document the per-domain PK divergence.
- [ ] ESC-PROMPT-TEST — `/api/prompt-templates/test` response shape (recommended default, awaiting confirmation) — `/test` is NOT UI-consumed (`grep -rn "prompt-templates/test" ui/src/` → ZERO) and no spec exercises it. Go returns `{data:{rendered:string}}` mirroring the mock `{rendered}` (`prompts.ts:39`); default request `{prompt_id?, system_prompt?, sample?}`, deterministic dependency-free render (resolved system_prompt + sample, NO live LLM). Richer interpolation is a follow-up. — Endpoint built for backend parity but has no e2e assertion; shape is escalation-defaulted.
- [ ] ESC-PROMPT-UPDATEDAT — DTO field set (recommended default) — STORE `updated_at` in the table (hygiene) but OMIT it from `promptDTO` to match the UI `PromptTemplate` type; corrected mock POST/PUT DROP the injected `updated_at` (`prompts.ts:11`). — Mock-mirror correction; low risk.
- [ ] ESC-FF-GETBYID — optional single-read route (recommended: include) — Mock has `GET /api/feature-flags/{id}` the page never calls; Go includes it for parity (cheap, no spec rides on it). Omit if a strictly minimal toggle-only surface is preferred. — Spec passes either way.
- [ ] ESC-FF-STORE / ESC-FF-DOMAIN / ESC-PROMPT-DOMAIN — store + layering decisions (recommended defaults) — Dedicated `feature_flags` + `prompt_templates` tables (NOT `kv.go`). NO `internal/governance/{featureflags,prompttemplates}.go` by default — per the w7-gov-1 ESC-ARCH finding (no in-tree arch test enforces transport→domain→repository; teams/virtualkeys call `h.store` directly). Add a thin prompt domain file ONLY if `/test` render logic grows non-trivial. Deliberate deviation from the MAP's listed new governance files. — Documents the layering deviation vs the MAP row.
- [ ] ESC-ROUTE — `/api/prompt-templates/test` vs `…/{id}` precedence (conditional) — Register STATIC `/test` BEFORE the `{id}` param (`routes_admin.go` teams precedent). Add an explicit Go handler test that `/test` resolves to `TestPromptTemplate`, not `GetPromptTemplate`. If fasthttp/router mis-disambiguates, escalate for a path arrangement (e.g. `/api/prompt-templates-test`); `/test` is not UI-consumed so a mis-route would NOT red a spec — silent failure risk. — Route ordering is the one real wrinkle in this plan.
- [ ] ESC-AUDIT-REUSE — reuse the w7-gov-1 audit seam (recommended default) — Flag toggle + prompt create/update/delete call `h.recordAudit(ctx,action,target,details)` (`audit.go:64-72`) best-effort/post-success, mirroring w7-gov-1's ESC-AUDIT-WRITE. NO edit to audit.go/governance/audit.go/handlers.go (the `audit` field + accessor already exist). `details` = human-readable summaries (flag key, template name), never raw payloads. — Consumes the w7-gov-1 seam read-only; no new audit code.
- [ ] Serial-slot dependency — w7-gov-2 TAKES the routes_admin.go slot after w7-gov-1 releases it (chain MAP §219-224) and RELEASES it to w7-gov-3 on close. Orchestrator confirms exactly one unmerged holder before T-routes. — Single-holder rule (decision 3).

## w7-plat-1 (proxy-pools backend + outbound proxy/SSRF mitigation) — 2026-06-14
- [ ] ESC-SSRF-SCOPE follow-up (broad outbound-target SSRF retrofit) — w7-plat-1 guards the connectivity-test probe target AND the user-configured proxy host (the directly user-controlled PAR-AUTH-020 vectors). It does NOT guard all provider outbound targets (catalog base URLs) — that risks blocking legitimate self-hosted backends. The `internal/platform/outboundproxy.go` evaluator (`IsBlockedIP`/`IsBlockedTarget`) is reusable for that future scope. Tracked follow-up; no spec rides on it.
- [ ] ESC-USAGE (`?includeUsage` field) — DEFERRED. The w6-m proxy-pools page calls `GET /api/proxy-pools` with NO query args (verified P4). w7-plat-1 accepts `?isActive` (filters) and `?includeUsage` (harmless no-op — does not add a `usage_count` field) but the page never sends them. If a future UI needs per-pool bound-connection counts in the list, add `usage_count` gated on `?includeUsage=true` (the `CountConnectionsUsingProxyPool` query already exists).
- [ ] ESC-BATCH (shipped) — `POST /api/proxy-pools/batch` ships (thin loop over Create, returns `{created:N}`) to mirror the registered mock, even though the w6-m page does not call it. Cheap parity; can be removed if confirmed dead.
- [ ] Proxy relay-deploy types (PAR-PLAT-006/007/008 — cloudflare/vercel/deno) remain MISSING. w7-plat-1 ships protocol-based pools (http/https/socks5); the ref's relay-deploy `type` ∈ {vercel,cloudflare,deno} + `*-deploy` sub-routes are a separate follow-up. PAR-PLAT-002 flipped HAVE for the CRUD+validation half; the relay-deploy half stays in PAR-PLAT-006/007/008.
- [ ] PAR-AUTH-020 MITM DNS-bypass half — w7-plat-1 delivers the SSRF/outbound-proxy half (→ HAVE). The "MITM DNS bypass" clause of PAR-AUTH-020 is the w7-plat-3 (mitm) surface; revisit the row note when w7-plat-3 lands.
- [ ] PRE-EXISTING e2e flake (not introduced by w7-plat-1) — `comprehensive.spec.ts:48` ("unauthenticated → /login" toHaveURL timeout) fails at base and HEAD identically (WORKFLOW.md:7000/7088/7676/7787). proxy-pools.spec isolated = 5/5 green; full suite = 150 passed / 1 pre-existing flake. No proxy-pools file touches comprehensive.spec.
- [ ] Serial-slot dependency — w7-plat-1 TOOK the routes_admin.go slot after w7-gov-3 released it and RELEASES it to w7-plat-2 on this close. The selection.go micro-serial slot was taken at T-proxywire and released after (no unmerged w7-route selection.go edit was in flight). Orchestrator confirms exactly one unmerged holder of each before the next plan.

## w7-plat-2 (tunnels backend — cloudflared + tailscale) — 2026-06-14
- [ ] ESC-OS-PRIV (ESCALATED, recommended default) — tailscale install (PAR-PLAT-019) + TUN-mode daemon (PAR-PLAT-020) are OS-privileged; cloudflared binary download (PAR-PLAT-015) needs network. The parity bar = admin API + state machine + injectable TunnelRunner (FULLY unit-tested via the fake). The real download/spawn/install/TUN are a THIN real impl behind `Runner`, integration-only (NOT unit-tested). Default tailscale mode = userspace-networking (no TUN, no root); TUN is escalated opt-in. — PAR-PLAT-015/019/020(TUN) flip HAVE with an "integration-only / OS-privileged" footnote; flag for orchestrator confirmation.
- [ ] ESC-CF-MODE (recommended default) — `POST /api/tunnels/cloudflare` with a token → named tunnel (`tunnel run --token`); without → quick tunnel (`tunnel --url`, extract `*.trycloudflare.com`). Token-presence (or explicit `mode`) selects the mode. Seed url contains trycloudflare.com → quick is the demoed path. — Flag.
- [ ] ESC-TS-MODE (recommended default) — tailscale userspace-networking default (no TUN/root); TUN escalated (ESC-OS-PRIV). — Flag.
- [ ] ESC-MAGICBYTE (recommended default) — factor a PURE `isValidExecutable(head []byte, goos string) bool` (unit-tested on canned ELF/Mach-O/PE bytes) out of the integration-only download path; download stays integration-only. — Flag.
- [ ] ESC-SEED-ROWS (recommended default) — `ListTunnels` returns the 2 known types (cloudflare, tailscale) overlaying any stored row → always exactly 2 entries (matches the spec's 2-card assertion) WITHOUT a seed migration. Alternative: `EnsureTunnelRows` on migrate. — Flag.
- [x] ESC-HEALTH-USE (RESOLVED) — P5 grep confirms the w6-m page DOES call `/api/tunnels/health` (`ui/src/routes/tunnels.tsx:39`, drives the healthy/unhealthy badge). `/health` shipped as a first-class endpoint backed by the Go admin test (`TestTunnelHealth`). No spec surprise.
- [x] ESC-CF-MODE / ESC-TS-MODE / ESC-MAGICBYTE / ESC-SEED-ROWS (RESOLVED — recommended defaults implemented) — cloudflared token-presence selects named-vs-quick (quick extracts `*.trycloudflare.com`); tailscale userspace-networking default; pure `isValidExecutable` magic-byte validator unit-tested; `List()` overlays the 2 known types over stored rows (always 2 entries) with no seed migration.
- [x] ESC-MOCK / ESC-ROUTE / ESC-ARCH (RESOLVED) — mock VERIFIED to already mirror the Go: `json()` wraps in `{data}`, GET array = 4-field DTO, `/health` = `{healthy}`, seed cloudflare url keeps `trycloudflare.com` → NO mock correction needed (T-mocks = verify-only, no commit). `/api/tunnels/health` (static) registered before `/api/tunnels/{type}` (param) with NO router collision (server tests green) — ESC-ROUTE not triggered. Built `internal/platform/tunnel` domain service per the SHIPPED proxy-pools layering precedent.
- [ ] ESC-GUARD-SETTINGS (conditional — DEFAULT TAKEN, follow-up) — w7-plat-2 does NOT write `settings["tunnelUrl"]`/`["tailscaleUrl"]` on enable (kept the surface minimal; operator sets them manually today). guard.go (`:135-141`) is consumed unchanged. Follow-up: a future plan may wire active-tunnel host into settings so the host-access guard sees the live host. NEVER edit guard.go.
- [x] Serial-slot — w7-plat-2 TOOK the routes_admin.go slot after w7-plat-1 released it (§116 above) and RELEASES it to w7-plat-3 on this close. NO selection.go micro-serial (tunnels are not an inference-path concern). Orchestrator confirms exactly one unmerged holder before the next plan.
- [ ] Serial-slot dependency — w7-plat-2 TAKES the routes_admin.go slot after w7-plat-1 released it (open-questions w7-plat-1 §116) and RELEASES it to w7-plat-3 on close. NO selection.go micro-serial (tunnels do not touch inference). Orchestrator confirms one unmerged holder before T-routes.

## w7-plat-3 (MITM backend — root CA + CA-cert serving + MITM proxy + per-tool toggle) - 2026-06-14
- [x] P7 base observation — Go `go build/vet/test ./...` green at base (1488 tests); `cd ui && npm run build` green; `npx playwright test e2e/mitm.spec.ts` green (5/5) at base against the W6 mock. `<base> = b6736b82ce206ceb70aa38b59962b62f5e26956e`. P1 greenfield confirmed (no `/api/mitm` Go, no `internal/{store,admin}/mitm.go`, no `internal/platform/mitm`, no `mitm_tools` table, ZERO `x509`/`crypto/tls` in `internal/`+`cmd/`).
- [x] ESC-CA-STORE (RESOLVED — option A implemented) — root CA cert+key as files under the data dir: `dataDir/mitm-ca.key` (0o600) + `dataDir/mitm-ca.crt` (0o644), generate-on-first-use, mirroring `internal/store/secret.go`. `store.DataDir()` gives the dir (in tests the t.TempDir DB dir). The private key NEVER leaves the process / never served / never logged / never in any DTO; only the public `CertPEM()` is served by the raw-PEM endpoint. **Flag for orchestrator: switch to (B) `mitm_ca(cert_pem,key_enc)` if single-store backup semantics are preferred.**
- [x] ESC-GLOBAL-FLAG (RESOLVED — settings) — the global `enabled` lives in the existing key-value `settings` surface under `mitmEnabled` via `store.GetSetting`/`SetSetting` (verified present at P4). No new `mitm_state` table needed.
- [x] ESC-SCHEMA (RESOLVED — typed columns) — `mitm_tools(id,name,enabled,dns_override,status,updated_at)` typed columns, mirroring the SHIPPED `proxy_pools`/`tunnels` blocks; additive table in the migrate `tables` slice (no DROP/RENAME).
- [x] ESC-KEYTYPE (RESOLVED — ECDSA P-256) — fast deterministic key-gen for cheap hermetic unit tests; modern; widely trusted for MITM leaves. Switch to RSA-2048 only if a target client rejects ECDSA leaves.
- [x] ESC-SEED-ROWS (RESOLVED — EnsureMitmTools, lazy) — the 2 named tools (mitm-1 Request Inspector, mitm-2 Response Modifier) are seeded via `EnsureMitmTools()` (INSERT ... ON CONFLICT DO NOTHING, idempotent). Called lazily by the service `Status`/`ToggleTool` so a fresh store always surfaces >=2 entries (keeps the 2-row mitm.spec green) without a forced migrate-time seed.
- [x] ESC-BACKOFF (RESOLVED — pure nextBackoff + integration-only loop) — `nextBackoff(attempt)` = 1s doubling, capped at 30s, max 5 attempts; PURE + unit-tested (doubling+cap). The retry loop that sleeps between `proxy.Start` attempts is integration-only (real sleeps on a real listener). NOTE: 9router's reference delays are [5s,10s,20s,30s,60s]; the in-tree default uses a 1s-base doubling capped at 30s (parity is the backoff *behavior*, not the exact constants) — adjust constants if exact-9router timing is later required.
- [x] ESC-CACHE (RESOLVED — unbounded mutex-guarded map) — leaf cache is `map[host]tls.Certificate` under a `sync.RWMutex`, minted-on-miss. Follow-up: add an LRU/TTL honoring leaf NotAfter only if memory becomes a concern.
- [x] ESC-MOCK / ESC-ROUTE (RESOLVED) — mock VERIFIED to already mirror the Go: `json()` wraps `{data}`, status = `{enabled,tools}`, tool = 5-field DTO, ca-cert = `route.fulfill` raw PEM `application/x-pem-file` (NOT `{data}`), seed = 2 named tools → NO mock/seed correction needed (T-mocks = verify-only, no commit). The 3 static `/api/mitm/<word>` routes + `/api/mitm/tools/{id}` param route register with NO router collision (server tests green) — ESC-ROUTE not triggered.
- [ ] ESC-OS-PRIV (DEFERRED/ESCALATED) — system-trust-store auto-install (PAR-PLAT-025 trust half) + hosts-file patching (the `dns_override` OS-enforcement, PAR-PLAT-026/027) + the AES sudo-password store (PAR-PLAT-029) are OS-privileged and out of scope for the server binary. The parity bar met = CA-gen + raw-PEM CA-cert serving + the proxy crypto core (mint/sign/verify/cache) + the admin API (FULLY unit-tested). The `dns_override` field is STORED + surfaced (config), not OS-enforced. Follow-up: a desktop/agent escalation wires trust-store install + hosts patching + the live forward target (handleIntercepted currently drains).
- [x] Integration-only listener (NOT unit-tested, §1.9) — `proxy.go` `listenerProxy.Start`/`Stop`/`serve`/`handleIntercepted` (real `net.Listen` + `tls.NewListener` + intercept loop) are excluded from `go test ./...` determinism. The `GetCertificate` closure (`certForClientHello`) is factored out and unit-tested with a synthetic `*tls.ClientHelloInfo` (no bind). Unit suite is fully hermetic (no listen/dial/handshake/exec in any `_test.go`).
- [x] Serial-slot — w7-plat-3 TOOK the routes_admin.go slot after w7-plat-2 released it (§ above) and RELEASES it to w7-misc on this close. NO selection.go micro-serial (MITM is not an inference-path concern). Platform track (plat-1/plat-2/plat-3) complete. Orchestrator confirms one unmerged holder before the next plan.

## w7-mcp-1 (MCP foundation — store + launcher + stdio↔SSE bridge) - 2026-06-14
- [ ] ESC-ALLOWLIST (RESOLVED at authoring) — allowlist = exact 9router set `npx,node,uvx,python,python3,bunx,bun` (coworkPlugins.js:67). Hardening over 9router's raw path.basename: match filepath.Base, REJECT shell metacharacters + relative paths (`./npx`), permit bare base OR absolute clean path. Unit-tested with every rejection (rm/bash/bash -c/sh/./npx/../npx//bin/rm/`npx; rm -rf /`/`$(x)`/empty). Flag for orchestrator confirmation of the metachar/abs-path policy.
- [ ] ESC-PERSIST (RESOLVED) — custom plugins persist to the SQLite `mcp_clients` table (NOT 9router's `DATA_DIR/mcp/customPlugins.json`); parity is the survive-restart behavior, not the file. Matches the WAL-store decision.
- [ ] ESC-SCHEMA (RESOLVED) — typed columns + JSON `*_json` columns for free-form `Config map[string]any`/`Args []string`/`Env map[string]string` (mirror alert_channels.events_json @ migrate.go:187). 4 additive tables: mcp_clients, mcp_instances, mcp_oauth_accounts, mcp_oauth_flows.
- [ ] ESC-FILTER (CONDITIONAL) — port `smartFilterText`/`collapseRepeated` (stdioSseBridge.js:20-89) verbatim: drop `generic`+empty `text`, collapse repeated role-prefixed siblings, 50_000-char hard cap, mutate only `type==="text"`. Unit test pins observable behavior; if a regex is ambiguous when implementing, STOP + ESCALATE with the ref text — never fabricate filter semantics.
- [ ] ESC-SPAWN (RESOLVED — integration-only, mirrors w7-plat-2 ESC-OS-PRIV) — real `os/exec` spawn/kill + pipes + cmd.Wait (osProcessRunner, process.go) are INTEGRATION-ONLY (spawning arbitrary MCP servers in CI is the RCE risk 9router cites at PAR-MCP-042). Parity bar = launcher+allowlist+bridge+filter+lifecycle FULLY unit-tested via the fake runner. PAR-MCP-003/036 HAVE with "integration-only spawn" footnote.
- [ ] ESC-HTTP-SSE-CLIENT (DEFERRED to w7-mcp-2) — the live HTTP/SSE MCP client (probe handshake, session-id replay, SSE parse — PAR-MCP-009..013) is w7-mcp-2. This plan models the transport MODE + persists the instance + exposes the session-sink seam only; no live dial.
- [ ] ESC-RESPAWN (RESOLVED) — re-spawn a plugin's process on a new request only if the prior process exited (`!IsRunning`); mirror 9router quirk #9 (no health-check before re-spawn).
- [ ] Foundation confirms NO routes / NO UI — w7-mcp-1 adds NO admin handler, NO routes_admin.go edit, NO UI/mock/seed/spec/e2e. The MCP routes_admin serial slot belongs to w7-mcp-3 (MAP §182/§208); this plan holds NONE. The w6-l MCP UI mocks are reconciled by w7-mcp-3. w7-mcp-1 is fully parallelizable with every non-MCP track.
- [ ] mcp-2/3 dependency handoff — w7-mcp-2 (probe/registry/OAuth) + w7-mcp-3 (admin transport + routes + tools/agent) depend on exported constructors: NewLauncher(st)/SetRunner, the Bridge+SessionSink seam, smartFilterText, isAllowedCommand, the store methods (*MCPClient/*MCPInstance/*MCPOAuth*), the default plugin definitions. Keep exported+stable; mcp-2 EXTENDS internal/mcp with NEW files (no edits to mcp-1 files).

## w7-mcp-2 (MCP client: SSE/message transport + probe + registry + OAuth engine) - 2026-06-14
- [ ] ESC-HTTP-SEAM (RESOLVED) — inject an `*http.Client` field per network component (Probe/Registry/Engine), nil-able to a real default, reusing `internal/auth/oauth.go:128` NewOAuthFlow's exact shape. Tests pass `&http.Client{Transport: fakeRoundTripper}`. Probe 8s timeout via `context.WithTimeout` (short ctx + blocking transport in tests); registry 1h cache via injectable `now func() time.Time` (fake clock in tests). NO real network/sleep/clock in any unit test.
- [ ] ESC-MCP-OAUTH-PROTOCOL (CONDITIONAL) — the frozen 9router ref disabled Cowork and ships NO MCP-OAuth engine (PAR-MCP-037 has no port reference). Default: author against the MCP authorization spec (RFC 9728 protected-resource-metadata discovery, RFC 8414 authorization-server-metadata, RFC 7636 PKCE S256) reusing `internal/auth/oauth.go` + the SHIPPED mcpoauth store. Escalate any ambiguous discovery detail (well-known path, resource param, dynamic client registration) with the spec section — never fabricate.
- [ ] ESC-PKCE-GENERALIZE (CONDITIONAL) — `pkceChallenge`/`randomURLSafe` (`auth/oauth.go:274,266`) are unexported. Attempt order: reuse directly; else add an ADDITIVE exported `internal/auth` helper (e.g. `GeneratePKCE()`) wrapping the private funcs with NO signature change; else escalate. NEVER copy the crypto into `internal/mcp`.
- [ ] ESC-STORE-ADD (CONDITIONAL) — health monitor may want `SetMCPOAuthAccountStatus`/`ListMCPOAuthAccountsByStatus`. Default: prefer existing `UpsertMCPOAuthAccount`+`ListMCPOAuthAccounts` (in-memory filter); add an additive method ONLY if a genuine query need exists, TDD'd RED-first. No column/migration change.
- [ ] ESC-REG-DTO (RESOLVED) — registry returns a NEW `RegistryServer` DTO mirroring 9router registry fields (name/slug/title/description/url/transport/oauth/toolNames/toolCount/iconUrl — registry:40-51), NOT a reuse of `PluginDefinition`. mcp-3 maps it to the marketplace/UI DTO.
- [ ] ESC-SSE-STREAM (RESOLVED) — the long-lived streaming SSE response reader (dispatch to SessionSink) + the health-monitor periodic ticker loop are INTEGRATION-ONLY; the PURE frame parsers + message-POST client + PURE health derivations are FULLY unit-tested. PAR-MCP-001/002/055/056 (client half) + PAR-MCP-038 HAVE with an "integration-only stream/ticker" footnote.
- [ ] w7-mcp-2 client confirms NO routes / NO UI — adds NO admin handler, NO routes_admin.go edit, NO UI/mock/seed/spec/e2e. The MCP routes_admin serial slot belongs to w7-mcp-3 (MAP §181/§208); this plan holds NONE. Serially AFTER w7-mcp-1 (consumes its constructors), BEFORE w7-mcp-3 (which consumes this plan's probe/registry/engine/transport).
- [ ] w7-mcp-3 dependency handoff — w7-mcp-3 depends on this plan's exported surface: NewProbe/Probe.Run, NewRegistry/Registry.List+RegistryServer, NewEngine+start/complete/refresh, the SSE/message client transport+parseSSEFrame/parseSSEDataFrames, the health derivations, the discovery cache+compact manifest. Keep exported+stable; mcp-3 EXTENDS internal/mcp with NEW files (agent.go) + adds the SERVER SSE/message routes + admin handlers — no edits to this plan's files.

## w7-mcp-3 (MCP admin transport + tools + skills + agent loop) closeout — 2026-06-14
- [x] ESC-CASING (RESOLVED as authored) — clientDTO/instanceDTO carry EXPLICIT PascalCase json tags (`ID`/`Name`/`Transport`/`Command`/`Args`/`Env`/`URL`/`IsActive`/`HealthStatus`/`CreatedAt`/`UpdatedAt`), a deliberate recorded exception to the project snake_case convention because the FROZEN w6-l page consumes PascalCase; toolGroupDTO snake_case, toolDTO OpenAI-shape, skillDTO flat-lowercase, accountDTO snake_case. RESULT: ZERO mock-body change — the four w6-l surfaces flipped variant-HAVE → true-HAVE with no mock/seed correction (mcp.spec + skills.spec green, the casing matched as predicted).
- [x] ESC-AGENT-DEPTH (RESOLVED as authored) — shipped the loop PRIMITIVE: bounded multi-turn `Agent.Run` with injected `ModelStep`/`ToolExecutor`, hard `maxTurns` cap (default 8); + the shared real bridge-backed `ToolExecutor` (`mcp.NewBridgeToolExecutor`) that `ExecuteTool` also uses. NO new agent HTTP route (the w6-l page has no agent surface). The LLM round-trip is integration-only behind the injected `ModelStep`. Unit-tested: terminate-on-final / execute-then-feed-back / stop-at-maxTurns / tool-error-surfaced — all hermetic (no LLM/process/network).
- [x] ESC-TOOLPOLICY-SCOPE (RESOLVED as authored) — ported the PURE builders (`stripServerPrefix`/`buildToolPolicy`/`buildManagedServers`/`sanitizePluginName`, `internal/mcp/toolpolicy.go`, unit-tested) and applied them at g0router's surfaces (tools-list prefix-strip PAR-MCP-046, create-name sanitize PAR-MCP-048, allowlist filter PAR-MCP-049 via the launcher). No managed-server READ endpoint exposed (no frozen page consumes it). PAR-MCP-018/019/050 marked HAVE-SUBSET.
- [x] ESC-ANTIGRAVITY (RESOLVED as authored) — PAR-MCP-060 ride-along only: PURE constant `unavailableAntigravityTool` (`mcp_sequential-thinking_sequentialthinking`, `{description:"This tool is currently unavailable.", unavailable:true}`) included in `ListTools`. The antigravity EXECUTOR remains w7-prov-special; no executor added here.
- [x] ESC-COWORK-CONFIG (RESOLVED as authored) — `DeleteInstance` removes the instance + best-effort `Launcher.Stop(name)`; the Cowork 3p-config / skip-approvals / 1p-legacy file rewrite is N/A to g0router (no such files). PAR-MCP-050 HAVE-SUBSET.
- [x] ESC-CLIENT-SRC (RESOLVED) — `ListClients` returns the union of `store.ListMCPClients()` ∪ `mcp.DefaultPlugins()` mapped to clientDTO, so the marketplace always shows the default catalog at real runtime. The e2e spec runs against the MOCK seed (GitHub), so the Go source choice does not affect spec green — runtime-only.
- [x] ESC-SKILLS-SRC (RESOLVED as authored) — static Go `skillsCatalog()` (no store table): 2 "Endpoint Skills" entries (filesystem/github) mirroring the seed + the default-plugin entries under "Default Plugins". Read-only, matching the frozen page (no create/delete control).
- [x] ESC-OAUTH-REDIRECT (RESOLVED) — `mcpRedirectURI` mirrors the shipped provider-OAuth `resolveRedirectURI` derivation (settings `oauth_redirect_uri` override → request Origin → scheme+host) with the MCP callback path `/api/mcp/auth/callback`.
- [x] ESC-PROBE-FIELD (RESOLVED) — added a nil-able `mcpProbe *mcp.Probe` field + `SetMCPProbe` setter; `ListTools` runs `Probe.Run` per http/sse instance when injected, else degrades to the default-plugin catalog. The unit test does not need the probe (the default catalog + antigravity ride-along satisfy a non-empty list).
- [x] ESC-BOOTSTRAP (RESOLVED) — the real launcher/engine/probe are wired in `internal/server/routes_admin.go` `NewAdminHandlers` (the same place `SetUsageServices` is called), as an additive append in the SAME serial-slot commit (no separate bootstrap file touched). `h.SetMCPLauncher(mcp.NewLauncher(st))` / `h.SetMCPEngine(mcp.NewEngine(st,nil))` / `h.SetMCPProbe(mcp.NewProbe(nil))`.
- [x] Integration-only spawn/network/LLM — the real `os/exec` stdio spawn (`osProcessRunner`), the live SSE stream/`sseClient` network dial, and the real LLM agent round-trip are excluded from `go test ./...` determinism; every unit test injects a fake runner/transport/ModelStep (no real spawn/network/LLM/port-bind, verified by the §5 grep proof).
- [x] Serial slot — w7-mcp-3 TOOK the `routes_admin.go` slot (free after w7-plat-3; chain …→w7-gov-3→w7-plat-1/2/3→**w7-mcp-3**) and RELEASES it to the next holder on this close commit. routes_admin.go touched in exactly ONE additive commit.

## w7-platnodes (Provider-node prefix-routing engine) closeout — 2026-06-14
- [x] ESC-SCHEMA (RESOLVED as authored) — additive typed columns `prefix`/`api_type` on the existing `providers` table (`ensureColumn`, `internal/store/migrate.go`) rather than a separate JSON-`data` table; continues the w6-f node-on-providers mapping, enables `WHERE prefix=?` lookup + the providers-row cascade. Defaults `''` keep all existing providers callers green.
- [x] ESC-HOOK (RESOLVED as authored) — the node-prefix override is the FIRST step of `Router.Resolve` (`internal/inference/router.go`), BEFORE `ResolveModelAlias`/`providerForModel`; additive (`SetNodeResolver` + step-0 block; NO `NewRouter` signature change; nil resolver → byte-identical to the static chain, proven by `TestNilNodeResolverByteIdentical`).
- [x] ESC-NODE-PROVIDER (RESOLVED — DEVIATION recorded) — no existing provider constructor accepts a runtime base URL without a signature change (`generic.New` is catalog-bound; `openai`/`anthropic` have fixed base URLs; the `schemas.Provider` interface is ~28 methods, infeasible to re-implement in `inference`). Per ESC-NODE-PROVIDER's sanctioned "additive helper", added an ADDITIVE `generic.NewNode(id, baseURL)` constructor (new exported func; ZERO change to existing `New`/fields/`SetNetworkConfig`) and a thin `inference` helper `buildNodeProvider`. NOTE: this touched `internal/providers/generic/provider.go`, which is ABSENT from the §7 file list — an authoring omission given §1.5/ESC-NODE-PROVIDER explicitly anticipated building the node provider. The change is strictly additive. Both openai- and anthropic-compatible nodes are served through the generic OpenAI-compatible adapter pointed at the node base URL; per-`api_type` adapter selection (route anthropic-compatible through the anthropic adapter) is a TRACKED FOLLOW-UP.
- [x] ESC-STORE-SHAPE (RESOLVED as authored) — `ProviderRecord` extended additively with `Prefix`/`APIType` + the shared INSERT/SELECT/UPDATE/scan column lists; node-specific reads (`ListProviderNodes`, `GetProviderNodeByPrefix`, `ListProviderNodePrefixes`, the `nodeTypes` set) live in the new `internal/store/providernodes.go`.
- [x] ESC-CASCADE (RESOLVED — providers-row update) — VERIFIED at P-check that `store.Connection` has NO base-URL field and caches no node prefix/baseURL (`internal/store/connections.go`); connections bind by `provider_id` and resolve the node base URL transitively via the providers row. The cascade is therefore the providers-row update (sanitized base_url/api_type/prefix); NO per-connection `RepointConnectionsForNode` needed (proven by `TestUpdateCascadesBaseURLToProvidersRow`).
- [x] ESC-PROVISION (RESOLVED as authored) — on create, if the request carries an `api_key`, a bound `api_key` connection (`provider_id=node.ID`) is auto-provisioned, encrypted at rest via the connection `*_enc` path, NEVER echoed; without a key, node-only create. Both branches unit-tested (admin + domain).
- [x] ESC-MOCK-DTO / ESC-MOCK-CRUD / ESC-MOCK-VALID (RESOLVED at P4) — the FROZEN page (`ui/src/components/keys/provider-node-modal.tsx`) reads a node DTO of `{id,name,base_url,type,enabled}` and validate `{valid,error?}`; it SENDS prefix/apiType on create but does not READ them back, and does NOT use `{id}` routes. Decisions: added `prefix`/`api_type` to mock `toNode` (mirror Go, harmless — page ignores), extended the GET filter to all three node types, added the `\/api\/provider-nodes\/[^/]+$` GET/PUT/DELETE mock route (mirror new Go surface), kept validate `{valid,error?}` (no `models` list). NO `/api/models/*` branch touched. No spec exercises `{id}`; the Go admin tests are the authoritative `{id}` proof.
- [x] Validate hermeticity / w6-f invariant — the real validate runs through an injectable `NodeProber` + SSRF guard (`platform.IsBlockedTarget`); a resolution error is NOT treated as blocked (falls through to the prober) and `defaultNodeProber` (no injection) returns valid:true on URL-shape, preserving the w6-f `TestProviderNodesValidateURL`/`TestProviderNodesValidateNeverPersistsAPIKey` invariants. api_key used transiently, never persisted/echoed.
- [x] Pre-existing e2e failure (NOT w7-platnodes) — `comprehensive.spec.ts` "unauthenticated user is redirected to login for protected routes" fails identically WITH and WITHOUT the nodes.ts change (verified by stash-and-rerun); it is an auth-redirect test independent of provider-nodes. The 14 nodes-consuming specs (endpoint/keys/providers) pass with the corrected mock. Tracked as a pre-existing flake outside this plan's scope.
- [x] Serial slot — w7-platnodes is FIRST in the routing serial sub-chain; the `routes_admin.go` slot was FREE at P-check (last touch = the merged w7-plat-1 proxy-pools + w7-mcp-3 mcp routes). TOOK it for the `{id}` route commit and RELEASES it to w7-route on this close commit. w7-platnodes UNBLOCKS w7-route (the routing prerequisite). selection.go UNTOUCHED.
- [ ] FOLLOW-UP: per-`api_type` node adapter — route `anthropic-compatible` nodes through the anthropic adapter (not the generic OpenAI-compatible adapter) and `custom-embedding` through the embeddings path. Currently all nodes route through `generic.NewNode` pointed at the node base URL. Non-blocking for PAR-ROUTE-009/040 (prefix override + base-URL routing proven); raise a follow-up plan if anthropic-native node request shaping is required.
- [ ] FOLLOW-UP: `/api/models/*` backend — the `/api/models/test` + `/api/models/availability` mock branches in `nodes.ts` are MOCK-ONLY and belong to w7-misc; w7-platnodes left them untouched. The real Go for those endpoints is w7-misc scope.

## w7-route-a (Routing admin CRUD + quota) - 2026-06-14
- [x] ESC-COMBOS (HIGH RISK — orchestrator path confirmation required before T-routes) — the frozen /combos page needs id-keyed `{id,name,strategy,steps[{provider,model}],is_active}` with a PUT-by-id `{steps}` body (combos.spec.ts:53-66), IRRECONCILABLE with the real Go combo engine `{name,models[]}` keyed by name (combos.go:15-21, routes_admin.go:194). Recommended default: Option A — a NEW separate `combos_admin` table + handlers that OWN `/api/combos[/{id}]` (re-registering the engine HTTP routes after verifying no non-page consumer; engine store + /v1/models lister stay intact via a best-effort mirror-write). Option B (distinct path) needs a frozen-page edit. — **RESOLVED: shipped Option A. The P1 consumer grep confirmed the engine `/api/combos` HTTP routes are consumed ONLY by the frozen admin pages (`combos.tsx`, `combo-form-modal.tsx`, `model-select-modal.tsx` — all of which already expect the id-keyed admin shape); the `/v1/models` combo lister (`routes_openai.go:59 SetComboLister(st)`) reads `store.ListCombos()` DIRECTLY, not via HTTP. The 4 engine HTTP routes were replaced by the 5 admin combos routes; the engine combos store + lister are untouched and fed by the admin handlers' best-effort mirror-write.**
- [x] ESC-ALIAS-SHAPE — UI Alias `{id,alias,provider,model}` (id-keyed) vs frozen gateway ModelAlias `{name,target}` (aliases.go:11). Default: NEW typed `aliases` admin table mirroring the UI shape; gateway model_aliases resolver stays frozen; admin edits best-effort mirror-write. — **RESOLVED: shipped the new typed `aliases` table; the gateway `model_aliases` resolver is untouched and best-effort mirror-written (alias → provider/model target) from admin create/update.**
- [x] ESC-IDTYPE — `model_limits` uses INTEGER PRIMARY KEY AUTOINCREMENT (UI ModelLimit.id is numeric, seed ids 1/2); handlers parse {id} via strconv.ParseInt, not string pathID. Same divergence w7-gov-2 hit. routing_rules + aliases keep TEXT-PK newID() (string seed ids). — **RESOLVED as designed: `model_limits` is INTEGER-PK; `modelLimitID` (strconv.ParseInt) parses `{id}` and 400s on non-numeric; routing_rules + aliases use TEXT-PK newID().**
- [x] ESC-QUOTA-SCOPE — /api/quota aggregates ALL connections via ListConnections through an injectable usage fetcher (default usage.FetchProviderUsage); oauth conns get real used/limit, non-oauth contribute used:0/limit:0. Verify the page does not assert exactly-N cards (quota.spec only asserts .first() visible). — **RESOLVED as designed: aggregates ALL connections (oauth via the fetcher seam, non-oauth zeroed); quota.spec only asserts `.first()` visible (no exact-N assertion), green. No token/secret in any response field (asserted in quota_test.go).**
- [ ] FOLLOW-UP: live routing-rule application — w7-route-a ships routing_rules admin CRUD only; applying rules to live inference traffic is a separate larger concern. Raise a follow-up plan.
- [ ] FOLLOW-UP: combos engine /api/combos HTTP-route retirement (ESC-COMBOS Option A) — if confirmed, the engine `{name,models[]}` HTTP routes are retired in favor of the admin surface; the engine store + /v1/models lister remain. Track any consumer surprises.

## w7-route-b (Dynamic routing engine) - 2026-06-14
- [x] ESC-WEIGHT-SRC — PAR-ROUTE-027 weighted selection reads per-connection weight from Connection.Metadata JSON (`{"weight":N}`, default 1), NO schema change; deterministic weighted accumulator (not math/rand). Alternative: an additive connections.weight column (touches frozen connection store). — **RESOLVED: shipped Metadata `{"weight":N}` (default 1) + smooth weighted round-robin accumulator (`connWeight` + `wrrState` in selection.go). Zero migration; deterministic (3:1 → cA,cA,cB,cA over 4 selects).**
- [x] ESC-055-OVERLAP — PAR-ROUTE-055 per-connection proxy may already be covered by w7-plat-1 PAR-PLAT-009 (merged ResolveProxy hook). w7-route-b VERIFIES; flips ROUTE-055 HAVE citing w7-plat-1 + verification, adding code only for a ROUTE-055-specific gap (connectionProxyEnabled/vercelRelayUrl). — **RESOLVED: VERIFIED — w7-plat-1's `ProxyPoolService.ResolveProxyForConnection` (reads `connection.proxy_pool_id` → active pool → URL) wired via `SelectionEngine.ResolveProxy` fully covers per-connection proxy resolution. g0router's typed `proxy_pool_id` linkage is the architectural equivalent of 9router's `providerSpecificData` proxy config; NO ROUTE-055-specific gap, NO code added. Flipped HAVE citing w7-plat-1 + `proxyresolve_test.go`.**
- [x] ESC-035-RETRY — PAR-ROUTE-035 ships the chatURLs() URL-list builder + unit; if wiring live retry-on-next-URL would touch a frozen handler body, defer the retry wiring to a follow-up. — **RESOLVED: shipped the `chatURLs()` builder + unit. Live retry-on-next-URL wiring DEFERRED — the request/dispatch path (`ChatCompletion`/`ChatCompletionStream` bodies) is a frozen handler body; see FOLLOW-UP below.**
- [x] ESC-MODELS-HOOK (cite correction) — the brief cites factory.go:104 for live-catalog override; the models-LIST surface is actually ModelsHandler.List with the established Set*Lister additive-adapter pattern. — **RESOLVED + cite correction confirmed: the `ModelsHandler` (with the `Set*Lister` adapter pattern) lives in `internal/api/models.go`, NOT `internal/server/routes_openai.go` (the brief/§7 named routes_openai.go). 056/059 interfaces+setters landed in `internal/api/models.go`; the concrete adapters wired in `routes_openai.go`. 060 gates the 056 fetch inside the inference resolver. 053 went to `runner.go` (the sanctioned build-site alternative to factory.go), NOT factory.go.**
- [x] SPLIT — original w7-route split into w7-route-a (admin CRUD + quota) + w7-route-b (engine; selection.go micro-serial vs w7-plat-1, NO routes_admin slot). — **RESOLVED: w7-route-b complete, zero shared Go files with w7-route-a; selection.go micro-serial held (sole unmerged holder; w7-plat-1 merged) and released; held NO routes_admin slot.**
- [ ] ESC-WEB-EXEC (escalation / integration-only) — PAR-ROUTE-059 ships pseudo-model EXPOSURE (`{alias}/search`+`{alias}/fetch`) ONLY (read from connection metadata `webSearchConfig`/`webFetchConfig`). The LIVE web-search/fetch EXECUTION (reverse-engineered web endpoints, fragile, network-bound) is DEFERRED to a follow-up plan. — Never fabricate live web calls. Pseudo-models appear in `/v1/models` but serving them is not wired.
- [ ] FOLLOW-UP: ESC-LIVE-FETCH (integration-only) — PAR-ROUTE-056 ships the `LiveCatalogResolver` + injectable `LiveCatalogFetcher` seam (hermetic, fake-tested) + the upstream gate (060). Production wires a NIL fetcher by default (so `/v1/models` makes NO live network call). The real per-account HTTP fetcher for Kiro/Qoder dynamic models is integration-only and DEFERRED to a follow-up plan (it needs live provider calls; cannot be hermetically proven beyond the seam).
- [ ] FOLLOW-UP: ESC-PROJ-FETCH (integration-only) — PAR-ROUTE-053 ships the `ProjectIDManager` + injectable `ProjectIDResolver`/`ProjectIDPersister` seams (hermetic, fake-tested) wired into `AccountRunner` (nil = no-op). The real antigravity/gemini-cli project-ID HTTP fetch + store-backed persister are integration-only and DEFERRED to a follow-up plan.
- [ ] FOLLOW-UP: ESC-035-RETRY-WIRING — PAR-ROUTE-035 `chatURLs()` is built and unit-proven but the live retry-on-next-URL loop is NOT wired (the dispatch path is a frozen handler body). Wiring the fallback consumption into the request/retry path is a follow-up that must touch (or additively wrap) the provider dispatch.

## w7-prov-special-a — 2026-06-14
- [ ] ESC-A1: Accept partner-only vertex (PAR-PROV-012) as HAVE, native vertex format deferred? — determines matrix flip scope.
- [ ] ESC-A2: xiaomi-tokenplan claude-native model variant + tts/voice models deferred (no ModelEntry.TargetFormat field) — read-site/media concern.
- [ ] ESC-A3: qoder ?Encode=1+sigPath signing and azure resource-URL template must be transcribed verbatim from executors/{qoder,azure}.js at impl; DEFER a provider if its signing cannot be soundly reproduced — never fabricate.
- [ ] ESC-A4: perplexity-web (PAR-PROV-030) / grok-web (PAR-PROV-031) cookie-scraper web endpoints — RECOMMEND DEFER (leave MISSING). Operator decision to exclude from W7 100%-feasible.
- [ ] ESC-A5: confirm existing anthropic/chat.go emits x-api-key + ?beta=true for glm/kimi/minimax (vs OAuth bearer); parameterize additively if not.

### w7-prov-special-a — closeout resolutions (2026-06-14)
- ESC-A1 (vertex native — OPEN, operator decision): SHIPPED the partner-openai path only (`internal/providers/urltemplate` builds `…/projects/{projectId}/locations/global/endpoints/openapi/chat/completions` from `providerSpecificData.projectId`, Bearer auth from `schemas.Key.Value`). PAR-PROV-012 flipped **HAVE-partial**. The native gemini-on-vertex format + the SA-JSON→GCP-token (JWT assertion) exchange are DEFERRED. **Open question stands: does the operator accept partner-only vertex as PAR-PROV-012 HAVE?**
- ESC-A2 (xiaomi claude-native + media — RESOLVED-as-noted): `mimo-v2.5-pro-claude` (targetFormat:"claude") + the tts/voice models port as plain openai/media `ModelEntry` rows (no `ModelEntry.TargetFormat` field added). The claude-native read-site behavior + media serving remain a w7-prov-media / read-site concern.
- ESC-A3 (qoder — DEFERRED, binding): RESOLVED to DEFER. `executors/qoder.js` requires opaque COSY signing (RSA+AES+MD5, ~17 `Cosy-*` headers via `buildCosyHeaders`) + a proprietary body encoder (`qoderEncodeBody`) + a non-trivial custom request shape — not soundly reproducible from the ref. PAR-PROV-028 left **MISSING**. (The azure resource-URL half of ESC-A3 WAS reproducible and shipped GREEN.)
- ESC-A4 (perplexity-web/grok-web — DEFERRED, operator decision): RESOLVED to DEFER per recommendation. PAR-PROV-030/031 left **MISSING** (cookie-scraper, low value / high breakage). Carried by neither special-a nor special-b. Operator decision needed to formally exclude from W7 100%-of-feasible.
- ESC-A5 (claude auth/beta — RESOLVED): the existing `anthropic/chat.go` set `x-api-key`+`anthropic-version` but the messages URL came from `baseURL+"/v1/messages"` and there was no `?beta=true`/`Anthropic-Beta`. Resolved ADDITIVELY: `anthropic.NewForProvider(id, baseURL)` sets a `chatURL` (catalog base URL verbatim + `?beta=true`) and a `betaHeader` (CLAUDE_API_HEADERS `claude-code-20250219,interleaved-thinking-2025-05-14`); the `NewProvider()` path is unchanged (proven by `TestNewProviderUnchanged`).

## w7-prov-special-b — 2026-06-14
- [ ] ESC-B1: cursor connect+protobuf (PAR-PROV-023) — build only if the protobuf/connect wire layout is soundly reconstructable from executors/cursor.js; else DEFER. Never fabricate protobuf. Operator acceptance of cursor-deferred needed.
- [ ] ESC-B2: antigravity per-model→backend (gemini/claude/gpt-oss) mapping + fallback URL order transcribe-verbatim from executors/antigravity.js.
- [ ] ESC-B3: kiro/antigravity OAuth token ACQUISITION is w7-prov-oauth, not special-b; executors consume a supplied schemas.Key token + kiro refresh-on-401. Cross-plan dependency.
- [ ] ESC-B5: verify kiro model block presence in models.go (Wave-1 w1-i-kiro-pair) before adding — avoid duplicate-key panic.

### w7-prov-special-b — closeout resolutions (2026-06-14)
- ESC-B1 (cursor connect+protobuf — RESOLVED to **BUILD**): the full wire format was soundly reconstructed from the frozen ref (`cursorProtobuf.js` is a hand-rolled protobuf with an explicit `FIELD` number map + wire types; `cursorChecksum.js` is a deterministic Jyh cipher + sha256 + uuid-v5; `executors/cursor.js` defines the connect frame = 1-byte flags + 4-byte BE length + gzip per flag). Built `internal/providers/cursor` with golden wire-byte encode/decode/frame/checksum tests — NO fabrication. PAR-PROV-023 flipped **HAVE**. No operator deferral needed.
- ESC-B2 (antigravity backend matrix — RESOLVED-as-noted): antigravity is a single Gemini-on-`/v1internal:streamGenerateContent` wire format; the "multi-backend" (gemini/claude/gpt-oss) is per-model and resolved inside the existing converters (`openaiToAntigravityRequest` branches on `isClaudeModel`). `backendForModel` surfaces the selection for tests. Fallback URL order transcribed verbatim from `providers.js:106-108` (`daily-cloudcode-pa.googleapis.com` → `.sandbox.`). UA `antigravity/1.107.0` + `x-request-source: local` per `appConstants.js`.
- ESC-B3 (kiro/antigravity OAuth ACQUISITION — OPEN, cross-plan): the executors consume a supplied `schemas.Key` token (Bearer). The initial OAuth login + kiro custom-JSON refresh ACQUISITION remain w7-prov-oauth. PAR-PROV-020/022/023 catalog+executor parity is satisfied by the wire path. **Cross-plan dependency flagged for w7-prov-oauth.**
- ESC-B5 (kiro model block — RESOLVED): VERIFIED kiro was ABSENT from `models.go` at P0; added the 12-entry block (providerModels.js:127-146, the commented-out opus-4.5 excluded). No duplicate-key panic.
- PAR-MCP-060 (antigravity unavailable-tool ride-along — RESOLVED): implemented `cloakTools` (filter.go) — client tools renamed with the `_ide` suffix (native AG names preserved), then the 21 AG decoy tools injected with description "This tool is currently unavailable." Golden-tool-list tested.

## w7-prov-special (SPLIT decision) — 2026-06-14
- [ ] The WAVE-7-MAP w7-prov-special row was SPLIT into w7-prov-special-a (claude-format + URL-template/custom-JSON; JSON-only, low risk) and w7-prov-special-b (kiro eventstream + cursor protobuf + antigravity multi-backend; binary protocols, high risk). Both share the factory.go micro-serial, sub-serialized a→b with key-disjoint switch arms. Confirm orchestrator accepts the split + sub-serial order.

## w7-prov-oauth — 2026-06-14
All 8 brief providers were soundly buildable from the ref (every clientId/endpoint copied verbatim from `src/lib/oauth/constants/oauth.js` @ 827e5c3) — **NONE deferred**. The OAuth FLOW landed for all 8; the following are recorded follow-ups (not built here):
- [ ] **ESC-DEVICE-ENDPOINT (DEFERRED — admin transport for device-code).** The device-code FLOW ENGINE shipped (`StartDevice`/`PollDevice` in `internal/auth/oauth_device.go`) + the 3 device config factories + flows-map entries. The existing redirect-shaped `OAuthStart`/`OAuthCallback` do NOT fit device-code (user enters a code; server polls). A NEW `internal/admin/oauth_device.go` (`POST /api/oauth/{provider}/device/start` + `…/device/poll`) was NOT added because **w6-e shipped no provider-OAuth device-code modal** to mirror (the only w6-e oauth mock is `ui/e2e/mocks/handlers/mcp.ts`, which is MCP-instance auth, not provider OAuth). Per the plan, an absent device-code modal is a **w6-e UI gap** — recorded here, not built. A follow-up plan owns: (1) the device admin endpoints, (2) the matching UI device-code modal (show user_code + verification_uri + poll). The engine is fully consumable.
- [ ] **w6-e UI gap (provider-OAuth modals).** No `ui/e2e/mocks/handlers/<provider-oauth>.ts` exists for the 8 providers (redirect or device). The redirect providers (claude/codex/gemini-cli/iflow/cline) work via the existing `/api/oauth/{provider}/{start,callback}` handlers, but a UI modal to drive them is a separate UI-wave concern. No mock-body correction was needed (no diverging mock to correct).
- [ ] **ESC-GH-COPILOT (DEFERRED — GitHub Copilot-token mint).** The GitHub device-code flow yields a GitHub access_token (stored). Using Copilot requires a SECOND exchange to `copilotTokenUrl` (`https://api.github.com/copilot_internal/v2/token`, `oauth.js:149`), short-lived + re-minted per request. This adapter/runtime concern (w7-prov-* catalog / w7-usage-quota) is a follow-up; the OAuth-flow half (PAR-PROV-021) is satisfied.
- [ ] **kilocode orgId→Metadata (adapter follow-up).** The kilocode poll success path (`providers.js:1093-1102`) fetches `/api/profile` for the `orgId` that becomes the `X-Kilocode-OrganizationID` header. The OAuth flow stores the access_token; persisting orgId→`Connection.Metadata` is wired when the device admin transport + adapter land (the secret-at-rest store already supports plaintext `Metadata`).
- [ ] **kimi-coding (PAR-PROV-024) — low-effort follow-up, NOT built.** Outside the brief's exact 8. Its config IS sound (`oauth.js:213-217`: clientId `17e5f671-…`, deviceCodeUrl `https://auth.kimi.com/api/oauth/device_authorization`, tokenUrl `https://auth.kimi.com/api/oauth/token`) — trivially the same standard device-code path + a `buildKimiHeaders` quirk on refresh. Flagged, not built, to honor the brief's exact 8. (The brief's "024" was a row-number slip; kilocode is PAR-PROV-026, cline is PAR-PROV-025.)

## w7-prov-media — media/embedding specialist providers (2026-06-14)
BUILD-vs-DEFER call was made per-provider on two architectural facts in the live tree: (1) only `Embedding` is reachable — `POST /v1/embeddings` (`routes_openai.go:98` → `embeddings.go:100` `provider.Embedding`) is the sole non-chat media route; there is NO `/v1/images/generations`, `/v1/audio/transcriptions`, or `/v1/audio/speech` handler (only inert label strings in `models.go:406-409`). (2) voyage-ai's embedding wire is OpenAI-compatible and already templated (`openai/embedding.go`). voyage-ai (PAR-PROV-066) was BUILT (real adapter, factory dispatch, catalog/model/alias, hermetic tests); the other 11 were DEFERRED — building an image/STT/TTS adapter now would be dead, unreachable code (editing `internal/api/**`+`internal/server/**` is a transport wave, out of this plan's scope). Recorded follow-ups:
- [ ] **ESC-M1 (STT — deepgram PAR-PROV-053, assemblyai PAR-PROV-054 — DEFERRED).** `Transcription` exists on the interface (`schemas/provider.go:93`) but NO `/v1/audio/transcriptions` route is registered → an STT adapter is unreachable dead code. assemblyai is additionally a two-step upload-then-poll flow (`sttCore.js:62`) that is not hermetically determinable as a single request/response; deepgram `/v1/listen` is a distinct non-OpenAI wire. Pick up when a `/v1/audio/transcriptions` transport route exists.
- [ ] **ESC-M2 (image — nanobanana 055, fal-ai 058, stability-ai 059, black-forest-labs 060, recraft 061, sdwebui 063, comfyui 064, huggingface 065 — DEFERRED).** No `/v1/images/generations` route exists; `ImageGeneration` is stubbed 501 in every adapter. Several are async (fal-ai queue/poll, bfl poll), bespoke-wire (stability multipart), or self-hosted-runtime-URL (sdwebui, comfyui). huggingface is image+STT (NOT in the embedding registry `embeddingProviders/index.js:6-8`), so it does not qualify as the cheap embedding case. NO catalog stubs were added (a base_url with no adapter and no route is misleading).
- [ ] **ESC-M3 (video — runwayml 062 — DEFERRED).** runwayml's `gen4_turbo`/`gen3a_turbo` are `type:"video"`. The `schemas.Provider` interface has NO video method — video is unmappable without a schema change (`schemas/**` is frozen). Its image models share the ESC-M2 no-route blocker.
- [ ] **ESC-M4 (media-transport routes — open question for the orchestrator).** The real blocker for image/STT/TTS parity is the absence of v1 media routes (`/v1/images/generations`, `/v1/audio/transcriptions`, `/v1/audio/speech`) and any router media-dispatch path — a TRANSPORT/API-layer concern, not a provider-adapter one. Should a future wave add `internal/api/images.go` + `internal/api/audio.go` + `routes_openai.go` registration so the 11 deferred adapters become buildable+reachable? Until then those PAR rows are correctly MISSING.

## w7-usage-quota (provider quota/usage fetchers) - 2026-06-15
PAR-USAGE-032: remaining 6 provider usage fetchers (GitHub, Antigravity, Codex, Kiro, GLM, MiniMax) behind the SHIPPED w5-e dispatcher (`internal/usage/providerusage.go:132 FetchProviderUsage`; claude+gemini done). The plan adds one additive `case` arm + one `quota_<provider>.go` per BUILT provider; no admin/route/UI change (route exists, DTO unchanged). Recorded follow-ups:
- [ ] **ESC-REF-ABSENT (BINDING — the frozen 9router JS ref is NOT on this host).** The per-provider usage endpoint URL/auth/response-shape (`src/app/api/usage/[connectionId]/route.js:122-188`) cannot be read here (ref lives at macOS `/Users/heitor/.../_refs/9router`). The §1.4 endpoint table is the authoring-time best determination from the matrix + catalog auth facts + public docs. At impl, the executor MUST confirm each provider's concrete endpoint+auth+shape against the frozen ref BEFORE flipping DEFER→BUILD; a provider that cannot be soundly confirmed ships a graceful provider-named fallback arm and is recorded DEFERRED — NEVER fabricate an endpoint. Authoring split: github + antigravity BUILT (high confidence); codex/kiro/glm/minimax BUILD-IF-CONFIRMED.
- [ ] **ESC-PROVIDER-TYPE.** Confirm the exact `provider.Type` string a GitHub Copilot connection records (`"github"` vs `"github-copilot"` vs `"copilot"`) — the `case` literal must match the stored Type, not the matrix wording. glm/minimax/codex/kiro/antigravity Types are confirmed in-tree (catalog/oauth_providers).
- [ ] **ESC-KIRO-SIGV4.** If the CodeWhisperer usage/limits endpoint needs AWS SigV4 signing (absent in-tree), DEFER kiro rather than importing a signer. BUILD only if route.js shows a simple bearer-token limits call.
- [ ] **ESC-OAUTH-DEP (NOTE — runtime-only).** MAP §187 "may depend on w7-prov-oauth" is for live token availability at runtime, NOT a code seam: every fetcher is hermetically unit-tested with a canned token, so the plan is code-unblocked today; a provider with no shipped OAuth flow still builds+tests and returns the graceful `{message}` at runtime when no connection token exists.
- [ ] **Matrix flip gate.** Flip PAR-USAGE-032 PARTIAL→HAVE only if all 6 are soundly built-or-deferred (each BUILT fetcher hermetically tested; each DEFER arm returning a tested fallback). Record the exact built-vs-deferred split; if any provider is in an unsound half-state, leave PARTIAL with a per-provider footnote.

### w7-usage-quota IMPL OUTCOME (2026-06-15, base 048df9a)
- **ESC-REF-ABSENT — CONFIRMED ABSENT at impl.** P5 verified the frozen `route.js` is NOT on this Linux host (`/Users/heitor/.../_refs/9router/...` does not exist here). No per-provider usage endpoint could be read from the ref. Per the binding rule, NO endpoint was fabricated. Per-provider verdicts:
  - **antigravity — BUILT.** Implemented `fetchAntigravityUsage` (`internal/usage/quota_antigravity.go`) as the structural twin of the SHIPPED gemini Cloud-Code fetcher: POST `/v1internal:retrieveUserQuota` `{project}` against the catalog-confirmed base `https://daily-cloudcode-pa.googleapis.com` (`catalog.go:100`), Bearer auth, project-id from `conn.Metadata` via the existing `projectIDFromMetadata`. The endpoint is grounded in the in-tree gemini precedent (not the absent ref), so it is sound, not fabricated. Hermetically tested (`TestAntigravityUsageFetcher`): method/path/Bearer + bucket→snake_case quota mapping + missing-project/missing-token/non-2xx graceful `{plan,message}` + no-camelCase + token-no-leak.
  - **github — DEFERRED.** route.js absent AND in-tree signal `oauth_providers.go:116` records the Copilot-token mint as deferred (ESC-GH-COPILOT) → no usable Copilot token in-tree and no confirmable quota endpoint. Ships a graceful provider-named fallback arm (`case "github"` → "Usage API not yet available for GitHub Copilot."). No `quota_github.go` created.
  - **codex — DEFERRED.** route.js absent; the ChatGPT/Codex usage path is provider-internal (`backend-api`) and cannot be soundly confirmed without the ref. Graceful fallback arm only.
  - **kiro — DEFERRED.** route.js absent; CodeWhisperer limits call may require AWS SigV4 (ESC-KIRO-SIGV4) with no signer in-tree, and the path is unconfirmable. Graceful fallback arm only.
  - **glm — DEFERRED.** route.js absent; `x-api-key` auth (`conn.Secret`) is clear but the z.ai usage path is unconfirmable. Graceful fallback arm only.
  - **minimax — DEFERRED.** route.js absent; `x-api-key` auth is clear but the account/quota path is unconfirmable. Graceful fallback arm only.
- **ESC-PROVIDER-TYPE — RESOLVED for the case literals used.** `github` Provider Type = `"github"` (`oauth_providers.go:124`); `codex` = `"codex"` (`:37`); `glm`/`minimax`/`kiro`/`antigravity` = catalog Type strings (`catalog.go:377,397,81,98`). The DEFER arms (and the antigravity BUILT arm) match these recorded Types.
- **ESC-KIRO-SIGV4 — OUTCOME: DEFER.** No signer imported; kiro ships the graceful fallback arm.
- **ESC-OAUTH-DEP — confirmed runtime-only.** All arms compile+test hermetically with canned/absent tokens; antigravity returns the graceful `{plan,message}` when token/project is absent (mirrors the gemini guard).
- **Built-vs-deferred split (PAR-USAGE-032):** BUILT = {claude, gemini (w5-e), antigravity (w7)}; DEFERRED-with-tested-fallback = {github, codex, kiro, glm, minimax}. Matrix kept **PARTIAL** (not flipped to HAVE) because 5/8 providers still return a fallback rather than a real external-quota fetch; flip to HAVE when route.js confirms the deferred endpoints. All 6 arms are present and the dispatch table (`TestProviderUsageDispatch`) asserts each routes away from the generic `default`.

## BIFROST-MAP (bifrost parity program) - 2026-06-15
- [ ] ESC-REF-ABSENT (BINDING) — Bifrost ref `@ca21298` absent at `/Users/heitor/Developer/github.com/bloodf/_refs/bifrost` on this host — clone it (or run from a host that has it) before bf-mcp-1 / bf-core-2 / bf-openai-4 protocol-detail rows; until then build to matrix text only, STOP-escalate on undocumented detail.
- [ ] Plugin pipeline (PAR-BF-CORE-004..018, 049, 050) — fund the runtime re-architecture or record permanently-deferred. (RECOMMEND defer.)
- [ ] Clustering + in-memory store + CAS + ghost-node reconciliation (core 039..042; gov 004, 013, 025, 041, 049, 050) — product-category change for a single-binary SQLite gateway. (RECOMMEND permanent escalation.)
- [ ] MCP per-user OAuth/header credential + sessions surface (~22 mcp rows) — is multi-tenant MCP a product goal? Build server-mode + per-VK (bf-mcp-1/2) first regardless. (RECOMMEND defer the per-user surface.)
- [ ] OpenAI extension surface (video/containers/async/WS-responses/rerank/OCR/non-/v1-aliases/Azure, ~37 rows) — any in scope? rerank/OCR are deferred-buildable if a provider is funded. (RECOMMEND defer.)
- [ ] Request-normalization layer (PAR-BF-OAI-101..118) — fund a multi-format passthrough/normalization plan or defer. (RECOMMEND defer.)
- [ ] Customer governance tier (gov 008, 046, 047) + VK hash-index+AES (gov 006) — g0router phase-18 is VK+Team only; add Customer tier? (RECOMMEND defer; treat 006 as variant.)
- [ ] Error-envelope labeling (PAR-BF-OAI-301/302/303) — record `{data,error}` as variant vs augment `APIError` with `param`/`event_id`. (RECOMMEND variant + optional tiny augment in bf-openai-4.)
- [ ] AllowFallbacks / per-attempt fallback (core 019/020) — g0router has account-level fallback (`WithAccountFallback`); add an `AllowFallbacks`-equivalent flag (rides bf-core-1) or label variant-HAVE?

### bf-openai-1 outcomes (2026-06-15) — CLOSED
- **ESC-TEXTCOMP — OUTCOME: Option A taken.** openai `TextCompletion`/`TextCompletionStream` implemented for real
  (`internal/providers/openai/completions.go`) as a copy of the in-tree embedding+chat transport; closed PAR-BF-OAI-002
  + 207 hermetically. The 42 other providers keep their 501 stub (route surfaces their 501 cleanly). No fallback to Option B.
- **ESC-MODELSGET-STALE — OUTCOME: stale note confirmed, NO code change.** Live `ModelsHandler.Get` already filters one
  model by id + 404s. Added a green-confirming regression test; flipped PAR-BF-OAI-019 PARTIAL→HAVE; corrected the stale cite.
- **ESC-OPENAI-SHAPE — OUTCOME: honored.** `/v1/completions` returns bare OpenAI shapes (no `{data,error}` admin envelope).
- **ESC-STREAMCHUNK-FIELD — OUTCOME: forward raw, no schema edit.** `schemas.StreamChunk`/`completions.go` UNCHANGED.
- **P0 dirty-tree note (deferred to program author):** at bf-openai-1 P0 the tree carried pre-existing uncommitted changes to
  `ui/dist/index.html` (built artifact) and this `open-questions.md` (BIFROST-MAP section). bf-openai-1 left both untouched
  beyond its own sanctioned append here. Program author should decide whether the stray `ui/dist/index.html` edit is intended.

## bf-openai-2 (closeout) - 2026-06-15
- [x] **w7-prov-media partial-unblock (ESC-M4 answered):** bf-openai-2 adds the `/v1/audio/{speech,transcriptions}` + `/v1/images/{generations,edits,variations}` ROUTES + handlers + the openai provider's media methods — resolving w7-prov-media's ROUTE-existence blocker. The 11 DEFERRED media provider ADAPTERS (deepgram/assemblyai STT; nanobanana/fal-ai/stability-ai/black-forest-labs/recraft/sdwebui/comfyui/huggingface image; runwayml video) are now route-reachable but remain a SEPARATE follow-up wave — NOT built by bf-openai-2. — Why it matters: the deferred-media follow-up can now proceed as pure provider-adapter work; the transport gap is closed. OUTCOME: routes shipped (`internal/api/{audio,images}.go` + `internal/server/routes_openai.go`); follow-up = pure adapter transport for the 11 deferred providers.
- [x] **Speech-stream SSE shape (ESC-SPEECH-STREAM, decided at impl):** OUTCOME = Option A (SSE-drain) taken; PAR-BF-OAI-209 → HAVE. The speech-stream hermetic test feeds upstream SSE frames that pass through cleanly via the shared `streamSSE` template, so 209 ships HAVE (no fall to Option B / 501). If a real upstream emits raw audio chunks (not SSE), a future per-method Option B would be warranted — the route + handler are sound regardless. Recorded in `docs/WORKFLOW.md`. — Why it matters: 209 is HAVE on a real impl, not a 501.
- [ ] **Video (runwayml, ESC-M3) still fully deferred:** no video method on `schemas.Provider`, no `/v1/videos*` route in bf-openai-2; unblocking video needs a schema + interface change (out of the additive-only phase). — Why it matters: scopes the next media wave.

## bf-openai-3 (closeout) - 2026-06-15
- [x] **ESC-STATEFULNESS — OUTCOME: Option A (stateless passthrough) taken.** OpenAI files/batches are stateful at the protocol level, but the STATE lives UPSTREAM at OpenAI — g0router is a thin proxy. Evidence: the provider-method signatures take `key schemas.Key` (the upstream credential) + return upstream objects (`internal/schemas/provider.go:96-105`), identical to the SHIPPED stateless media methods; the stubs lived in the PROVIDER layer not a store layer; every shipped openai method is a thin upstream proxy; NO files/batches store seam/table/worker exists. bf-openai-3 implemented the 9 openai-provider methods (`internal/providers/openai/{files,batches}.go`) as upstream proxies + wired the 9 routes (`internal/api/{files,batches}.go` + `internal/server/routes_openai.go`). NO new SQLite tables, NO migrations, NO worker, NO `New(...)` signature change. The other 42 providers' File*/Batch* stubs are UNTOUCHED (keep their 501). — Why it matters: closes 020/021/022/023/025/026/027/028/029 additively without a stateful subsystem.
- [ ] **ESC-LOCAL-STORE (DEFERRED — future open question, NOT bf-openai-3).** IF a future wave genuinely wants a g0router-MANAGED file/batch store (local SQLite tables, additive migrations, an async batch-execution worker that fans a JSONL input out to per-line completions, accrues `BatchRequestCounts`, writes an `output_file_id`, transitions `status` validating→in_progress→finalizing→completed), that is a SEPARATE, deliberate, consensus-gated plan (a stateful subsystem, far beyond route-over-stub). bf-openai-3 did NOT build it; OpenAI remains the system of record under Option A. — Why it matters: the option is recorded so it is not lost, but it is OUT of the buildable-additive bifrost phase.
- [ ] **ESC-BATCH-RESULTS (PAR-BF-OAI-024) — ESCALATED, STAYS MISSING.** `GET /v1/batches/{batch_id}/results` has NO provider interface method and NO schema (`internal/schemas/provider.go:102-105` declares only `BatchCreate`/`BatchList`/`BatchRetrieve`/`BatchCancel`; grep-confirmed there is no `BatchResults`/`batch_results` symbol anywhere in `internal/`). Adding a literal `/results` route would require a new interface method + schema — a NON-additive change touching all 43 providers' Provider implementations — which is OUT of the buildable-additive bifrost phase. **The capability the row describes IS reachable through PAR-BF-OAI-029 (HAVE, this plan):** OpenAI canonically exposes a completed batch's results via its `output_file_id`, fetched with `GET /v1/files/{output_file_id}/content`. So 024 STAYS MISSING in the matrix with a cross-reference to 029; a literal `/results` route is a future, deliberate interface-changing plan if ever wanted. NEVER fabricate a `BatchResults` method/schema to "hit the 020..029 span". — Why it matters: honest matrix-shorthand-vs-interface reconciliation; the results capability is not actually missing.
- [x] **ESC-RESOLVE-NO-MODEL — OUTCOME: empty-model sentinel resolves openai.** Files/batches requests carry NO `model` field. Verified at impl: `inference.Router.Resolve("")` → `providerForModel("")` falls through to the `default: return "openai", true` branch (`internal/inference/factory.go`), landing the openai provider + its key. The handlers call `resolveAndGate(ctx, ...)` with a `const model = ""` sentinel — the minimal sound resolution, no fabricated key, no hard-coded credential. The x-g0-vk gate + pinned-key override still apply (mirroring `AudioHandler.resolveAndGate`). — Why it matters: the no-model routes resolve deterministically to openai without an interface change.
- [x] **ESC-FILE-CONTENT-BYTES + ESC-MULTIPART-UPLOAD — OUTCOME: honored, SHIPPED patterns reused.** `GET /v1/files/{id}/content` returns RAW bytes (`FileContent` clones upstream `resp.Body()`, mirror Speech `audio.go:65`); the handler writes them verbatim with `Content-Type: application/octet-stream` — NO jsonMarshal, NO `{data}`/`{error}`. `POST /v1/files` parses multipart/form-data by REUSING the bf-openai-2 SHIPPED helpers (`isMultipart`/`readMultipartFile`/`formValue` inbound; stdlib `mime/multipart.NewWriter` outbound) with an explicit `file`+`purpose` whitelist (+ optional `filename`, defaulting to the file part header). Hermetic tests use in-memory multipart — no real files/network. The bare OpenAI list shapes (`FileListResponse`/`BatchListResponse`) legitimately carry a top-level `data` array (the real payload, NOT the admin envelope) — tests assert only the absence of a top-level `error` key for those. — Why it matters: the one non-JSON route + the one multipart route are sound and ESC-REF-ABSENT-safe.

## bf-openai-4 (closeout) - 2026-06-15
- [x] **ESC-COUNT-BODY (PAR-BF-OAI-004) — OUTCOME: Option (a) chat-shaped body, hermetic green.** The upstream `/v1/responses/input_tokens` endpoint is Responses-shaped but `CountTokens` receives a translated `*ChatRequest` (the handler reuses the SHIPPED responses→chat translation with `stream=false`). Resolved at impl with Option (a): OpenAI's tokenizer is model-keyed and body-shape-tolerant, so the openai `CountTokens` (`internal/providers/openai/counttokens.go`) posts the chat-shaped body and decodes `{input_tokens|tokens: N}` → `*TokenCountResponse{Tokens:N}`. The hermetic test asserts the handler→provider→upstream round-trip with a fake upstream returning `{"input_tokens":42}` → handler emits `{"tokens":42}`. NO interface-signature change, NO fall to Option B (501). — Why it matters: 004 ships HAVE on a real upstream proxy, not a stub.
- [ ] **PAR-BF-OAI-005 (`/v1/responses/compact`) — ESCALATED, STAYS MISSING (ESC-COMPACTION).** There is NO `Compaction` method on the `Provider` interface (`internal/schemas/provider.go:69-108` — grep-confirmed absent; the BIFROST-MAP's "interface method exists" premise is FALSE for Compaction — it conflated CountTokens, which DOES exist, with Compaction, which never did). NO `Compaction`/`Compact*` schema anywhere in `internal/schemas/`. Adding a `Compaction` interface method is a NON-additive change touching all 43 providers' Provider implementations (violates the no-dead-interface-methods guard); its wire shape is unverifiable without the frozen Bifrost ref (ESC-REF-ABSENT) and its behavior (server-side conversation compaction) is part of the responses-rewrite ESC. bf-openai-4 added NO Compaction method/schema/route/stub. A literal `/v1/responses/compact` route is a future, deliberate, interface-changing plan if ever funded. — Why it matters: honest matrix-shorthand-vs-interface reconciliation; never fabricate a Compaction surface to "hit the 005 row".
- [ ] **PAR-BF-OAI-202 (fasthttp internal-pipe bypass SSE reader) — ESCALATED, STAYS MISSING (ESC-SSE-PIPE).** Bifrost's `lib.NewSSEStreamReader` is a performance/transport abstraction; g0router writes directly to `ctx` by design. Pure perf, not a correctness gap. A future transport rework could adopt a streaming-reader abstraction; out of the buildable-additive bifrost phase. — Why it matters: scopes the divergence as deliberate, not a defect.
- [ ] **PAR-BF-OAI-205 (raw upstream-bytes passthrough) — STAYS MISSING (already MAP-ESC).** g0router always unmarshals/remarshals; raw `RawResponse` passthrough is a cross-cutting transport change. Out of scope. — Why it matters: confirms the prior MAP escalation still holds.
- [x] **PAR-BF-OAI-301/302/303 envelope (ESC, §1.6) — OUTCOME: variant-record; existing `param` surfaced; no restructuring.** g0router's flat OpenAI `{"error":{message,type,code[,param]}}` + admin `{data,error}` envelope is CANONICAL (AGENTS.md). The matrix:108 "lacks Param" note was STALE — `APIError.Param` ALREADY EXISTS (`internal/schemas/errors.go:7`) and so does `ProviderError.Param` (`errors.go:29`). bf-openai-4 made ONE tiny additive change: a `writeErrorWithParam` sibling helper (`internal/api/errors.go`; `writeError` delegates with nil, keeping all ~80 call sites stable) that surfaces a non-nil `param`, and `writeProviderError` now forwards `perr.Param`. `event_id`/`is_bifrost_error` were NOT added (variant-by-design — a different envelope contract). The envelope shape is UNCHANGED. — Why it matters: completes the OpenAI error object (`param`) additively without diverging g0router's canonical envelope.
- [ ] **PAR-BF-OAI-203 image-gen-stream half (ESC-IMG-EVENT-TYPE) — VARIANT, STAYS MISSING.** `event: <type>` SSE typing is HAVE for the responses path (`internal/translation/sse.go:144-153`). Image-gen streams (bf-openai-2, SHIPPED) use the passthrough processor (plain `data:`), not the responses translator; re-framing them with `event:` typing is a passthrough-processor change in bf-openai-2's domain. — Why it matters: scopes the remaining 203 half as a separate, deliberate change.
- [ ] **PAR-BF-OAI-204 skip-toggle half (ESC-DONE-SKIP) — VARIANT-by-design, STAYS MISSING.** The positive `[DONE]` terminator is HAVE (`internal/translation/stream.go:136`). g0router has no `includeEventType`/`skipDoneMarker` toggle and always terminates with `[DONE]` by design. — Why it matters: the negative clause is a Bifrost-specific toggle, not a g0router gap.
- [ ] **PAR-BF-OAI-304 mid-stream re-frame half (ESC-SSE-MIDSTREAM-FRAME) — VARIANT, STAYS MISSING.** The SSE-OPEN error half folds into PAR-BF-OAI-201 HAVE (the real bug fix: stream-open errors now return JSON with the correct content-type + the provider's real status). The mid-stream `event: error` RE-FRAME is a separate concern: mid-stream errors abort the channel (`internal/translation/stream.go:60-61`) and are logged — the channel is already torn down, so re-framing is a passthrough-processor change coupled to the responses-rewrite ESC. — Why it matters: distinguishes the fixed open-error half from the deferred mid-stream re-frame.
- [x] **The 003/208/203/204 FLIP discovery — OUTCOME: four matrix rows were STALE-MISSING; the live tree already HAD them.** `POST /v1/responses` (003), responses streaming (208), `event: <type>` typing (203 responses half), and `[DONE]` (204 positive half) were all already built (the MAP index predated/was independent of the SHIPPED bf-openai-1/2/3 work). bf-openai-4 added a regression-lock test (`TestResponsesStreamEventTypingAndDone`, `internal/api/responses_flip_test.go`) and flipped the rows to HAVE with cites — NO new feature code for these four. — Why it matters: honest closeout of the bifrost-openai surface; the matrix was overstating the gap.
- [x] **SSE-setup-ordering bug (PAR-BF-OAI-201) — OUTCOME: real bug fixed across all `/v1/*` streaming handlers.** The handlers set `text/event-stream` BEFORE confirming the provider stream opened, and on stream-open `*ProviderError` hardcoded `StatusBadGateway`. Fixed: open the stream FIRST; on `perr` call `writeError`/`writeProviderError` with the DEFAULT (`application/json`) content-type + the provider's real status (`perr.StatusCode`, fallback 502, mirroring the non-stream branch); set SSE headers ONLY after a successful open. Applied to `chat.go`, `responses.go`, `completions.go`, `audio.go` (Speech+Transcription), `images.go`, `messages.go`. Behavior-preserving on success, correctness-fixing on error. NOTE: `writeError` already overwrites the content-type to `application/json`, so the strongest observable assertion is the corrected status code; the content-type guard is retained as a regression check. — Why it matters: provider-open errors now return a correct OpenAI JSON error with the right status, not a malformed event-stream.
- [ ] **P0 dirty-tree note (carried forward, unchanged):** at bf-openai-4 P0 the tree still carried a pre-existing uncommitted change to `ui/dist/index.html` (built artifact — hashed asset filenames). bf-openai-4 left it UNTOUCHED (explicit `git add <file>` only; never `-A`). Same observation bf-openai-1/2/3 recorded; program author should decide whether the stray `ui/dist/index.html` edit is intended. — Why it matters: the working-tree artifact is isolated from this plan's commits.
- **OpenAI serial chain COMPLETE.** bf-openai-4 was the FINAL holder of the `internal/server/routes_openai.go` serial slot. On its close commit the OpenAI chain (bf-openai-1 → 2 → 3 → 4) is complete — no successor to release the slot to.

## bf-gov-1 — VK↔Team hierarchy — 2026-06-15
- [ ] Customer tier (gov 008/019/046/047 + CustomerID on 001/009) — ESC; needs operator decision (BIFROST-MAP Escalation §7). Why: third governance tier = scope expansion beyond the phase-18 VK+Team 2-level design.
- [ ] teams.budget_used_usd display accumulator — bf-gov-1 does NOT enforce against it (it has no live writer in the request path); the Team budget tier enforces via the live SumCostByTeam request_log aggregate instead (D8). The post-request write to keep budget_used_usd an accurate DISPLAY figure remains phase-18:69 middleware / bf-gov-3 work. Why: tracked so the display column does not silently diverge from the enforced aggregate.
- [ ] AllowAllKeys deny-semantics ("empty Keys + AllowAllKeys=false → no keys allowed") — HTTP rejection shape undocumented in the matrix; bf-gov-1 defaults to fall-through (D5: AllowAllKeys=true returns no pin; false+empty KeyIDs falls through to normal selection). Why: avoid inventing a 403 the frozen Bifrost ref cannot confirm (ESC-REF-ABSENT).
- [ ] IsActive tri-state (true *bool, nil-distinguishable) — bf-gov-1 uses the existing NOT-NULL-DEFAULT-1 column (D1, VAR=nil-means-active). A true *bool is an additive follow-up if an operator needs unset≠active.
- [ ] Decision/EvaluationResult enum (gov 035/036) — deferred to bf-gov-3; bf-gov-1 keeps the existing (bool,int,string) denial tuple with reasons "team budget exhausted"/"team rate limit exceeded".
- [ ] ValidateBudgetOwner live-consumer note (D2 no-leftovers) — the function is consumed live in the VK admin create/update validation path (internal/admin/virtualkeys.go:93). In g0router the VK-level Budget is intrinsically VK-owned and team_id is membership (not budget ownership), so the call is single-owner and guarded-but-live (D2 sanctions this) rather than reaching a >1-owner denial through the handler; the >1-owner deny case is covered by the governance unit test. A reachable multi-owner denial only materializes when a future Customer-tier (or provider/model budget-owner) plan adds a second owner FK and populates it into the same BudgetOwner. Why: documents that the no-leftovers STOP-condition is satisfied by a live caller, with the meaningful deny path deferred to the owner-set expansion.

## bf-gov-2 — typed WhiteList/BlackList allow/block semantics — 2026-06-15
- [ ] Provider key-level blacklists (gov 029/048) — ESC; g0router has no provider-key model store (schemas.Key is a bare credential, store.Connection persists secrets only, keys pinned via KeyIDs). Building a key-level blacklist would be a dead field (no live single-key model-gate). Why: needs operator decision to fund a provider-key model-policy store.
- [ ] WhiteList empty=deny-all is implemented + unit-tested at the TYPE level (schemas.WhiteList.IsAllowed); the GATE (matchProviderConfig, vk.go:120) deliberately preserves legacy match-all on empty `AllowedModels` (nil OR empty) for backward-compat (VAR, D1). Adopting strict empty=deny-all at the gate is a future opt-in setting, NOT built — to avoid silently denying every existing VK that serializes "allowed_models": [] as "no restriction". Why: confirm whether/when operators want a strict-gate opt-in.
- [ ] Distinct "ModelBlocked" decision/reason (gov 035/036 Decision enum) — bf-gov-2 reuses the existing 403 "provider/model not allowed for virtual key" reason for a blacklisted model (no new HTTP status, no new error envelope). Why: a distinct enum/reason is bf-gov-3 scope; record so the wire shape isn't invented (ESC-REF-ABSENT).
- [ ] List `["*"]` glob/prefix matching (e.g. gpt-*) — NOT built; matrix documents only the literal ["*"] all-case (D6). Why: glob semantics unverifiable without the ref; possible additive follow-up.
- [ ] WhiteList/BlackList.Validate mix-rule (D5) — defaults to "'*' cannot be combined with explicit models"; if the ref documents no such rule, Validate is a no-op for well-formed lists. The rule is consumed live in the VK admin create/update path (internal/admin/virtualkeys.go:75,78). Why: ESC-REF-ABSENT; confirm the intended validation contract.

## bf-gov-3 — dual-dim RL (SQL-live) + reset + Decision enum — 2026-06-15
- [ ] 10s in-memory→DB sync worker + startup-reset + graceful-flush (gov 016/023/044/045) — VAR/ESC by-design. g0router governance is fully SQL-live (budget bf-gov-1 D8, tokens+requests bf-gov-3 D1/D3): there is no in-memory accumulator to sync, reset, dump, or rehydrate. A worker would be the maintenance mechanism for exactly the sync.Map in-memory store the program ESCs. Restart-survival is automatic (every dimension reads persisted request_log live — strictly better than rehydrate/flush); calendar reset is inherent in the windowStart-bounded aggregate. Why: legitimate sound-parity (like 9router's deploy-relay escalations), NOT a missing worker.
- [ ] UsageUpdate/Accrue streaming-accrual type (gov 021, VK slice of 022) — VAR. g0router's SaveUsage (requestlog.go) is the streaming-aware finalized write: one request_log row per completed request with final usage = matrix quirk #6 (tokens once with usage data; one request on completion, never per-chunk). The SQL-live aggregates honor it inherently; an Accrue method would be dead code. Verified by internal/api/usage_glue_test.go:721 (one recorder entry with final usage). Why: verify the existing write, don't build a no-consumer type.
- [ ] EvaluationResult sub-structs (RateLimitInfo/BudgetInfo/UsageInfo per-dimension remaining/reset metadata, gov 036) — ESC-REF-ABSENT; bf-gov-3 carries {Decision,Reason,Status} only. Why: the rich sub-struct shapes are unverifiable without the frozen Bifrost ref.
- [ ] Decision values ModelBlocked/ProviderBlocked/MCPToolBlocked (gov 035) — DECLARED in the gov-3 enum but EMITTED by bf-gov-2 (lists) / bf-mcp tiers, not gov-3. Why: shared enum type; emission owned by the sibling plans.
- [ ] CAS-spin atomic bumps (gov 013/018-CAS/041) — ESC; g0router "accrues" by the persisted request_log row + checks the live SQL aggregate (the MAP-directed 018 SQL variant). Why: presupposes the sync.Map in-memory store (ESC).
- [ ] global/user/per-user-scoped/per-VK-scoped usage tiers (gov 022 non-VK, 043) — ESC; bf-gov-3 builds the VK→Team slice only. Why: same slice boundary as bf-gov-1.
- [ ] Cluster remote-baseline rate check + ghost-node reconciliation (gov 040, 024, 025) — ESC; single-binary SQLite gateway. Why: clustering is a product-category change.
- [ ] Decision error.code surfaced only at the chat denial site so far (internal/api/chat.go via api.DecisionCodeForReason). The other gate-denial handlers (audio/batches/completions/embeddings/files/images/input_tokens/messages/responses) still pass nil code. Why: the live + tested surfacing exists (D8 satisfied); extending it to every handler is an additive follow-up, not a correctness gap.
- [ ] Dual-dimension SQL hot-path cost — token SUM + request COUNT are read per gate call (alongside the shipped budget SUM); D1 notes they MAY be folded into one combined query. If profiling shows a hot-path concern, that single-query optimization (or a cached snapshot) is an additive follow-up. Why: tracked as a perf knob, not a correctness gap (and still no worker — a cache would be process-local + best-effort).

## bf-core-1 — provider-interface reconciliation — 2026-06-15
- [ ] Compaction (`/v1/responses/compact`, PAR-BF-CORE-001 subset / bf-openai-4 005) — ESC. No funded route → no interface method added (would be dead on 43 providers). Owner: the plan that funds the /v1/responses/compact route adds method+schema+impl together. Why: route-first prevents a dead interface method (Wave-5 NewWithShutdown lesson). Also ESC-REF-ABSENT for the wire shape.
- [ ] GatewayContext typed KV / request-metadata (PAR-BF-CORE-028) — ESC/VAR. No live consumer today; RequestID-only is g0router's minimal design. A field is added only by a future LIVE feature (request-metadata stamping / plugin / tracing) that reads it, in the same plan. Why: avoid a dead context field.
- [ ] AllowFallbacks flag on ProviderError (PAR-BF-CORE-020) — VAR; not added (dead flag). g0router's fallback is Verdict/StatusCode-driven (errorclass.go + selection.go + runner.go). Honoring a per-attempt flag would require rewiring the working fallback engine (non-additive). Why: variant-HAVE; per-attempt PreLLMHook mechanism is plugin-ESC.
- [ ] 20+ request-type constants (PAR-BF-CORE-027 enum half) — VAR; not added (dead constants; most enumerate ESC surfaces). g0router uses string literals + dedicated handlers.
- [ ] bifrost-core large escalation set (plugin pipeline, ProviderQueue, pooling, KVStore/clustering, CEL/adaptive-LB/health-states, OTEL/tracing, vector backends — rows 003-018,021-026,029-033,037-050) — ESC per BIFROST-MAP §8; revisit only if an operator funds the respective re-architecture.

## bf-mcp-1 — MCP server-mode foundation — 2026-06-15
- [ ] Per-VK tool scoping for /mcp — ESC; bf-mcp-2 (rows 004/019/020/033). bf-mcp-1 resolves the VK but does NOT scope tools. Why: global un-scoped surface is the foundation; scoping is the next plan.
- [ ] Mandatory-VK enforcement for /mcp (reject when NO VK is supplied) — bf-mcp-1 validates a PROVIDED VK and rejects an invalid/inactive one (D4), but an ABSENT VK is allowed (optional global surface); mandatory-when-absent + per-VK scoping is bf-mcp-2. Why: avoid breaking the global surface before per-VK lands.
- [ ] /mcp local-only vs public — bf-mcp-1 ships /mcp as VK-gated public (NOT in LOCAL_ONLY_PATHS, D2). Operator decision if some deployments need /mcp local-only. Why: it is the public MCP-server surface per matrix row 052.
- [ ] MCP server-mode numeric usage/cost shape (row 054) — bf-mcp-1's deferred finalizer writes a real best-effort `recordAudit` per /mcp tools/call (D8, the parity-meaningful deferred payload), but the exact NUMERIC usage/cost recorded for a /mcp tools/call is undocumented in the matrix (ESC-REF-ABSENT) and NOT invented. Why: the audit entry proves the deferral works over a real write; a usage-cost shape needs the ref or a g0router decision.
- [ ] complete-oauth create-vs-update distinction (row 039) — ESC; g0router has a single create flow (OAuth re-rotation 051 ESC). Why: update-flow presupposes the enterprise update path.
- [ ] config_mcp_clients enterprise columns (row 032: per_user/code_mode/tool_pricing/config_hash/allow_on_all_vk) — ESC; g0router aligns only the server-mode subset (D6). ZERO new columns added in bf-mcp-1 (no server-mode consumer). Why: per-user/enterprise/code-mode surface.
- [ ] mark3labs/mcp-go dependency (rows 075/076) — VAR; g0router rolls its own bridge (D1). Why: adding the dep would duplicate shipped framing.
- [ ] Per-user OAuth/header credential + sessions API (~22 rows) — ESC; the bifrost-mcp enterprise core (BIFROST-MAP §269).
- [ ] ESC-REF-ABSENT: the exact Bifrost initialize-capability object, JSON-RPC error-code numbering, and any non-standard framing are unverifiable; bf-mcp-1 built to the base MCP/JSON-RPC 2.0 spec g0router's own client already speaks (D1). Restore the ref to confirm wire fidelity.
