---
phase: 02-schemas-catalog
plan: 03
type: execute
wave: 1
depends_on: [02-01-PLAN.md, 02-02-PLAN.md]
files_modified:
  - internal/schemas/provider.go
  - internal/schemas/governance.go
  - internal/schemas/catalog.go
  - internal/schemas/mcp.go
  - internal/schemas/schemas_test.go
autonomous: true
requirements_addressed: [PROV-01, PROV-02, GOV-01, CATALOG-08]
---

<objective>
Define the Provider capability interface, governance types (virtual keys, provider configs), catalog metadata types, and a minimal MCP type stub. These are the binding contracts between the inference engine, providers, and governance layer.
</objective>

<tasks>
  <task type="auto">
    <name>Create provider.go with Provider interface and supporting types</name>
    <read_first>
      - internal/schemas/chat.go (for ChatRequest, ChatResponse, StreamChunk, Usage)
      - internal/schemas/completions.go (for TextCompletionRequest, TextCompletionResponse)
      - internal/schemas/embedding.go (for EmbeddingRequest, EmbeddingResponse)
      - internal/schemas/images.go (for ImageGenerationRequest, ImageGenerationResponse, ImageEditRequest, ImageVariationRequest)
      - internal/schemas/audio.go (for SpeechRequest, SpeechResponse, TranscriptionRequest, TranscriptionResponse)
      - internal/schemas/files.go (for FileObject, FileUploadRequest, FileListResponse, FileDeleteResponse)
      - internal/schemas/batch.go (for Batch, BatchCreateRequest, BatchListResponse)
      - internal/schemas/responses.go (for ResponsesRequest, ResponsesResponse)
      - internal/schemas/errors.go (for ProviderError)
      - internal/schemas/doc.go
      - docs/superpowers/specs/2026-06-08-9router-bifrost-port-design.md (provider interface)
    </read_first>
    <action>
      Create internal/schemas/provider.go with package schemas containing:
      - type ModelProvider string
      - const block with provider enum values:
        ProviderOpenAI ModelProvider = "openai"
        ProviderAnthropic ModelProvider = "anthropic"
        ProviderGemini ModelProvider = "gemini"
        ProviderGroq ModelProvider = "groq"
        ProviderMistral ModelProvider = "mistral"
        ProviderCohere ModelProvider = "cohere"
        ProviderFireworks ModelProvider = "fireworks"
        ProviderTogether ModelProvider = "together"
        ProviderDeepSeek ModelProvider = "deepseek"
        ProviderMiniMax ModelProvider = "minimax"
        ProviderOllama ModelProvider = "ollama"
        ProviderBedrock ModelProvider = "bedrock"
        ProviderVertex ModelProvider = "vertex"
        ProviderOpenRouter ModelProvider = "openrouter"
      - type GatewayContext struct with fields:
        RequestID string
      - type Key struct with fields:
        ID string
        Provider string
        Value string
      - type NetworkConfig struct with fields:
        Timeout int `json:"timeout"`
        ProxyURL string `json:"proxy_url,omitempty"`
        MaxRetries int `json:"max_retries,omitempty"`
      - type PostHookRunner interface with method:
        Run(ctx *GatewayContext, response any) error
      - type ListModelsResponse struct with fields:
        Object string `json:"object"`
        Data []ModelEntry `json:"data"`
      - type ModelEntry struct with fields:
        ID string `json:"id"`
        Object string `json:"object"`
        Created int64 `json:"created"`
        OwnedBy string `json:"owned_by"`
      - type TokenCountResponse struct with fields:
        Tokens int `json:"tokens"`
      - type Provider interface with methods:
        GetProvider() ModelProvider
        SetNetworkConfig(config NetworkConfig)
        ListModels(ctx *GatewayContext, key Key) (*ListModelsResponse, *ProviderError)
        ChatCompletion(ctx *GatewayContext, key Key, request *ChatRequest) (*ChatResponse, *ProviderError)
        ChatCompletionStream(ctx *GatewayContext, postHookRunner PostHookRunner, key Key, request *ChatRequest) (chan *StreamChunk, *ProviderError)
        TextCompletion(ctx *GatewayContext, key Key, request *TextCompletionRequest) (*TextCompletionResponse, *ProviderError)
        TextCompletionStream(ctx *GatewayContext, postHookRunner PostHookRunner, key Key, request *TextCompletionRequest) (chan *StreamChunk, *ProviderError)
        Responses(ctx *GatewayContext, key Key, request *ResponsesRequest) (*ResponsesResponse, *ProviderError)
        ResponsesStream(ctx *GatewayContext, postHookRunner PostHookRunner, key Key, request *ResponsesRequest) (chan *StreamChunk, *ProviderError)
        Embedding(ctx *GatewayContext, key Key, request *EmbeddingRequest) (*EmbeddingResponse, *ProviderError)
        ImageGeneration(ctx *GatewayContext, key Key, request *ImageGenerationRequest) (*ImageGenerationResponse, *ProviderError)
        ImageGenerationStream(ctx *GatewayContext, postHookRunner PostHookRunner, key Key, request *ImageGenerationRequest) (chan *StreamChunk, *ProviderError)
        ImageEdit(ctx *GatewayContext, key Key, request *ImageEditRequest) (*ImageGenerationResponse, *ProviderError)
        ImageVariation(ctx *GatewayContext, key Key, request *ImageVariationRequest) (*ImageGenerationResponse, *ProviderError)
        Speech(ctx *GatewayContext, key Key, request *SpeechRequest) (*SpeechResponse, *ProviderError)
        SpeechStream(ctx *GatewayContext, postHookRunner PostHookRunner, key Key, request *SpeechRequest) (chan *StreamChunk, *ProviderError)
        Transcription(ctx *GatewayContext, key Key, request *TranscriptionRequest) (*TranscriptionResponse, *ProviderError)
        TranscriptionStream(ctx *GatewayContext, postHookRunner PostHookRunner, key Key, request *TranscriptionRequest) (chan *StreamChunk, *ProviderError)
        FileUpload(ctx *GatewayContext, key Key, request *FileUploadRequest) (*FileObject, *ProviderError)
        FileList(ctx *GatewayContext, key Key) (*FileListResponse, *ProviderError)
        FileRetrieve(ctx *GatewayContext, key Key, fileID string) (*FileObject, *ProviderError)
        FileDelete(ctx *GatewayContext, key Key, fileID string) (*FileDeleteResponse, *ProviderError)
        FileContent(ctx *GatewayContext, key Key, fileID string) ([]byte, *ProviderError)
        BatchCreate(ctx *GatewayContext, key Key, request *BatchCreateRequest) (*Batch, *ProviderError)
        BatchList(ctx *GatewayContext, key Key) (*BatchListResponse, *ProviderError)
        BatchRetrieve(ctx *GatewayContext, key Key, batchID string) (*Batch, *ProviderError)
        BatchCancel(ctx *GatewayContext, key Key, batchID string) (*Batch, *ProviderError)
        CountTokens(ctx *GatewayContext, key Key, request *ChatRequest) (*TokenCountResponse, *ProviderError)
    </action>
    <acceptance_criteria>
      - internal/schemas/provider.go exists
      - file contains `type Provider interface`
      - file contains `type ModelProvider string`
      - file contains `ProviderOpenAI ModelProvider = "openai"`
      - file contains `type GatewayContext struct`
      - file contains `type Key struct`
      - file contains `type NetworkConfig struct`
      - file contains `type PostHookRunner interface`
      - file contains `type ListModelsResponse struct`
      - file contains `type ModelEntry struct`
      - file contains `type TokenCountResponse struct`
      - Provider interface has at least 25 method signatures
      - `go test ./internal/schemas/...` compiles without error
    </acceptance_criteria>
  </task>

  <task type="auto">
    <name>Create governance.go with virtual key and provider config types</name>
    <read_first>
      - internal/schemas/chat.go (for json tag patterns)
      - internal/schemas/doc.go
      - docs/superpowers/specs/2026-06-08-9router-bifrost-port-design.md (governance section)
    </read_first>
    <action>
      Create internal/schemas/governance.go with package schemas containing:
      - type VirtualKey struct with fields:
        ID string `json:"id"`
        Name string `json:"name"`
        ProviderConfigs []ProviderConfig `json:"provider_configs"`
        Budget *Budget `json:"budget,omitempty"`
        RateLimitRPM *int `json:"rate_limit_rpm,omitempty"`
      - type ProviderConfig struct with fields:
        Provider string `json:"provider"`
        AllowedModels []string `json:"allowed_models"`
        KeyIDs []string `json:"key_ids"`
        Weight *float64 `json:"weight,omitempty"`
      - type Budget struct with fields:
        Limit float64 `json:"limit"`
        Period string `json:"period"`
        Used float64 `json:"used"`
    </action>
    <acceptance_criteria>
      - internal/schemas/governance.go exists
      - file contains `type VirtualKey struct`
      - file contains `type ProviderConfig struct`
      - file contains `type Budget struct`
      - ProviderConfig.AllowedModels field has tag `json:"allowed_models"`
      - VirtualKey.ProviderConfigs field has tag `json:"provider_configs"`
      - `go test ./internal/schemas/...` compiles without error
    </acceptance_criteria>
  </task>

  <task type="auto">
    <name>Create catalog.go with catalog metadata types</name>
    <read_first>
      - internal/schemas/chat.go (for json tag patterns)
      - internal/schemas/doc.go
      - docs/superpowers/specs/2026-06-08-9router-bifrost-port-design.md (catalog section)
    </read_first>
    <action>
      Create internal/schemas/catalog.go with package schemas containing:
      - type RequestType string
      - const block:
        RequestTypeChat RequestType = "chat"
        RequestTypeCompletion RequestType = "completion"
        RequestTypeEmbedding RequestType = "embedding"
        RequestTypeImage RequestType = "image"
        RequestTypeAudio RequestType = "audio"
        RequestTypeResponses RequestType = "responses"
      - type PricingEntry struct with fields:
        Provider string `json:"provider"`
        Model string `json:"model"`
        Mode RequestType `json:"mode"`
        InputPrice float64 `json:"input_price"`
        OutputPrice float64 `json:"output_price"`
        ImagePrice float64 `json:"image_price,omitempty"`
        AudioInputPrice float64 `json:"audio_input_price,omitempty"`
        AudioOutputPrice float64 `json:"audio_output_price,omitempty"`
        TieredPricing []Tier `json:"tiered_pricing,omitempty"`
      - type Tier struct with fields:
        Threshold int `json:"threshold"`
        InputPrice float64 `json:"input_price"`
        OutputPrice float64 `json:"output_price"`
      - type ModelCapability struct with fields:
        ContextWindow int `json:"context_window"`
        Modalities []string `json:"modalities"`
        Tools bool `json:"tools"`
        Reasoning bool `json:"reasoning"`
        NoTemperature bool `json:"no_temperature"`
      - type Cost struct with fields:
        InputCost float64 `json:"input_cost"`
        OutputCost float64 `json:"output_cost"`
        TotalCost float64 `json:"total_cost"`
    </action>
    <acceptance_criteria>
      - internal/schemas/catalog.go exists
      - file contains `type PricingEntry struct`
      - file contains `type ModelCapability struct`
      - file contains `type Cost struct`
      - file contains `type Tier struct`
      - file contains `type RequestType string`
      - file contains `RequestTypeChat RequestType = "chat"`
      - PricingEntry.Provider field has tag `json:"provider"`
      - `go test ./internal/schemas/...` compiles without error
    </acceptance_criteria>
  </task>

  <task type="auto">
    <name>Create mcp.go with minimal MCP type stubs</name>
    <read_first>
      - internal/schemas/chat.go (for json tag patterns)
      - internal/schemas/doc.go
    </read_first>
    <action>
      Create internal/schemas/mcp.go with package schemas containing:
      - type MCPClient struct with fields:
        ID string `json:"id"`
        Name string `json:"name"`
        Type string `json:"type"`
        Config map[string]any `json:"config,omitempty"`
      - type MCPInstance struct with fields:
        ID string `json:"id"`
        ClientID string `json:"client_id"`
        Name string `json:"name"`
        Transport string `json:"transport"`
        URL string `json:"url,omitempty"`
        Command string `json:"command,omitempty"`
        Args []string `json:"args,omitempty"`
        Env map[string]string `json:"env,omitempty"`
        Status string `json:"status"`
      - type MCPTool struct with fields:
        Name string `json:"name"`
        Description string `json:"description"`
        InputSchema map[string]any `json:"input_schema"`
      - type MCPToolGroup struct with fields:
        ID string `json:"id"`
        Name string `json:"name"`
        ToolNames []string `json:"tool_names"`
      These are minimal stubs; full MCP types arrive in Phase 14.
    </action>
    <acceptance_criteria>
      - internal/schemas/mcp.go exists
      - file contains `type MCPClient struct`
      - file contains `type MCPInstance struct`
      - file contains `type MCPTool struct`
      - file contains `type MCPToolGroup struct`
      - `go test ./internal/schemas/...` compiles without error
    </acceptance_criteria>
  </task>

  <task type="auto">
    <name>Verify all schema types compile together</name>
    <read_first>
      - internal/schemas/schemas_test.go
    </read_first>
    <action>
      Add a compile-check test to internal/schemas/schemas_test.go that references every exported type to prevent dead-code elimination and ensure cross-type compatibility:
      TestSchemaTypesCompile: create one instance of each type (ChatRequest, ChatResponse, TextCompletionRequest, TextCompletionResponse, EmbeddingRequest, EmbeddingResponse, ImageGenerationRequest, ImageGenerationResponse, SpeechRequest, SpeechResponse, TranscriptionRequest, TranscriptionResponse, FileObject, FileUploadRequest, Batch, BatchCreateRequest, ResponsesRequest, ResponsesResponse, APIError, ErrorResponse, ProviderError, GatewayContext, Key, NetworkConfig, ListModelsResponse, ModelEntry, TokenCountResponse, VirtualKey, ProviderConfig, Budget, PricingEntry, ModelCapability, Cost, MCPClient, MCPInstance, MCPTool, MCPToolGroup) and assign to `_`.
      Run `go test ./internal/schemas/...` and `go vet ./internal/schemas/...`.
    </action>
    <acceptance_criteria>
      - internal/schemas/schemas_test.go contains `func TestSchemaTypesCompile`
      - `go test ./internal/schemas/...` exits 0
      - `go vet ./internal/schemas/...` exits 0
      - `go build ./internal/schemas/...` exits 0
    </acceptance_criteria>
  </task>
</tasks>

<verification>
1. Run `go test ./internal/schemas/...` — must pass.
2. Run `go vet ./internal/schemas/...` — must pass.
3. Run `go build ./internal/schemas/...` — must pass.
</verification>

<success_criteria>
- Provider interface is defined with all capability methods.
- GatewayContext, Key, NetworkConfig, PostHookRunner, ListModelsResponse, ModelEntry, TokenCountResponse types exist.
- VirtualKey, ProviderConfig, Budget governance types exist.
- PricingEntry, ModelCapability, Cost, Tier, RequestType catalog types exist.
- MCP stub types exist.
- All types compile together without errors.
</success_criteria>

<must_haves>
- Provider interface has explicit methods for all OpenAI-compatible capabilities.
- ModelProvider is a string type with constants for all supported providers.
- ProviderConfig includes AllowedModels, KeyIDs, and Weight fields.
- PricingEntry includes InputPrice, OutputPrice, ImagePrice, AudioInputPrice, AudioOutputPrice, and TieredPricing.
- ModelCapability includes ContextWindow, Modalities, Tools, Reasoning, and NoTemperature.
- All JSON tags use snake_case with omitempty for optional fields.
</must_haves>
