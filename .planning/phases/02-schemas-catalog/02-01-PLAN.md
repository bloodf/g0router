---
phase: 02-schemas-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/schemas/chat.go
  - internal/schemas/completions.go
  - internal/schemas/embedding.go
  - internal/schemas/schemas_test.go
autonomous: true
requirements_addressed: [OPENAI-01, OPENAI-02, OPENAI-03, OPENAI-04, OPENAI-12]
---

<objective>
Define the core OpenAI-compatible request/response types for chat completions, text completions, embeddings, and streaming. These are the most frequently used wire-format structs in the gateway and must match the OpenAI API spec shape exactly, using snake_case JSON tags.
</objective>

<tasks>
  <task type="auto">
    <name>Create chat.go with chat completion types</name>
    <read_first>
      - internal/schemas/doc.go (existing package doc)
      - docs/superpowers/specs/2026-06-08-9router-bifrost-port-design.md (schema references)
    </read_first>
    <action>
      Create internal/schemas/chat.go with package schemas containing:
      - type ChatRequest struct with fields:
        Model string `json:"model"`
        Messages []Message `json:"messages"`
        Temperature *float64 `json:"temperature,omitempty"`
        MaxTokens *int `json:"max_tokens,omitempty"`
        TopP *float64 `json:"top_p,omitempty"`
        N *int `json:"n,omitempty"`
        Stream bool `json:"stream"`
        Stop []string `json:"stop,omitempty"`
        PresencePenalty *float64 `json:"presence_penalty,omitempty"`
        FrequencyPenalty *float64 `json:"frequency_penalty,omitempty"`
        LogitBias map[string]int `json:"logit_bias,omitempty"`
        User string `json:"user,omitempty"`
        Tools []Tool `json:"tools,omitempty"`
        ToolChoice *ToolChoice `json:"tool_choice,omitempty"`
        ResponseFormat *ResponseFormat `json:"response_format,omitempty"`
        Seed *int `json:"seed,omitempty"`
      - type ChatResponse struct with fields:
        ID string `json:"id"`
        Object string `json:"object"`
        Created int64 `json:"created"`
        Model string `json:"model"`
        Choices []Choice `json:"choices"`
        Usage *Usage `json:"usage,omitempty"`
      - type Message struct with fields:
        Role string `json:"role"`
        Content string `json:"content"`
        Name *string `json:"name,omitempty"`
        ToolCalls []ToolCall `json:"tool_calls,omitempty"`
        ToolCallID *string `json:"tool_call_id,omitempty"`
      - type Choice struct with fields:
        Index int `json:"index"`
        Message *Message `json:"message,omitempty"`
        FinishReason string `json:"finish_reason,omitempty"`
        Logprobs *Logprobs `json:"logprobs,omitempty"`
      - type Tool struct with fields:
        Type string `json:"type"`
        Function FunctionDefinition `json:"function"`
      - type ToolCall struct with fields:
        ID string `json:"id"`
        Type string `json:"type"`
        Function FunctionCall `json:"function"`
      - type FunctionDefinition struct with fields:
        Name string `json:"name"`
        Description *string `json:"description,omitempty"`
        Parameters map[string]any `json:"parameters,omitempty"`
      - type FunctionCall struct with fields:
        Name string `json:"name"`
        Arguments string `json:"arguments"`
      - type ToolChoice struct with fields:
        Type string `json:"type"`
        Function *FunctionChoice `json:"function,omitempty"`
      - type FunctionChoice struct with fields:
        Name string `json:"name"`
      - type ResponseFormat struct with fields:
        Type string `json:"type"`
        JSONSchema *JSONSchema `json:"json_schema,omitempty"`
      - type JSONSchema struct with fields:
        Name string `json:"name"`
        Schema map[string]any `json:"schema,omitempty"`
        Strict *bool `json:"strict,omitempty"`
      - type Logprobs struct with fields:
        Content []LogprobContent `json:"content,omitempty"`
      - type LogprobContent struct with fields:
        Token string `json:"token"`
        Logprob float64 `json:"logprob"`
        Bytes []int `json:"bytes,omitempty"`
        TopLogprobs []TopLogprob `json:"top_logprobs,omitempty"`
      - type TopLogprob struct with fields:
        Token string `json:"token"`
        Logprob float64 `json:"logprob"`
        Bytes []int `json:"bytes,omitempty"`
      - type Usage struct with fields:
        PromptTokens int `json:"prompt_tokens"`
        CompletionTokens int `json:"completion_tokens"`
        TotalTokens int `json:"total_tokens"`
        PromptTokensDetails *TokensDetails `json:"prompt_tokens_details,omitempty"`
        CompletionTokensDetails *TokensDetails `json:"completion_tokens_details,omitempty"`
      - type TokensDetails struct with fields:
        AudioTokens int `json:"audio_tokens,omitempty"`
        CachedTokens int `json:"cached_tokens,omitempty"`
        TextTokens int `json:"text_tokens,omitempty"`
        ImageTokens int `json:"image_tokens,omitempty"`
      - type StreamChunk struct with fields:
        ID string `json:"id"`
        Object string `json:"object"`
        Created int64 `json:"created"`
        Model string `json:"model"`
        Choices []StreamChoice `json:"choices"`
        Usage *Usage `json:"usage,omitempty"`
      - type StreamChoice struct with fields:
        Index int `json:"index"`
        Delta Message `json:"delta"`
        FinishReason *string `json:"finish_reason,omitempty"`
        Logprobs *Logprobs `json:"logprobs,omitempty"`
    </action>
    <acceptance_criteria>
      - internal/schemas/chat.go exists
      - file contains `type ChatRequest struct`
      - file contains `type ChatResponse struct`
      - file contains `type Message struct`
      - file contains `type Usage struct`
      - file contains `type StreamChunk struct`
      - ChatRequest.Temperature field has tag `json:"temperature,omitempty"`
      - Usage.PromptTokens field has tag `json:"prompt_tokens"`
      - `go test ./internal/schemas/...` compiles without error
    </acceptance_criteria>
  </task>

  <task type="auto">
    <name>Create completions.go with legacy completion types</name>
    <read_first>
      - internal/schemas/chat.go (for Usage reuse)
      - internal/schemas/doc.go
    </read_first>
    <action>
      Create internal/schemas/completions.go with package schemas containing:
      - type TextCompletionRequest struct with fields:
        Model string `json:"model"`
        Prompt string `json:"prompt"`
        Suffix *string `json:"suffix,omitempty"`
        MaxTokens *int `json:"max_tokens,omitempty"`
        Temperature *float64 `json:"temperature,omitempty"`
        TopP *float64 `json:"top_p,omitempty"`
        N *int `json:"n,omitempty"`
        Stream bool `json:"stream"`
        Logprobs *int `json:"logprobs,omitempty"`
        Echo *bool `json:"echo,omitempty"`
        Stop []string `json:"stop,omitempty"`
        PresencePenalty *float64 `json:"presence_penalty,omitempty"`
        FrequencyPenalty *float64 `json:"frequency_penalty,omitempty"`
        BestOf *int `json:"best_of,omitempty"`
        LogitBias map[string]int `json:"logit_bias,omitempty"`
        User string `json:"user,omitempty"`
      - type TextCompletionResponse struct with fields:
        ID string `json:"id"`
        Object string `json:"object"`
        Created int64 `json:"created"`
        Model string `json:"model"`
        Choices []TextCompletionChoice `json:"choices"`
        Usage *Usage `json:"usage,omitempty"`
      - type TextCompletionChoice struct with fields:
        Text string `json:"text"`
        Index int `json:"index"`
        Logprobs *Logprobs `json:"logprobs,omitempty"`
        FinishReason string `json:"finish_reason,omitempty"`
    </action>
    <acceptance_criteria>
      - internal/schemas/completions.go exists
      - file contains `type TextCompletionRequest struct`
      - file contains `type TextCompletionResponse struct`
      - file contains `type TextCompletionChoice struct`
      - TextCompletionRequest.MaxTokens field has tag `json:"max_tokens,omitempty"`
      - `go test ./internal/schemas/...` compiles without error
    </acceptance_criteria>
  </task>

  <task type="auto">
    <name>Create embedding.go with embedding types</name>
    <read_first>
      - internal/schemas/chat.go (for Usage pattern)
      - internal/schemas/doc.go
    </read_first>
    <action>
      Create internal/schemas/embedding.go with package schemas containing:
      - type EmbeddingRequest struct with fields:
        Input any `json:"input"`
        Model string `json:"model"`
        EncodingFormat *string `json:"encoding_format,omitempty"`
        Dimensions *int `json:"dimensions,omitempty"`
        User string `json:"user,omitempty"`
      - type EmbeddingResponse struct with fields:
        Object string `json:"object"`
        Data []Embedding `json:"data"`
        Model string `json:"model"`
        Usage *Usage `json:"usage,omitempty"`
      - type Embedding struct with fields:
        Object string `json:"object"`
        Embedding []float64 `json:"embedding"`
        Index int `json:"index"`
    </action>
    <acceptance_criteria>
      - internal/schemas/embedding.go exists
      - file contains `type EmbeddingRequest struct`
      - file contains `type EmbeddingResponse struct`
      - file contains `type Embedding struct`
      - EmbeddingRequest.Input field has tag `json:"input"`
      - Embedding.Embedding field has tag `json:"embedding"`
      - `go test ./internal/schemas/...` compiles without error
    </acceptance_criteria>
  </task>

  <task type="auto">
    <name>Add JSON round-trip tests to schemas_test.go</name>
    <read_first>
      - internal/schemas/schemas_test.go (existing placeholder)
      - internal/schemas/chat.go
      - internal/schemas/completions.go
      - internal/schemas/embedding.go
    </read_first>
    <action>
      Replace the placeholder test in internal/schemas/schemas_test.go with:
      1. TestChatRequestRoundTrip: create ChatRequest{Model:"gpt-4o", Messages:[]Message{{Role:"user",Content:"hello"}}, Temperature:ptrFloat64(0.7)}, marshal to JSON, unmarshal back, assert Model=="gpt-4o", len(Messages)==1, Messages[0].Role=="user", *Temperature==0.7.
      2. TestChatResponseRoundTrip: create ChatResponse{ID:"chat-123", Object:"chat.completion", Created:1234567890, Model:"gpt-4o", Choices:[]Choice{{Index:0,Message:&Message{Role:"assistant",Content:"hi"},FinishReason:"stop"}}}, marshal/unmarshal, assert ID=="chat-123", Choices[0].Message.Content=="hi".
      3. TestEmbeddingRequestRoundTrip: create EmbeddingRequest{Input:"hello", Model:"text-embedding-3-small"}, marshal/unmarshal, assert Input=="hello", Model=="text-embedding-3-small".
      4. TestStreamChunkRoundTrip: create StreamChunk{ID:"chatcmpl-123", Object:"chat.completion.chunk", Model:"gpt-4o", Choices:[]StreamChoice{{Index:0,Delta:Message{Role:"assistant",Content:"hi"}}}}, marshal/unmarshal, assert ID=="chatcmpl-123".
      Add a helper function `ptrFloat64(v float64) *float64` and `ptrInt(v int) *int` in the test file.
      All tests must pass with `go test ./internal/schemas/...`.
    </action>
    <acceptance_criteria>
      - internal/schemas/schemas_test.go contains `func TestChatRequestRoundTrip`
      - internal/schemas/schemas_test.go contains `func TestChatResponseRoundTrip`
      - internal/schemas/schemas_test.go contains `func TestEmbeddingRequestRoundTrip`
      - internal/schemas/schemas_test.go contains `func TestStreamChunkRoundTrip`
      - `go test ./internal/schemas/...` exits 0
      - `go vet ./internal/schemas/...` exits 0
    </acceptance_criteria>
  </task>
</tasks>

<verification>
1. Run `go test ./internal/schemas/...` — must pass.
2. Run `go vet ./internal/schemas/...` — must pass.
3. Run `go build ./internal/schemas/...` — must pass.
</verification>

<success_criteria>
- ChatRequest, ChatResponse, Message, Choice, Usage, and StreamChunk types are defined in internal/schemas/chat.go.
- TextCompletionRequest, TextCompletionResponse types are defined in internal/schemas/completions.go.
- EmbeddingRequest, EmbeddingResponse, Embedding types are defined in internal/schemas/embedding.go.
- JSON round-trip tests verify marshal/unmarshal correctness for all core types.
- No compiler errors or vet warnings.
</success_criteria>

<must_haves>
- ChatRequest and ChatResponse structs exist with all standard OpenAI fields.
- Usage struct is defined and reusable across chat, completions, and embeddings.
- StreamChunk struct supports SSE streaming for chat completions.
- EmbeddingRequest.Input uses type `any` to accept string or []string.
- All JSON tags use snake_case with omitempty for optional fields.
</must_haves>
