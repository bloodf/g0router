// Package schemas defines the shared Go types used across the g0router
// v2.0 codebase. All wire-format structs (request/response payloads, DTOs
// shared between the API, admin, inference, and persistence layers)
// live here so that no two packages can drift on shape.
package schemas
