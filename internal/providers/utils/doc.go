// Package utils contains the shared helpers used by every per-provider
// adapter: a configured fasthttp client, SSE scanner pools, common
// retry/timeout primitives, and the request/response envelopes the
// adapters exchange with the inference engine.
package utils
