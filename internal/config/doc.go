// Package config loads the runtime configuration: listener address,
// database path, secret keys, and feature flags. The loader merges
// defaults, environment variables, and an optional config file.
package config
