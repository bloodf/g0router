// Package catalog manages the model catalog and per-model pricing.
// It is the source of truth for "what models exist, what they cost,
// what providers can serve them" used by the inference engine.
package catalog
