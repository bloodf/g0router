// Package platform contains the 9router-specific features that set
// g0router v2.0 apart from a vanilla OpenAI proxy: RTK (remote tool
// kit), caveman compression, model combos, the BiFrost translator,
// the 9router node sync, and the proxy pool.
package platform
