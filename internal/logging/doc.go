// Package logging owns the request log and the audit trail. It
// captures every public-API call and every admin mutation so the
// dashboard's traffic and audit views have something to render.
package logging
